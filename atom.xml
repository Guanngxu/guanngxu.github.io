<?xml version="1.0" encoding="utf-8"?>
<feed xmlns="http://www.w3.org/2005/Atom">
    <id>https://www.guanngxu.com</id>
    <title>Guanngxu</title>
    <updated>2026-08-04T08:55:41.672Z</updated>
    <generator>https://github.com/jpmonette/feed</generator>
    <author>
        <name>Guanngxu</name>
        <uri>https://www.guanngxu.com</uri>
    </author>
    <link rel="alternate" href="https://www.guanngxu.com"/>
    <link rel="self" href="https://www.guanngxu.com/atom.xml"/>
    <subtitle>Keep trying</subtitle>
    <logo>https://www.guanngxu.com/images/avatar.png</logo>
    <icon>https://www.guanngxu.com/favicon.ico</icon>
    <rights>All rights reserved 2026, Guanngxu</rights>
    <entry>
        <title type="html"><![CDATA[涂鸦跳跃（Doodle Jump）从设计到实现（Python 版）]]></title>
        <id>https://www.guanngxu.com/iWhYq5Ansy/</id>
        <link href="https://www.guanngxu.com/iWhYq5Ansy/"/>
        <updated>2026-03-24T14:47:40.000Z</updated>
        <summary type="html"><![CDATA[打开游戏后，背景是一张带有淡黄色的方格纸，所有的元素（主角、平台、道具等）看起来都像是用彩笔随手涂鸦上去的，线条有些许的不规整，但是却显得灵动、亲切。
游戏主角是一个亮绿色、有四条小短腿、长着一个漏斗状长鼻子的外星生物。当按下空格时它会向上...]]></summary>
        <content type="html"><![CDATA[<p>打开游戏后，背景是一张带有淡黄色的方格纸，所有的元素（<code>主角</code>、<code>平台</code>、<code>道具</code>等）看起来都像是用彩笔随手涂鸦上去的，线条有些许的不规整，但是却显得灵动、亲切。</p>
<p><code>游戏主角</code>是一个亮绿色、有四条小短腿、长着一个漏斗状长鼻子的<code>外星生物</code>。当按下空格时它会向上蹦跳，整个过程中都可以通过左右方向键控制游戏主角左右移动，蹦跳时借助绿色<code>平台</code>不断攀升到新的高度，以此来获得更高的分数。</p>
<p>在<code>游戏主角</code>向上攀升的过程中，偶尔会运气好遇到一些<code>加速神器</code>，帮助<code>玩家</code>更快的获得分数，这些<code>加速神器</code>包括<code>弹簧</code>、<code>竹蜻蜓</code>、<code>火箭喷气背包</code>。</p>
<p>当<code>游戏主角</code>踩到<code>弹簧</code>时会发出清脆的「叮」的声音，并让<code>游戏主角</code>获得比自主跳跃要更高的跳跃高度。如果<code>游戏主角</code>踩到<code>竹蜻蜓</code>，<code>竹蜻蜓</code>会戴在<code>游戏主角</code>头顶，并像<code>直升机</code>一样拖着<code>游戏主角</code>飞行一段距离，期间会发出直升机螺旋桨转动的声音。若<code>游戏主角</code>有幸遇到了<code>火箭喷气背包</code>，它可以获得比<code>竹蜻蜓</code>更快的飞行速度，并且会带着<code>游戏主角</code>飞行更久的时间，自然也会获得更多的分数。</p>
<p>整个游戏没有终点，只有不断增加的分数。在<code>游戏主角</code>跳跃过程中始终都会有重力存在，因此若<code>游戏主角</code>下落过程中没有遇到<code>平台</code>便会一直下坠，直到坠落超过底部则 Game Over。</p>
<h3 id="领域建模">领域建模</h3><p>前文是对 Doodle Jump 游戏的场景及玩法描述，虽然相比各大游戏平台的版本，我的这个描述简化了许多，但请不要在意这些细节。在编辑前文描述的过程中我已经将其中的名词做了特殊标记，下面我们把前文出现的名词去重后列举出来。</p>
<blockquote>
<p>游戏主角、外星生物、平台、加速神器、玩家、弹簧、竹蜻蜓、火箭喷气背包</p>
</blockquote>
<p>可以发现其中的 游戏主角、外星生物、玩家其实指的是同一事物，我们给它统一为<strong>玩家</strong>。平台姑且就叫<strong>平台</strong>吧。加速神器我们给它取名为<strong>道具</strong>，弹簧、竹蜻蜓、火箭喷气背包的名字保持不变，则我们就有了下面的统一语言。</p>
<ul>
<li>玩家：游戏的控制主体，拥有重力属性，可以进行跳跃；</li>
<li>平台：玩家可以接力向上，或是可以停留保持不下坠的载体；</li>
<li>道具：可以改变玩家物理状态，比如跳跃的初始速度、赋予飞行的能力等，弹簧、竹蜻蜓、火箭喷气背包均为某一具体的道具。</li>
</ul>
<p>当然仅仅只有上面的名词表还不够，每个名词仅仅只是一个演员而已，我们还需要一个优秀的导演来指导才能演一出好戏，这个导演在领域驱动设计（DDD）里面叫做<strong>聚合根（Aggregate Root）</strong>。</p>
<h3 id="代码实现">代码实现</h3><p>有了前文的分析我们就可以开始写代码了，首先可以搭建出下面的框架结构。</p>
<pre class="hljs"><code><span class="hljs-keyword">class</span> <span class="hljs-title class_">Player</span>: <span class="hljs-comment"># 玩家</span>
    <span class="hljs-keyword">pass</span>

<span class="hljs-keyword">class</span> <span class="hljs-title class_">Platform</span>: <span class="hljs-comment"># 平台</span>
    <span class="hljs-keyword">pass</span>

<span class="hljs-keyword">class</span> <span class="hljs-title class_">Item</span>: <span class="hljs-comment"># 道具</span>
    <span class="hljs-keyword">pass</span>

<span class="hljs-keyword">class</span> <span class="hljs-title class_">Spring</span>(<span class="hljs-title class_ inherited__">Item</span>): <span class="hljs-comment"># 弹簧</span>
    <span class="hljs-keyword">pass</span>

<span class="hljs-keyword">class</span> <span class="hljs-title class_">Propeller</span>(<span class="hljs-title class_ inherited__">Item</span>): <span class="hljs-comment"># 竹蜻蜓</span>
    <span class="hljs-keyword">pass</span>

<span class="hljs-keyword">class</span> <span class="hljs-title class_">Rocket</span>(<span class="hljs-title class_ inherited__">Item</span>): <span class="hljs-comment"># 火箭喷气背包</span>
    <span class="hljs-keyword">pass</span>

<span class="hljs-keyword">class</span> <span class="hljs-title class_">GameSession</span>: <span class="hljs-comment"># 聚合根（导演）</span>
    <span class="hljs-keyword">pass</span>


<span class="hljs-keyword">if</span> __name__ == <span class="hljs-string">&quot;__main__&quot;</span>:
    <span class="hljs-keyword">pass</span>
</code></pre>
<p>我们采用 <a href="https://www.pygame.org/">Pygame</a> 来实现 Doodle Jump，因此需要导入 Pygame 包，并快速验证一下 Pygame 是否可以正常使用，直接完善<code>GameSession</code>类的代码即可。</p>
<pre class="hljs"><code><span class="hljs-keyword">import</span> pygame
<span class="hljs-keyword">import</span> sys


SCREEN_WIDTH, SCREEN_HEIGHT = <span class="hljs-number">400</span>, <span class="hljs-number">600</span>  <span class="hljs-comment"># 定义游戏窗口的宽度和高度</span>
FPS = <span class="hljs-number">60</span>                                <span class="hljs-comment"># 每秒刷新的帧数，控制游戏运行流畅度</span>

<span class="hljs-keyword">class</span> <span class="hljs-title class_">GameSession</span>:
    <span class="hljs-keyword">def</span> <span class="hljs-title function_">__init__</span>(<span class="hljs-params">self</span>):
        pygame.init()
        <span class="hljs-variable language_">self</span>.screen = pygame.display.set_mode((SCREEN_WIDTH, SCREEN_HEIGHT))
        pygame.display.set_caption(<span class="hljs-string">&quot;Doodle Jump By Guanngxu&quot;</span>)
        <span class="hljs-variable language_">self</span>.clock = pygame.time.Clock()

    <span class="hljs-keyword">def</span> <span class="hljs-title function_">run</span>(<span class="hljs-params">self</span>):
        <span class="hljs-keyword">while</span> <span class="hljs-literal">True</span>:
            <span class="hljs-keyword">for</span> event <span class="hljs-keyword">in</span> pygame.event.get():
                <span class="hljs-keyword">if</span> event.<span class="hljs-built_in">type</span> == pygame.QUIT:
                    pygame.quit()
                    sys.exit()
            
            <span class="hljs-variable language_">self</span>.clock.tick(FPS)  <span class="hljs-comment"># 控制游戏循环以每秒FPS帧的速度运行</span>
            pygame.display.flip()  <span class="hljs-comment"># 更新屏幕显示</span>


<span class="hljs-keyword">if</span> __name__ == <span class="hljs-string">&quot;__main__&quot;</span>:
    GameSession().run()
</code></pre>
<p>运行代码后确定可以正常弹出 pygame 弹窗，下一步即可添加游戏窗口背景。由于背景是一张图片，因此我们在源文件同目录下新建<code>images</code>文件夹，用于存放程序所需要用到的图片文件。加载图片需要使用<code>pygame.image</code>模块，我们增加一个工具类<code>Utils</code>用于处理此类需求。</p>
<pre class="hljs"><code><span class="hljs-keyword">import</span> os

<span class="hljs-keyword">class</span> <span class="hljs-title class_">Utils</span>:
    <span class="hljs-comment"># --- 资源加载助手函数 ---</span>
<span class="hljs-meta">    @staticmethod</span>
    <span class="hljs-keyword">def</span> <span class="hljs-title function_">load_img</span>(<span class="hljs-params">name, scale=<span class="hljs-literal">None</span></span>):
        path = os.path.join(<span class="hljs-string">&quot;./images/&quot;</span>, name) <span class="hljs-comment"># 拼接图片路径</span>
        <span class="hljs-keyword">try</span>:
            img = pygame.image.load(path).convert_alpha() <span class="hljs-comment"># 加载图片并转换Alpha通道（透明度优化）</span>
            <span class="hljs-keyword">if</span> scale: img = pygame.transform.scale(img, scale) <span class="hljs-comment"># 如果指定了尺寸，则进行缩放</span>
            <span class="hljs-keyword">return</span> img
        <span class="hljs-keyword">except</span>:
            <span class="hljs-comment"># 如果图片丢失，生成一个占位用的灰色方块，确保程序不崩溃</span>
            surf = pygame.Surface(scale <span class="hljs-keyword">if</span> scale <span class="hljs-keyword">else</span> (<span class="hljs-number">30</span>, <span class="hljs-number">30</span>))
            surf.fill((<span class="hljs-number">200</span>, <span class="hljs-number">200</span>, <span class="hljs-number">200</span>))
            <span class="hljs-keyword">return</span> surf
</code></pre>
<p>有了<code>Utils</code>工具类后，就像导演搭建舞台一样，即可对<code>GameSession</code>进行修改，增加<code>bg</code>属性和<code>update</code>、<code>draw</code>方法，分别用以存储背景图片和更新游戏数据以及绘制游戏界面，直观效果即游戏界面可加载背景图片显示的更好看了。</p>
<figure data-type="image" tabindex="1"><img src="https://www.guanngxu.com/post-images/1774410068998.png" alt=""></figure>
<pre class="hljs"><code><span class="hljs-keyword">class</span> <span class="hljs-title class_">GameSession</span>:
    <span class="hljs-keyword">def</span> <span class="hljs-title function_">__init__</span>(<span class="hljs-params">self</span>):
        pygame.init()
        <span class="hljs-variable language_">self</span>.screen = pygame.display.set_mode((SCREEN_WIDTH, SCREEN_HEIGHT))
        pygame.display.set_caption(<span class="hljs-string">&quot;Doodle Jump By Guanngxu&quot;</span>)
        <span class="hljs-variable language_">self</span>.clock = pygame.time.Clock()
        <span class="hljs-variable language_">self</span>.bg = Utils.load_img(<span class="hljs-string">&quot;background.png&quot;</span>, (SCREEN_WIDTH, SCREEN_HEIGHT)) <span class="hljs-comment"># 加载背景图片</span>

    <span class="hljs-keyword">def</span> <span class="hljs-title function_">update</span>(<span class="hljs-params">self</span>): <span class="hljs-comment"># 更新游戏数据</span>
        <span class="hljs-keyword">pass</span>
    
    <span class="hljs-keyword">def</span> <span class="hljs-title function_">draw</span>(<span class="hljs-params">self</span>): <span class="hljs-comment"># 绘制游戏画面</span>
        <span class="hljs-variable language_">self</span>.screen.blit(<span class="hljs-variable language_">self</span>.bg, (<span class="hljs-number">0</span>, <span class="hljs-number">0</span>)) <span class="hljs-comment"># 绘制背景图片</span>

    <span class="hljs-keyword">def</span> <span class="hljs-title function_">run</span>(<span class="hljs-params">self</span>):
        <span class="hljs-keyword">while</span> <span class="hljs-literal">True</span>:
            <span class="hljs-keyword">for</span> event <span class="hljs-keyword">in</span> pygame.event.get():
                <span class="hljs-keyword">if</span> event.<span class="hljs-built_in">type</span> == pygame.QUIT:
                    pygame.quit()
                    sys.exit()
            
            <span class="hljs-variable language_">self</span>.update()
            <span class="hljs-variable language_">self</span>.draw()

            <span class="hljs-variable language_">self</span>.clock.tick(FPS)  <span class="hljs-comment"># 控制游戏循环以每秒FPS帧的速度运行</span>
            pygame.display.flip()  <span class="hljs-comment"># 更新屏幕显示</span>
</code></pre>
<p>游戏背景加载成功之后，继续完善<code>Player</code>和<code>Platform</code>类，先将所属的图片资源加载进来，与<code>GameSession</code>类同理，我们也需要增加<code>update</code>和<code>draw</code>方法用于更新位置、动作数据和绘制工作。</p>
<pre class="hljs"><code><span class="hljs-keyword">class</span> <span class="hljs-title class_">Player</span>:
    <span class="hljs-keyword">def</span> <span class="hljs-title function_">__init__</span>(<span class="hljs-params">self</span>):
        <span class="hljs-variable language_">self</span>.image = Utils.load_img(<span class="hljs-string">&quot;player.png&quot;</span>, (<span class="hljs-number">40</span>, <span class="hljs-number">40</span>)) <span class="hljs-comment"># 加载玩家图片并缩放到40x40像素</span>
        <span class="hljs-variable language_">self</span>.rect = <span class="hljs-variable language_">self</span>.image.get_rect() <span class="hljs-comment"># 获取玩家图片的矩形区域，用于碰撞检测和位置管理</span>

    <span class="hljs-keyword">def</span> <span class="hljs-title function_">update</span>(<span class="hljs-params">self</span>):
        <span class="hljs-keyword">pass</span>

    <span class="hljs-keyword">def</span> <span class="hljs-title function_">draw</span>(<span class="hljs-params">self, screen</span>):
        screen.blit(<span class="hljs-variable language_">self</span>.image, <span class="hljs-variable language_">self</span>.rect) <span class="hljs-comment"># 将玩家图片绘制在屏幕底部中央位置</span>

<span class="hljs-keyword">class</span> <span class="hljs-title class_">Platform</span>:
    <span class="hljs-keyword">def</span> <span class="hljs-title function_">__init__</span>(<span class="hljs-params">self</span>):
        <span class="hljs-variable language_">self</span>.image = Utils.load_img(<span class="hljs-string">&quot;platform.png&quot;</span>, (<span class="hljs-number">70</span>, <span class="hljs-number">20</span>)) <span class="hljs-comment"># 加载平台图片并缩放到70x20像素</span>
        <span class="hljs-variable language_">self</span>.rect = <span class="hljs-variable language_">self</span>.image.get_rect() <span class="hljs-comment"># 获取平台图片的矩形区域，用于碰撞检测和位置管理</span>

    <span class="hljs-keyword">def</span> <span class="hljs-title function_">update</span>(<span class="hljs-params">self</span>):
        <span class="hljs-keyword">pass</span>

    <span class="hljs-keyword">def</span> <span class="hljs-title function_">draw</span>(<span class="hljs-params">self, screen</span>):
        screen.blit(<span class="hljs-variable language_">self</span>.image, <span class="hljs-variable language_">self</span>.rect) <span class="hljs-comment"># 将平台图片绘制在屏幕上</span>
</code></pre>
<p>为了让玩家能够有地方停留住，我们将第一个平台固定在屏幕底部正中央，其它平台则随机生成铺满即可，更新后的<code>GameSession</code>类如下。</p>
<figure data-type="image" tabindex="2"><img src="https://www.guanngxu.com/post-images/1774410812240.png" alt=""></figure>
<pre class="hljs"><code><span class="hljs-keyword">import</span> random

<span class="hljs-keyword">class</span> <span class="hljs-title class_">GameSession</span>:
    <span class="hljs-keyword">def</span> <span class="hljs-title function_">__init__</span>(<span class="hljs-params">self</span>):
        pygame.init()
        <span class="hljs-variable language_">self</span>.screen = pygame.display.set_mode((SCREEN_WIDTH, SCREEN_HEIGHT))
        pygame.display.set_caption(<span class="hljs-string">&quot;Doodle Jump By Guanngxu&quot;</span>)
        <span class="hljs-variable language_">self</span>.clock = pygame.time.Clock()
        <span class="hljs-variable language_">self</span>.bg = Utils.load_img(<span class="hljs-string">&quot;background.png&quot;</span>, (SCREEN_WIDTH, SCREEN_HEIGHT)) <span class="hljs-comment"># 加载背景图片</span>
        <span class="hljs-variable language_">self</span>.player = Player() <span class="hljs-comment"># 创建玩家实例</span>
        <span class="hljs-variable language_">self</span>.platforms = []
        <span class="hljs-variable language_">self</span>._init_platforms() <span class="hljs-comment"># 初始化平台列表</span>

    <span class="hljs-keyword">def</span> <span class="hljs-title function_">_init_platforms</span>(<span class="hljs-params">self</span>):
        platform = Platform()
        platform.rect.x = SCREEN_WIDTH // <span class="hljs-number">2</span> - platform.rect.width // <span class="hljs-number">2</span>
        platform.rect.y = SCREEN_HEIGHT - <span class="hljs-number">50</span>
        <span class="hljs-variable language_">self</span>.platforms.append(platform)
        <span class="hljs-comment"># 初始化一些平台，确保玩家有地方跳</span>
        <span class="hljs-keyword">for</span> i <span class="hljs-keyword">in</span> <span class="hljs-built_in">range</span>(<span class="hljs-number">5</span>):
            platform = Platform()
            platform.rect.x = random.randint(<span class="hljs-number">0</span>, SCREEN_WIDTH - platform.rect.width)
            platform.rect.y = random.randint(<span class="hljs-number">0</span>, SCREEN_HEIGHT - platform.rect.height)
            <span class="hljs-variable language_">self</span>.platforms.append(platform)


    <span class="hljs-keyword">def</span> <span class="hljs-title function_">update</span>(<span class="hljs-params">self</span>): <span class="hljs-comment"># 更新游戏数据</span>
        <span class="hljs-keyword">pass</span>
    
    <span class="hljs-keyword">def</span> <span class="hljs-title function_">draw</span>(<span class="hljs-params">self</span>): <span class="hljs-comment"># 绘制游戏画面</span>
        <span class="hljs-variable language_">self</span>.screen.blit(<span class="hljs-variable language_">self</span>.bg, (<span class="hljs-number">0</span>, <span class="hljs-number">0</span>)) <span class="hljs-comment"># 绘制背景图片</span>
        <span class="hljs-keyword">for</span> platform <span class="hljs-keyword">in</span> <span class="hljs-variable language_">self</span>.platforms:
            platform.draw(<span class="hljs-variable language_">self</span>.screen) <span class="hljs-comment"># 绘制平台</span>
        <span class="hljs-variable language_">self</span>.player.draw(<span class="hljs-variable language_">self</span>.screen) <span class="hljs-comment"># 绘制玩家</span>
</code></pre>
<p>运行代码后的效果如下。</p>
<figure data-type="image" tabindex="3"><img src="https://www.guanngxu.com/post-images/1774410989205.png" alt=""></figure>
<p>可以发现平台生成的位置也太随意了，我们理应控制平台生成的高度间距以确保玩家可以跳上去，因此把<code>Platform</code>类的初始化函数做修改，将生成坐标改外外部传参以方便指定位置，待后续调整时方便修改。同时玩家还需要调整位置站在第一个平台上面。</p>
<figure data-type="image" tabindex="4"><img src="https://www.guanngxu.com/post-images/1774411428368.png" alt=""></figure>
<figure data-type="image" tabindex="5"><img src="https://www.guanngxu.com/post-images/1774452332350.png" alt=""></figure>
<pre class="hljs"><code>
<span class="hljs-keyword">class</span> <span class="hljs-title class_">Player</span>:
    <span class="hljs-keyword">def</span> <span class="hljs-title function_">__init__</span>(<span class="hljs-params">self</span>):
        <span class="hljs-variable language_">self</span>.image = Utils.load_img(<span class="hljs-string">&quot;player.png&quot;</span>, (<span class="hljs-number">40</span>, <span class="hljs-number">40</span>)) <span class="hljs-comment"># 加载玩家图片并缩放到40x40像素</span>
        <span class="hljs-comment"># 第一个平台离 y 轴 50，平台高度 20，所以减去 70</span>
        <span class="hljs-variable language_">self</span>.rect = <span class="hljs-variable language_">self</span>.image.get_rect(center=(SCREEN_WIDTH // <span class="hljs-number">2</span>, SCREEN_HEIGHT - <span class="hljs-number">70</span>)) <span class="hljs-comment"># 获取玩家图片的矩形区域，用于碰撞检测和位置管理</span>

<span class="hljs-keyword">class</span> <span class="hljs-title class_">Platform</span>:
    <span class="hljs-keyword">def</span> <span class="hljs-title function_">__init__</span>(<span class="hljs-params">self, x, y</span>):
        <span class="hljs-variable language_">self</span>.image = Utils.load_img(<span class="hljs-string">&quot;platform.png&quot;</span>, (<span class="hljs-number">70</span>, <span class="hljs-number">20</span>)) <span class="hljs-comment"># 加载平台图片并缩放到70x20像素</span>
        <span class="hljs-variable language_">self</span>.rect = <span class="hljs-variable language_">self</span>.image.get_rect(topleft=(x, y)) <span class="hljs-comment"># 获取平台图片的矩形区域，用于碰撞检测和位置管理</span>

<span class="hljs-keyword">class</span> <span class="hljs-title class_">GameSession</span>:
    <span class="hljs-keyword">def</span> <span class="hljs-title function_">_init_platforms</span>(<span class="hljs-params">self</span>):
        <span class="hljs-comment"># 在屏幕底部中央创建一个初始平台，确保玩家有地方跳</span>
        <span class="hljs-comment"># platform 的 width 为 70，所以 x 坐标需要减去 35 来居中</span>
        platform = Platform(SCREEN_WIDTH // <span class="hljs-number">2</span> - <span class="hljs-number">35</span>, SCREEN_HEIGHT - <span class="hljs-number">50</span>)
        <span class="hljs-variable language_">self</span>.platforms.append(platform)
        <span class="hljs-comment"># 初始化一些平台，确保玩家有地方跳</span>
        <span class="hljs-keyword">for</span> i <span class="hljs-keyword">in</span> <span class="hljs-built_in">range</span>(<span class="hljs-number">8</span>):
            platform = Platform(random.randint(<span class="hljs-number">0</span>, SCREEN_WIDTH - <span class="hljs-number">70</span>), SCREEN_HEIGHT - (i * <span class="hljs-number">80</span>) - <span class="hljs-number">150</span>)
            <span class="hljs-variable language_">self</span>.platforms.append(platform)
</code></pre>
<p>接下来需要加入动作效果了，还记得前文描述说整个过程都有重力作用在玩家身上，下面我们加入重力让玩家不断下坠。同时也需要引入碰撞检测以确保玩家在平台上时可以跳起来。</p>
<figure data-type="image" tabindex="6"><img src="https://www.guanngxu.com/post-images/1774412289814.png" alt=""></figure>
<figure data-type="image" tabindex="7"><img src="https://www.guanngxu.com/post-images/1774412294703.png" alt=""></figure>
<pre class="hljs"><code>GRAVITY = <span class="hljs-number">0.7</span>                           <span class="hljs-comment"># 模拟物理重力，每帧给玩家增加的向下速度</span>

<span class="hljs-keyword">class</span> <span class="hljs-title class_">Player</span>:
    <span class="hljs-keyword">def</span> <span class="hljs-title function_">__init__</span>(<span class="hljs-params">self</span>):
        <span class="hljs-variable language_">self</span>.image = Utils.load_img(<span class="hljs-string">&quot;player.png&quot;</span>, (<span class="hljs-number">40</span>, <span class="hljs-number">40</span>)) <span class="hljs-comment"># 加载玩家图片并缩放到40x40像素</span>
        <span class="hljs-comment"># 第一个平台离 y 轴 50，平台高度 20，所以减去 70</span>
        <span class="hljs-variable language_">self</span>.rect = <span class="hljs-variable language_">self</span>.image.get_rect(center=(SCREEN_WIDTH // <span class="hljs-number">2</span>, SCREEN_HEIGHT - <span class="hljs-number">70</span>)) <span class="hljs-comment"># 获取玩家图片的矩形区域，用于碰撞检测和位置管理</span>
        <span class="hljs-variable language_">self</span>.speed_y = <span class="hljs-number">0</span> <span class="hljs-comment"># 玩家在 y 轴上的速度，初始为0</span>

    <span class="hljs-keyword">def</span> <span class="hljs-title function_">update</span>(<span class="hljs-params">self</span>):
        <span class="hljs-variable language_">self</span>.speed_y += GRAVITY <span class="hljs-comment"># 每帧增加重力加速度</span>
        <span class="hljs-variable language_">self</span>.rect.y += <span class="hljs-variable language_">self</span>.speed_y <span class="hljs-comment"># 根据速度更新玩家的 y 坐标</span>

<span class="hljs-keyword">class</span> <span class="hljs-title class_">GameSession</span>:
    <span class="hljs-keyword">def</span> <span class="hljs-title function_">update</span>(<span class="hljs-params">self</span>): <span class="hljs-comment"># 更新游戏数据</span>
        <span class="hljs-keyword">for</span> platform <span class="hljs-keyword">in</span> <span class="hljs-variable language_">self</span>.platforms:
            platform.update() <span class="hljs-comment"># 更新平台状态</span>
        <span class="hljs-variable language_">self</span>.player.update() <span class="hljs-comment"># 更新玩家状态</span>

        <span class="hljs-keyword">for</span> platform <span class="hljs-keyword">in</span> <span class="hljs-variable language_">self</span>.platforms:
            <span class="hljs-comment"># 检测玩家是否与平台发生碰撞，并且玩家正在向下移动时检测，向上移动时不检测</span>
            <span class="hljs-keyword">if</span> <span class="hljs-variable language_">self</span>.player.rect.colliderect(platform.rect) <span class="hljs-keyword">and</span> <span class="hljs-variable language_">self</span>.player.speed_y &gt; <span class="hljs-number">0</span>:
                <span class="hljs-variable language_">self</span>.player.speed_y = -<span class="hljs-number">15</span> <span class="hljs-comment"># 碰撞后给予玩家一个向上的速度，模拟跳跃效果</span>
</code></pre>
<p>仔细观察后会发现玩家在跳跃的过程中，小脚有可能会嵌入到平台中间，为了解决这个不符合逻辑的问题，我们需要在检测到碰撞后调整玩家和平台的相对位置，让玩家的底部坐标与平台的顶部坐标相同。</p>
<p>回顾前文描述，我们的初始设计是玩家站在平台上时若按下空格键，此时就可以跳跃起来。因此我们需要监听键盘事件，同时玩家还可以左右移动的逻辑也一并加入。</p>
<p>考虑到跳跃动作需要检测玩家是否站在平台上，跳跃的触发逻辑放在<code>GameSession</code>类的<code>update</code>方法中应会更方便，玩家左右移动的逻辑不涉及其它对象，则可尤其自身的<code>update</code>处理即可。</p>
<figure data-type="image" tabindex="8"><img src="https://www.guanngxu.com/post-images/1774451318792.png" alt=""></figure>
<figure data-type="image" tabindex="9"><img src="https://www.guanngxu.com/post-images/1774451324756.png" alt=""></figure>
<pre class="hljs"><code><span class="hljs-keyword">class</span> <span class="hljs-title class_">Player</span>:
    <span class="hljs-keyword">def</span> <span class="hljs-title function_">update</span>(<span class="hljs-params">self</span>): <span class="hljs-comment"># 更新游戏数据</span>
        <span class="hljs-keyword">for</span> platform <span class="hljs-keyword">in</span> <span class="hljs-variable language_">self</span>.platforms:
            platform.update() <span class="hljs-comment"># 更新平台状态</span>
        <span class="hljs-variable language_">self</span>.player.update() <span class="hljs-comment"># 更新玩家状态</span>

        <span class="hljs-keyword">for</span> platform <span class="hljs-keyword">in</span> <span class="hljs-variable language_">self</span>.platforms:
            <span class="hljs-comment"># 检测玩家是否与平台发生碰撞，并且玩家正在向下移动时检测，向上移动时不检测</span>
            <span class="hljs-keyword">if</span> <span class="hljs-variable language_">self</span>.player.rect.colliderect(platform.rect) <span class="hljs-keyword">and</span> <span class="hljs-variable language_">self</span>.player.speed_y &gt; <span class="hljs-number">0</span>:
                <span class="hljs-variable language_">self</span>.player.rect.bottom = platform.rect.top <span class="hljs-comment"># 碰撞后将玩家的底部位置调整到平台的顶部，避免玩家穿过平台</span>
                <span class="hljs-variable language_">self</span>.player.speed_y = <span class="hljs-number">0</span> <span class="hljs-comment"># 碰撞后将玩家的垂直速度重置为0，模拟站在平台上的效果</span>
                keys = pygame.key.get_pressed()
                <span class="hljs-keyword">if</span> keys[pygame.K_SPACE]: <span class="hljs-comment"># 只有按空格才跳跃</span>
                    <span class="hljs-variable language_">self</span>.player.jump() <span class="hljs-comment"># 调用玩家的 jump 方法，执行跳跃逻辑</span>
</code></pre>
<p>继续运行发现玩家如果头部碰到平台，玩家就会被那一个平台给「吸」上去，这种现象我们是不允许发生的，因此需要对玩家与平台的碰撞检测逻辑做一些修整，确保只有玩家的小脚碰到平台才会站住。</p>
<figure data-type="image" tabindex="10"><img src="https://www.guanngxu.com/post-images/1774451878019.png" alt=""></figure>
<pre class="hljs"><code><span class="hljs-keyword">class</span> <span class="hljs-title class_">GameSession</span>:
    <span class="hljs-keyword">def</span> <span class="hljs-title function_">update</span>(<span class="hljs-params">self</span>): <span class="hljs-comment"># 更新游戏数据</span>
        <span class="hljs-keyword">for</span> platform <span class="hljs-keyword">in</span> <span class="hljs-variable language_">self</span>.platforms:
            platform.update() <span class="hljs-comment"># 更新平台状态</span>
        <span class="hljs-variable language_">self</span>.player.update() <span class="hljs-comment"># 更新玩家状态</span>

        <span class="hljs-keyword">for</span> platform <span class="hljs-keyword">in</span> <span class="hljs-variable language_">self</span>.platforms:
            <span class="hljs-comment"># 检测玩家是否与平台发生碰撞，并且玩家正在向下移动时检测，向上移动时不检测</span>
            <span class="hljs-keyword">if</span> <span class="hljs-variable language_">self</span>.player.rect.colliderect(platform.rect) <span class="hljs-keyword">and</span> <span class="hljs-variable language_">self</span>.player.speed_y &gt; <span class="hljs-number">0</span>:
                <span class="hljs-comment"># 15 是一个经验值，表示玩家底部与平台顶部的碰撞距离，如果小于这个值才算真正的站在平台上，避免侧面碰撞误判</span>
                <span class="hljs-keyword">if</span> <span class="hljs-variable language_">self</span>.player.rect.bottom - platform.rect.top &lt; <span class="hljs-number">15</span>: <span class="hljs-comment"># 碰撞时只检测玩家底部与平台顶部的碰撞，避免侧面碰撞误判</span>
                    <span class="hljs-variable language_">self</span>.player.rect.bottom = platform.rect.top <span class="hljs-comment"># 碰撞后将玩家的底部位置调整到平台的顶部，避免玩家穿过平台</span>
                    <span class="hljs-variable language_">self</span>.player.speed_y = <span class="hljs-number">0</span> <span class="hljs-comment"># 碰撞后将玩家的垂直速度重置为0，模拟站在平台上的效果</span>
                    keys = pygame.key.get_pressed()
                    <span class="hljs-keyword">if</span> keys[pygame.K_SPACE]: <span class="hljs-comment"># 只有按空格才跳跃</span>
                        <span class="hljs-variable language_">self</span>.player.jump() <span class="hljs-comment"># 调用玩家的 jump 方法，执行跳跃逻辑</span>
</code></pre>
<p>再运行代码会发现玩家不会被平台「吸」上去了，但是如果玩家左右移动超过边界时就看不见了，也不知道玩家到底移动到哪里去了，此处我们加入一个「穿墙」的效果，即如果玩家从右边移出了边界就让玩家从左边出现，反之亦然。</p>
<p>为了让游戏更加生动，当玩家跳跃时播放一个动效声音。声音文件与图片文件类似，将其存放在代码同目录下的<code>sounds</code>文件夹下，自然在工具类中需要增加加载音频文件的方法。</p>
<figure data-type="image" tabindex="11"><img src="https://www.guanngxu.com/post-images/1774453660293.png" alt=""></figure>
<figure data-type="image" tabindex="12"><img src="https://www.guanngxu.com/post-images/1774453667205.png" alt=""></figure>
<pre class="hljs"><code><span class="hljs-keyword">class</span> <span class="hljs-title class_">Utils</span>:
<span class="hljs-meta">    @staticmethod</span>
    <span class="hljs-keyword">def</span> <span class="hljs-title function_">load_sound</span>(<span class="hljs-params">name</span>):
        base_path = <span class="hljs-string">&quot;./sounds/&quot;</span>
        <span class="hljs-keyword">for</span> ext <span class="hljs-keyword">in</span> [<span class="hljs-string">&#x27;.wav&#x27;</span>, <span class="hljs-string">&#x27;.mp3&#x27;</span>, <span class="hljs-string">&#x27;.ogg&#x27;</span>]: <span class="hljs-comment"># 遍历常见的音频格式</span>
            full_path = os.path.join(base_path, name + ext)
            <span class="hljs-keyword">if</span> os.path.exists(full_path):
                <span class="hljs-keyword">try</span>: <span class="hljs-keyword">return</span> pygame.mixer.Sound(full_path)
                <span class="hljs-keyword">except</span>: <span class="hljs-keyword">continue</span>
        <span class="hljs-keyword">return</span> <span class="hljs-literal">None</span> <span class="hljs-comment"># 如果没有找到任何格式的音效文件，返回 None</span>

<span class="hljs-keyword">class</span> <span class="hljs-title class_">Player</span>:
    <span class="hljs-keyword">def</span> <span class="hljs-title function_">__init__</span>(<span class="hljs-params">self</span>):
        <span class="hljs-variable language_">self</span>.image = Utils.load_img(<span class="hljs-string">&quot;player.png&quot;</span>, (<span class="hljs-number">40</span>, <span class="hljs-number">40</span>)) <span class="hljs-comment"># 加载玩家图片并缩放到40x40像素</span>
        <span class="hljs-comment"># 第一个平台离 y 轴 50，平台高度 20，所以减去 70</span>
        <span class="hljs-variable language_">self</span>.rect = <span class="hljs-variable language_">self</span>.image.get_rect(center=(SCREEN_WIDTH // <span class="hljs-number">2</span>, SCREEN_HEIGHT - <span class="hljs-number">70</span>)) <span class="hljs-comment"># 获取玩家图片的矩形区域，用于碰撞检测和位置管理</span>
        <span class="hljs-variable language_">self</span>.speed_y = <span class="hljs-number">0</span> <span class="hljs-comment"># 玩家在 y 轴上的速度，初始为0</span>
        <span class="hljs-variable language_">self</span>.speed_x = <span class="hljs-number">8</span> <span class="hljs-comment"># 玩家在 x 轴上的速度，固定为8</span>
        <span class="hljs-variable language_">self</span>.jump_sound = Utils.load_sound(<span class="hljs-string">&quot;jump&quot;</span>) <span class="hljs-comment"># 加载跳跃音效</span>

    <span class="hljs-keyword">def</span> <span class="hljs-title function_">jump</span>(<span class="hljs-params">self</span>):
        <span class="hljs-variable language_">self</span>.speed_y = -<span class="hljs-number">15</span> <span class="hljs-comment"># 碰撞后给予玩家一个向上的速度，模拟跳跃效果</span>
        <span class="hljs-keyword">if</span> <span class="hljs-variable language_">self</span>.jump_sound:
            <span class="hljs-variable language_">self</span>.jump_sound.play() <span class="hljs-comment"># 播放跳跃音效</span>

    <span class="hljs-keyword">def</span> <span class="hljs-title function_">update</span>(<span class="hljs-params">self</span>):
        <span class="hljs-variable language_">self</span>.speed_y += GRAVITY <span class="hljs-comment"># 每帧增加重力加速度</span>
        <span class="hljs-variable language_">self</span>.rect.y += <span class="hljs-variable language_">self</span>.speed_y <span class="hljs-comment"># 根据速度更新玩家的 y 坐标</span>
        
        <span class="hljs-comment"># 处理键盘左右键输入</span>
        keys = pygame.key.get_pressed()
        <span class="hljs-keyword">if</span> keys[pygame.K_LEFT]:
            <span class="hljs-variable language_">self</span>.rect.x -= <span class="hljs-variable language_">self</span>.speed_x <span class="hljs-comment"># 向左移动</span>
        <span class="hljs-keyword">if</span> keys[pygame.K_RIGHT]:
            <span class="hljs-variable language_">self</span>.rect.x += <span class="hljs-variable language_">self</span>.speed_x <span class="hljs-comment"># 向右移动</span>

        <span class="hljs-keyword">if</span> <span class="hljs-variable language_">self</span>.rect.right &lt; <span class="hljs-number">0</span>: <span class="hljs-comment"># 如果玩家完全移出左边界</span>
            <span class="hljs-variable language_">self</span>.rect.left = SCREEN_WIDTH <span class="hljs-comment"># 从右边重新出现</span>
        <span class="hljs-keyword">elif</span> <span class="hljs-variable language_">self</span>.rect.left &gt; SCREEN_WIDTH: <span class="hljs-comment"># 如果玩家完全移出右边界</span>
            <span class="hljs-variable language_">self</span>.rect.right = <span class="hljs-number">0</span> <span class="hljs-comment"># 从左边重新出现</span>

    <span class="hljs-keyword">def</span> <span class="hljs-title function_">draw</span>(<span class="hljs-params">self, screen</span>):
        screen.blit(<span class="hljs-variable language_">self</span>.image, <span class="hljs-variable language_">self</span>.rect) <span class="hljs-comment"># 将玩家图片绘制在屏幕底部中央位置</span>
</code></pre>
<p>有了声音之后的程序是不是交互感更加强烈了，有木有？但是玩家跳到顶部后就没办法再往上跳了，所以我们要增加屏幕滚动的逻辑，确保玩家可以一直向上跳跃。我们可以设定一个高度阈值，如果玩家跳跃超过了这个高度阈值，就让整个屏幕向下滚动，需要注意的是屏幕向下滚动后需要在顶部区域生成新的平台才能保证游戏可继续下去。</p>
<p>所谓的屏幕滚动其实就是把除了背景的所有元素全部往下移动，人眼看起来就是屏幕在整体向下滚动，因此我们可以看玩家超过高度阈值多少，就让所有元素向下移动多少距离。</p>
<p>别忘了此时可以引入游戏计分的逻辑了，此处我们根据滚动距离增加分数。对于分数的显示借助<code>pygame.font</code>模块渲染文字即可。</p>
<figure data-type="image" tabindex="13"><img src="https://www.guanngxu.com/post-images/1774454320620.png" alt=""></figure>
<figure data-type="image" tabindex="14"><img src="https://www.guanngxu.com/post-images/1774454327686.png" alt=""></figure>
<pre class="hljs"><code><span class="hljs-keyword">class</span> <span class="hljs-title class_">GameSession</span>:
    <span class="hljs-keyword">def</span> <span class="hljs-title function_">__init__</span>(<span class="hljs-params">self</span>):
        pygame.init()
        <span class="hljs-variable language_">self</span>.screen = pygame.display.set_mode((SCREEN_WIDTH, SCREEN_HEIGHT))
        pygame.display.set_caption(<span class="hljs-string">&quot;Doodle Jump By Guanngxu&quot;</span>)
        <span class="hljs-variable language_">self</span>.clock = pygame.time.Clock()
        <span class="hljs-variable language_">self</span>.bg = Utils.load_img(<span class="hljs-string">&quot;background.png&quot;</span>, (SCREEN_WIDTH, SCREEN_HEIGHT)) <span class="hljs-comment"># 加载背景图片</span>
        <span class="hljs-variable language_">self</span>.player = Player() <span class="hljs-comment"># 创建玩家实例</span>
        <span class="hljs-variable language_">self</span>.platforms = [] <span class="hljs-comment"># 初始化平台列表</span>
        <span class="hljs-variable language_">self</span>.score = <span class="hljs-number">0</span> <span class="hljs-comment"># 初始化分数</span>
        <span class="hljs-variable language_">self</span>._init_platforms() <span class="hljs-comment"># 初始化平台列表</span>

    <span class="hljs-keyword">def</span> <span class="hljs-title function_">update_scroll</span>(<span class="hljs-params">self</span>):
        scroll_threshold = SCREEN_HEIGHT // <span class="hljs-number">2</span> <span class="hljs-comment"># 定义一个滚动阈值，当玩家超过这个高度时，平台开始向下滚动</span>
        scroll_amount = <span class="hljs-number">0</span> <span class="hljs-comment"># 初始化滚动量</span>
        <span class="hljs-keyword">if</span> <span class="hljs-variable language_">self</span>.player.rect.top &lt; scroll_threshold:
            scroll_amount = scroll_threshold - <span class="hljs-variable language_">self</span>.player.rect.top <span class="hljs-comment"># 计算需要滚动的距离</span>
            <span class="hljs-variable language_">self</span>.player.rect.top = scroll_threshold <span class="hljs-comment"># 将玩家位置固定在滚动阈值上</span>
            <span class="hljs-keyword">for</span> platform <span class="hljs-keyword">in</span> <span class="hljs-variable language_">self</span>.platforms:
                platform.rect.y += scroll_amount <span class="hljs-comment"># 平台向下滚动</span>

        <span class="hljs-comment"># 平台向下滚动后，移除那些已经完全移出屏幕底部的平台，并在顶部生成新的平台</span>
        <span class="hljs-variable language_">self</span>.platforms = [p <span class="hljs-keyword">for</span> p <span class="hljs-keyword">in</span> <span class="hljs-variable language_">self</span>.platforms <span class="hljs-keyword">if</span> p.rect.top &lt; SCREEN_HEIGHT]
        <span class="hljs-variable language_">self</span>.score += scroll_amount // <span class="hljs-number">10</span> <span class="hljs-comment"># 根据滚动距离增加分数，10 是一个经验值，表示每滚动10像素得1分</span>
        <span class="hljs-keyword">while</span> <span class="hljs-built_in">len</span>(<span class="hljs-variable language_">self</span>.platforms) &lt; <span class="hljs-number">8</span>: <span class="hljs-comment"># 保持屏幕上至少有8个平台</span>
            new_platform = Platform(random.randint(<span class="hljs-number">0</span>, SCREEN_WIDTH - <span class="hljs-number">70</span>), random.randint(-<span class="hljs-number">100</span>, -<span class="hljs-number">40</span>))
            <span class="hljs-variable language_">self</span>.platforms.append(new_platform)

    <span class="hljs-keyword">def</span> <span class="hljs-title function_">update</span>(<span class="hljs-params">self</span>): <span class="hljs-comment"># 更新游戏数据</span>
        <span class="hljs-variable language_">self</span>.update_scroll() <span class="hljs-comment"># 更新滚动逻辑</span>
        <span class="hljs-comment"># 省略部分代码 .....</span>

    <span class="hljs-keyword">def</span> <span class="hljs-title function_">draw</span>(<span class="hljs-params">self</span>): <span class="hljs-comment"># 绘制游戏画面</span>
        <span class="hljs-variable language_">self</span>.screen.blit(<span class="hljs-variable language_">self</span>.bg, (<span class="hljs-number">0</span>, <span class="hljs-number">0</span>)) <span class="hljs-comment"># 绘制背景图片</span>
        <span class="hljs-keyword">for</span> platform <span class="hljs-keyword">in</span> <span class="hljs-variable language_">self</span>.platforms:
            platform.draw(<span class="hljs-variable language_">self</span>.screen) <span class="hljs-comment"># 绘制平台</span>
        <span class="hljs-variable language_">self</span>.player.draw(<span class="hljs-variable language_">self</span>.screen) <span class="hljs-comment"># 绘制玩家</span>
        scrore_text = pygame.font.SysFont(<span class="hljs-string">&quot;Arial&quot;</span>, <span class="hljs-number">24</span>).render(<span class="hljs-string">f&quot;Score: <span class="hljs-subst">{self.score}</span>&quot;</span>, <span class="hljs-literal">True</span>, (<span class="hljs-number">0</span>, <span class="hljs-number">0</span>, <span class="hljs-number">0</span>))
        <span class="hljs-variable language_">self</span>.screen.blit(scrore_text, (<span class="hljs-number">10</span>, <span class="hljs-number">10</span>)) <span class="hljs-comment"># 在屏幕左</span>
</code></pre>
<p>确保玩家的相关逻辑都完善后，我们开始引入道具助力玩家获得更多的分数。道具是对弹簧、竹蜻蜓、火箭喷气背包等的统称，即我们可以实现一个道具父类<code>Item</code>，所有道具共性的部分由<code>Item</code>类来实现，具体道具个性化的部分则自行实现。</p>
<p>道具在未生效前应该和平台绑定，因为道具始终停留在平台之上，这样也可以在平台更新时顺便就更新了道具。考虑到生成道具还需要一定的概率，所以我们在工具类中再添加一个计算概率的方法。我们先加入弹簧类看看效果。</p>
<figure data-type="image" tabindex="15"><img src="https://www.guanngxu.com/post-images/1774483380926.png" alt=""></figure>
<figure data-type="image" tabindex="16"><img src="https://www.guanngxu.com/post-images/1774483386558.png" alt=""></figure>
<figure data-type="image" tabindex="17"><img src="https://www.guanngxu.com/post-images/1774483392920.png" alt=""></figure>
<pre class="hljs"><code><span class="hljs-keyword">class</span> <span class="hljs-title class_">Utils</span>:
<span class="hljs-meta">    @staticmethod</span>
    <span class="hljs-keyword">def</span> <span class="hljs-title function_">hit_probability</span>(<span class="hljs-params">prob</span>):
        <span class="hljs-keyword">return</span> random.random() &lt; prob <span class="hljs-comment"># 返回一个布尔值，表示是否以给定概率命中</span>

<span class="hljs-keyword">class</span> <span class="hljs-title class_">Platform</span>:
    <span class="hljs-keyword">def</span> <span class="hljs-title function_">__init__</span>(<span class="hljs-params">self, x, y</span>):
        <span class="hljs-variable language_">self</span>.image = Utils.load_img(<span class="hljs-string">&quot;platform.png&quot;</span>, (<span class="hljs-number">70</span>, <span class="hljs-number">20</span>)) <span class="hljs-comment"># 加载平台图片并缩放到70x20像素</span>
        <span class="hljs-variable language_">self</span>.rect = <span class="hljs-variable language_">self</span>.image.get_rect(topleft=(x, y)) <span class="hljs-comment"># 获取平台图片的矩形区域，用于碰撞检测和位置管理</span>
        <span class="hljs-variable language_">self</span>.item = <span class="hljs-literal">None</span> <span class="hljs-comment"># 平台上可能有一个道具，初始为 None</span>

    <span class="hljs-keyword">def</span> <span class="hljs-title function_">update</span>(<span class="hljs-params">self</span>):
        <span class="hljs-keyword">if</span> <span class="hljs-variable language_">self</span>.item:
            <span class="hljs-variable language_">self</span>.item.update() <span class="hljs-comment"># 如果平台上有道具，更新道具状态</span>

    <span class="hljs-keyword">def</span> <span class="hljs-title function_">draw</span>(<span class="hljs-params">self, screen</span>):
        screen.blit(<span class="hljs-variable language_">self</span>.image, <span class="hljs-variable language_">self</span>.rect) <span class="hljs-comment"># 将平台图片绘制在屏幕上</span>
        <span class="hljs-keyword">if</span> <span class="hljs-variable language_">self</span>.item:
            <span class="hljs-variable language_">self</span>.item.draw(screen) <span class="hljs-comment"># 如果平台上有道具，绘制道具</span>

<span class="hljs-keyword">class</span> <span class="hljs-title class_">Item</span>:
    probability = <span class="hljs-number">0.3</span> <span class="hljs-comment"># 物品生成的概率，默认30%</span>
    
    <span class="hljs-keyword">def</span> <span class="hljs-title function_">__init__</span>(<span class="hljs-params">self, platform</span>):
        <span class="hljs-variable language_">self</span>.platform = platform <span class="hljs-comment"># 物品所在的平台</span>
        <span class="hljs-variable language_">self</span>.frames = [] <span class="hljs-comment"># 存储动画帧的列表</span>
        <span class="hljs-variable language_">self</span>.current_frame = <span class="hljs-number">0</span> <span class="hljs-comment"># 当前动画帧的索引</span>
        <span class="hljs-variable language_">self</span>.rect = <span class="hljs-literal">None</span> <span class="hljs-comment"># 物品的矩形区域，用于碰撞检测和位置管理</span>
        <span class="hljs-variable language_">self</span>.has_used = <span class="hljs-literal">False</span> <span class="hljs-comment"># 物品是否已经被玩家使用过，避免重复使用</span>

    <span class="hljs-keyword">def</span> <span class="hljs-title function_">update</span>(<span class="hljs-params">self</span>):
        <span class="hljs-variable language_">self</span>.rect.midbottom = <span class="hljs-variable language_">self</span>.platform.rect.midtop <span class="hljs-comment"># 物品始终跟随平台移动，保持在平台顶部</span>

    <span class="hljs-keyword">def</span> <span class="hljs-title function_">draw</span>(<span class="hljs-params">self, screen</span>):
        <span class="hljs-keyword">if</span> <span class="hljs-variable language_">self</span>.frames:
            screen.blit(<span class="hljs-variable language_">self</span>.frames[<span class="hljs-variable language_">self</span>.current_frame], <span class="hljs-variable language_">self</span>.rect) <span class="hljs-comment"># 绘制当前动画帧</span>

<span class="hljs-keyword">class</span> <span class="hljs-title class_">Spring</span>(<span class="hljs-title class_ inherited__">Item</span>):
    probability = <span class="hljs-number">0.5</span> <span class="hljs-comment"># 弹簧生成的概率，50%</span>

    <span class="hljs-keyword">def</span> <span class="hljs-title function_">__init__</span>(<span class="hljs-params">self, platform</span>):
        <span class="hljs-built_in">super</span>().__init__(platform)
        <span class="hljs-variable language_">self</span>.frames = [Utils.load_img(<span class="hljs-string">f&quot;spring_<span class="hljs-subst">{i}</span>.png&quot;</span>, (<span class="hljs-number">30</span>, <span class="hljs-number">30</span>)) <span class="hljs-keyword">for</span> i <span class="hljs-keyword">in</span> <span class="hljs-built_in">range</span>(<span class="hljs-number">2</span>)] <span class="hljs-comment"># 加载弹簧的两帧动画</span>
        <span class="hljs-variable language_">self</span>.rect = <span class="hljs-variable language_">self</span>.frames[<span class="hljs-number">0</span>].get_rect() <span class="hljs-comment"># 获取弹簧图片的矩形区域，用于碰撞检测和位置管理</span>
        <span class="hljs-variable language_">self</span>.sound = Utils.load_sound(<span class="hljs-string">&quot;spring&quot;</span>) <span class="hljs-comment"># 加载弹簧音效</span>

<span class="hljs-keyword">class</span> <span class="hljs-title class_">GameSession</span>:
    <span class="hljs-keyword">def</span> <span class="hljs-title function_">update_scroll</span>(<span class="hljs-params">self</span>):
        <span class="hljs-comment"># 省略部分代码 ......</span>
        <span class="hljs-keyword">while</span> <span class="hljs-built_in">len</span>(<span class="hljs-variable language_">self</span>.platforms) &lt; <span class="hljs-number">8</span>: <span class="hljs-comment"># 保持屏幕上至少有8个平台</span>
            new_platform = Platform(random.randint(<span class="hljs-number">0</span>, SCREEN_WIDTH - <span class="hljs-number">70</span>), random.randint(-<span class="hljs-number">100</span>, -<span class="hljs-number">40</span>))
            <span class="hljs-keyword">if</span> Utils.hit_probability(Spring.probability): <span class="hljs-comment"># 根据弹簧的生成概率决定是否在新平台上生成弹簧</span>
                spring = Spring(new_platform)
                new_platform.item = spring <span class="hljs-comment"># 将弹簧作为平台的一个属性，方便后续碰撞检测和更新</span>
            <span class="hljs-variable language_">self</span>.platforms.append(new_platform)
</code></pre>
<p>现在程序运行过程中会出现弹簧道具了，但是玩家碰到弹簧道具并没有相应的效果生效，下面就来完善弹簧触发后的效果逻辑。</p>
<p>若玩家触碰到弹簧，则首先弹簧会「弹开」，即涉及到弹簧的动画播放。其实对于弹簧弹开的动画，我们只需要快速切换不同的弹簧图片即可，这样看起来就是弹簧弹开一样，这个过程由<code>animate</code>方法实现。</p>
<p>弹簧弹开后，玩家会被弹簧作用一个更强的向上的速度，以此来模拟更高的跳跃效果，所有道具对玩家的作用我们都通过<code>apply_effect</code>方法实现。</p>
<figure data-type="image" tabindex="18"><img src="https://www.guanngxu.com/post-images/1774484650014.png" alt=""></figure>
<figure data-type="image" tabindex="19"><img src="https://www.guanngxu.com/post-images/1774484655630.png" alt=""></figure>
<figure data-type="image" tabindex="20"><img src="https://www.guanngxu.com/post-images/1774484661471.png" alt=""></figure>
<pre class="hljs"><code><span class="hljs-keyword">class</span> <span class="hljs-title class_">Platform</span>:
    <span class="hljs-keyword">def</span> <span class="hljs-title function_">update</span>(<span class="hljs-params">self</span>):
        <span class="hljs-keyword">if</span> <span class="hljs-variable language_">self</span>.item:
            <span class="hljs-variable language_">self</span>.item.update() <span class="hljs-comment"># 如果平台上有道具，更新道具状态</span>
            <span class="hljs-variable language_">self</span>.item.animate() <span class="hljs-comment"># 如果平台上有道具，执行道具动画</span>

<span class="hljs-keyword">class</span> <span class="hljs-title class_">Item</span>:
    <span class="hljs-keyword">def</span> <span class="hljs-title function_">apply_effect</span>(<span class="hljs-params">self, player</span>):
        <span class="hljs-keyword">pass</span>

<span class="hljs-keyword">class</span> <span class="hljs-title class_">Spring</span>(<span class="hljs-title class_ inherited__">Item</span>):
    <span class="hljs-keyword">def</span> <span class="hljs-title function_">apply_effect</span>(<span class="hljs-params">self, player</span>):
        <span class="hljs-keyword">if</span> <span class="hljs-variable language_">self</span>.has_used: <span class="hljs-keyword">return</span> <span class="hljs-comment"># 如果已经被使用过，直接返回，避免重复使用</span>
        <span class="hljs-variable language_">self</span>.has_used = <span class="hljs-literal">True</span> <span class="hljs-comment"># 标记为已使用</span>
        player.speed_y = -<span class="hljs-number">20</span> <span class="hljs-comment"># 弹簧给予玩家一个更强的向上的速度，模拟更高的跳跃效果</span>
        <span class="hljs-keyword">if</span> <span class="hljs-variable language_">self</span>.sound:
            <span class="hljs-variable language_">self</span>.sound.play() <span class="hljs-comment"># 播放弹簧音效</span>
    
    <span class="hljs-keyword">def</span> <span class="hljs-title function_">animate</span>(<span class="hljs-params">self</span>):
        <span class="hljs-keyword">if</span> <span class="hljs-variable language_">self</span>.has_used:
            <span class="hljs-variable language_">self</span>.current_frame = (<span class="hljs-variable language_">self</span>.current_frame + <span class="hljs-number">1</span>) % <span class="hljs-built_in">len</span>(<span class="hljs-variable language_">self</span>.frames) <span class="hljs-comment"># 切换到下一帧动画</span>
        <span class="hljs-keyword">else</span>:
            <span class="hljs-variable language_">self</span>.current_frame = <span class="hljs-number">0</span> <span class="hljs-comment"># 如果没有被使用过，保持在第一帧动画</span>

<span class="hljs-keyword">class</span> <span class="hljs-title class_">GameSession</span>:
    <span class="hljs-keyword">def</span> <span class="hljs-title function_">update_scroll</span>(<span class="hljs-params">self</span>):
        scroll_threshold = SCREEN_HEIGHT // <span class="hljs-number">2</span> <span class="hljs-comment"># 定义一个滚动阈值，当玩家超过这个高度时，平台开始向下滚动</span>
        scroll_amount = <span class="hljs-number">0</span> <span class="hljs-comment"># 初始化滚动量</span>
        <span class="hljs-keyword">if</span> <span class="hljs-variable language_">self</span>.player.rect.top &lt; scroll_threshold:
            scroll_amount = scroll_threshold - <span class="hljs-variable language_">self</span>.player.rect.top <span class="hljs-comment"># 计算需要滚动的距离</span>
            <span class="hljs-variable language_">self</span>.player.rect.top = scroll_threshold <span class="hljs-comment"># 将玩家位置固定在滚动阈值上</span>
            <span class="hljs-keyword">for</span> platform <span class="hljs-keyword">in</span> <span class="hljs-variable language_">self</span>.platforms:
                platform.rect.y += scroll_amount <span class="hljs-comment"># 平台向下滚动</span>

        <span class="hljs-comment"># 平台向下滚动后，移除那些已经完全移出屏幕底部的平台，并在顶部生成新的平台</span>
        <span class="hljs-variable language_">self</span>.platforms = [p <span class="hljs-keyword">for</span> p <span class="hljs-keyword">in</span> <span class="hljs-variable language_">self</span>.platforms <span class="hljs-keyword">if</span> p.rect.top &lt; SCREEN_HEIGHT]
        <span class="hljs-variable language_">self</span>.score += scroll_amount // <span class="hljs-number">10</span> <span class="hljs-comment"># 根据滚动距离增加分数，10 是一个经验值，表示每滚动10像素得1分</span>
        <span class="hljs-keyword">while</span> <span class="hljs-built_in">len</span>(<span class="hljs-variable language_">self</span>.platforms) &lt; <span class="hljs-number">8</span>: <span class="hljs-comment"># 保持屏幕上至少有8个平台</span>
            new_platform = Platform(random.randint(<span class="hljs-number">0</span>, SCREEN_WIDTH - <span class="hljs-number">70</span>), random.randint(-<span class="hljs-number">100</span>, -<span class="hljs-number">40</span>))
            <span class="hljs-keyword">if</span> Utils.hit_probability(Spring.probability): <span class="hljs-comment"># 根据弹簧的生成概率决定是否在新平台上生成弹簧</span>
                spring = Spring(new_platform)
                new_platform.item = spring <span class="hljs-comment"># 将弹簧作为平台的一个属性，方便后续碰撞检测和更新</span>
            <span class="hljs-variable language_">self</span>.platforms.append(new_platform)

    <span class="hljs-comment"># 检测玩家与道具的碰撞，并处理道具效果</span>
    <span class="hljs-keyword">def</span> <span class="hljs-title function_">item_colliderect</span>(<span class="hljs-params">self, item</span>):
        <span class="hljs-comment"># 检测玩家是否与道具发生碰撞，并且道具没有被使用过</span>
        <span class="hljs-keyword">if</span> <span class="hljs-variable language_">self</span>.player.rect.colliderect(item.rect) <span class="hljs-keyword">and</span> <span class="hljs-keyword">not</span> item.has_used:
            item.apply_effect(<span class="hljs-variable language_">self</span>.player) <span class="hljs-comment"># 调用 apply_effect 方法</span>

    <span class="hljs-comment"># 检测玩家与平台的碰撞，并处理跳跃逻辑</span>
    <span class="hljs-keyword">def</span> <span class="hljs-title function_">platform_colliderect</span>(<span class="hljs-params">self, platform</span>):
        <span class="hljs-comment"># 检测玩家是否与平台发生碰撞，并且玩家正在向下移动时检测，向上移动时不检测</span>
            <span class="hljs-keyword">if</span> <span class="hljs-variable language_">self</span>.player.rect.colliderect(platform.rect) <span class="hljs-keyword">and</span> <span class="hljs-variable language_">self</span>.player.speed_y &gt; <span class="hljs-number">0</span>:
                <span class="hljs-comment"># 15 是一个经验值，表示玩家底部与平台顶部的碰撞距离，如果小于这个值才算真正的站在平台上，避免侧面碰撞误判</span>
                <span class="hljs-keyword">if</span> <span class="hljs-variable language_">self</span>.player.rect.bottom - platform.rect.top &lt; <span class="hljs-number">15</span>: <span class="hljs-comment"># 碰撞时只检测玩家底部与平台顶部的碰撞，避免侧面碰撞误判</span>
                    <span class="hljs-variable language_">self</span>.player.rect.bottom = platform.rect.top <span class="hljs-comment"># 碰撞后将玩家的底部位置调整到平台的顶部，避免玩家穿过平台</span>
                    <span class="hljs-variable language_">self</span>.player.speed_y = <span class="hljs-number">0</span> <span class="hljs-comment"># 碰撞后将玩家的垂直速度重置为0，模拟站在平台上的效果</span>
                    keys = pygame.key.get_pressed()
                    <span class="hljs-keyword">if</span> keys[pygame.K_SPACE]: <span class="hljs-comment"># 只有按空格才跳跃</span>
                        <span class="hljs-variable language_">self</span>.player.jump() <span class="hljs-comment"># 调用玩家的 jump 方法，执行跳跃逻辑</span>
    
    <span class="hljs-keyword">def</span> <span class="hljs-title function_">update</span>(<span class="hljs-params">self</span>): <span class="hljs-comment"># 更新游戏数据</span>
        <span class="hljs-variable language_">self</span>.update_scroll() <span class="hljs-comment"># 更新滚动逻辑</span>

        <span class="hljs-keyword">for</span> platform <span class="hljs-keyword">in</span> <span class="hljs-variable language_">self</span>.platforms:
            platform.update() <span class="hljs-comment"># 更新平台状态</span>
        <span class="hljs-variable language_">self</span>.player.update() <span class="hljs-comment"># 更新玩家状态</span>

        <span class="hljs-comment"># 检测玩家与平台的碰撞，并处理跳跃逻辑，同时检测玩家与道具的碰撞，并处理道具效果</span>
        <span class="hljs-keyword">for</span> platform <span class="hljs-keyword">in</span> <span class="hljs-variable language_">self</span>.platforms:
            <span class="hljs-variable language_">self</span>.platform_colliderect(platform) <span class="hljs-comment"># 检测玩家与平台的碰撞，并处理跳跃逻辑</span>
            <span class="hljs-keyword">if</span> platform.item: <span class="hljs-comment"># 如果平台上有道具，检测玩家与道具的碰撞，并处理道具效果</span>
                <span class="hljs-variable language_">self</span>.item_colliderect(platform.item)
</code></pre>
<p>运行之后发现此前实现的<code>animate</code>方法不适用于弹簧道具，玩家触碰到弹簧道具后应是弹簧弹开一次即可，现在的效果是弹簧会一直不停的切换状态，且切换的速度太快了，需要重新实现<code>animate</code>方法，并调整一下动画的播放速度。</p>
<p>除了弹簧弹开动画不适用外，还存在的问题是玩家在跳跃过程中，如果脑袋先触碰到弹簧也会直接触发效果，这和现实世界逻辑是不对应的，弹簧应该是跌落过程踩到才可生效，因此需要对玩家与道具的碰撞检测进行条件限制，限制在向下移动的过程中。</p>
<figure data-type="image" tabindex="21"><img src="https://www.guanngxu.com/post-images/1774857629948.png" alt=""></figure>
<figure data-type="image" tabindex="22"><img src="https://www.guanngxu.com/post-images/1774857635246.png" alt=""></figure>
<figure data-type="image" tabindex="23"><img src="https://www.guanngxu.com/post-images/1774857639603.png" alt=""></figure>
<pre class="hljs"><code><span class="hljs-keyword">class</span> <span class="hljs-title class_">Item</span>:
    probability = <span class="hljs-number">0.3</span> <span class="hljs-comment"># 物品生成的概率，默认30%</span>
    animate_speed = <span class="hljs-number">0.2</span> <span class="hljs-comment"># 物品动画的速度，经验值，表示每帧切换动画的概率</span>
    
    <span class="hljs-keyword">def</span> <span class="hljs-title function_">__init__</span>(<span class="hljs-params">self, platform</span>):
        <span class="hljs-variable language_">self</span>.platform = platform <span class="hljs-comment"># 物品所在的平台</span>
        <span class="hljs-variable language_">self</span>.frames = [] <span class="hljs-comment"># 存储动画帧的列表</span>
        <span class="hljs-variable language_">self</span>.current_frame_index = <span class="hljs-number">0</span> <span class="hljs-comment"># 当前动画帧的索引</span>
        <span class="hljs-variable language_">self</span>.animate_timer = <span class="hljs-number">0</span> <span class="hljs-comment"># 动画计时器，用于控制动画切换速度</span>
        <span class="hljs-variable language_">self</span>.rect = <span class="hljs-literal">None</span> <span class="hljs-comment"># 物品的矩形区域，用于碰撞检测和位置管理</span>
        <span class="hljs-variable language_">self</span>.has_used = <span class="hljs-literal">False</span> <span class="hljs-comment"># 物品是否已经被玩家使用过，避免重复使用</span>

    <span class="hljs-keyword">def</span> <span class="hljs-title function_">draw</span>(<span class="hljs-params">self, screen</span>):
        <span class="hljs-keyword">if</span> <span class="hljs-variable language_">self</span>.frames:
            screen.blit(<span class="hljs-variable language_">self</span>.frames[<span class="hljs-variable language_">self</span>.current_frame_index], <span class="hljs-variable language_">self</span>.rect) <span class="hljs-comment"># 绘制当前动画帧</span>

<span class="hljs-keyword">class</span> <span class="hljs-title class_">Spring</span>(<span class="hljs-title class_ inherited__">Item</span>):
    probability = <span class="hljs-number">0.5</span> <span class="hljs-comment"># 弹簧生成的概率，50%</span>

    <span class="hljs-keyword">def</span> <span class="hljs-title function_">__init__</span>(<span class="hljs-params">self, platform</span>):
        <span class="hljs-built_in">super</span>().__init__(platform)
        <span class="hljs-variable language_">self</span>.frames = [Utils.load_img(<span class="hljs-string">f&quot;spring_<span class="hljs-subst">{i}</span>.png&quot;</span>, (<span class="hljs-number">30</span>, <span class="hljs-number">30</span>)) <span class="hljs-keyword">for</span> i <span class="hljs-keyword">in</span> <span class="hljs-built_in">range</span>(<span class="hljs-number">2</span>)] <span class="hljs-comment"># 加载弹簧的两帧动画</span>
        <span class="hljs-variable language_">self</span>.rect = <span class="hljs-variable language_">self</span>.frames[<span class="hljs-number">0</span>].get_rect() <span class="hljs-comment"># 获取弹簧图片的矩形区域，用于碰撞检测和位置管理</span>
        <span class="hljs-variable language_">self</span>.sound = Utils.load_sound(<span class="hljs-string">&quot;spring&quot;</span>) <span class="hljs-comment"># 加载弹簧音效</span>
        <span class="hljs-variable language_">self</span>.animate_played = <span class="hljs-literal">False</span> <span class="hljs-comment"># 标记动画是否已经播放过，避免重复播放</span>
    
    <span class="hljs-keyword">def</span> <span class="hljs-title function_">animate</span>(<span class="hljs-params">self</span>):
        <span class="hljs-keyword">if</span> <span class="hljs-variable language_">self</span>.has_used <span class="hljs-keyword">and</span> <span class="hljs-keyword">not</span> <span class="hljs-variable language_">self</span>.animate_played:
            <span class="hljs-variable language_">self</span>.animate_timer += <span class="hljs-variable language_">self</span>.animate_speed <span class="hljs-comment"># 增加动画计时器</span>
            <span class="hljs-keyword">if</span> <span class="hljs-variable language_">self</span>.animate_timer &gt;= <span class="hljs-number">1</span>: <span class="hljs-comment"># 如果计时器达到切换动画的条件</span>
                <span class="hljs-variable language_">self</span>.animate_timer = <span class="hljs-number">0</span> <span class="hljs-comment"># 重置动画计时器</span>
                <span class="hljs-variable language_">self</span>.current_frame_index = (<span class="hljs-variable language_">self</span>.current_frame_index + <span class="hljs-number">1</span>) % <span class="hljs-built_in">len</span>(<span class="hljs-variable language_">self</span>.frames) <span class="hljs-comment"># 切换到下一帧动画</span>
                <span class="hljs-keyword">if</span> <span class="hljs-variable language_">self</span>.current_frame_index == <span class="hljs-built_in">len</span>(<span class="hljs-variable language_">self</span>.frames) - <span class="hljs-number">1</span>: <span class="hljs-comment"># 如果动画已经播放到最后一帧，标记动画已经播放过</span>
                    <span class="hljs-variable language_">self</span>.animate_played = <span class="hljs-literal">True</span>
        <span class="hljs-keyword">elif</span> <span class="hljs-keyword">not</span> <span class="hljs-variable language_">self</span>.has_used:
            <span class="hljs-variable language_">self</span>.current_frame_index = <span class="hljs-number">0</span> <span class="hljs-comment"># 如果没有被使用过，保持在第一帧动画</span>

<span class="hljs-keyword">class</span> <span class="hljs-title class_">GameSession</span>:
    <span class="hljs-keyword">def</span> <span class="hljs-title function_">update</span>(<span class="hljs-params">self</span>): <span class="hljs-comment"># 更新游戏数据</span>
        <span class="hljs-variable language_">self</span>.update_scroll() <span class="hljs-comment"># 更新滚动逻辑</span>

        <span class="hljs-keyword">for</span> platform <span class="hljs-keyword">in</span> <span class="hljs-variable language_">self</span>.platforms:
            platform.update() <span class="hljs-comment"># 更新平台状态</span>
        <span class="hljs-variable language_">self</span>.player.update() <span class="hljs-comment"># 更新玩家状态</span>

        <span class="hljs-keyword">if</span> <span class="hljs-variable language_">self</span>.player.speed_y &gt;= <span class="hljs-number">0</span>: <span class="hljs-comment"># 只有当玩家正在向下移动时才检测碰撞，向上移动时不检测</span>
            <span class="hljs-comment"># 检测玩家与平台的碰撞，并处理跳跃逻辑，同时检测玩家与道具的碰撞，并处理道具效果</span>
            <span class="hljs-keyword">for</span> platform <span class="hljs-keyword">in</span> <span class="hljs-variable language_">self</span>.platforms:
                <span class="hljs-variable language_">self</span>.platform_colliderect(platform) <span class="hljs-comment"># 检测玩家与平台的碰撞，并处理跳跃逻辑</span>
                <span class="hljs-keyword">if</span> platform.item: <span class="hljs-comment"># 如果平台上有道具，检测玩家与道具的碰撞，并处理道具效果</span>
                    <span class="hljs-variable language_">self</span>.item_colliderect(platform.item)
</code></pre>
<p>确认弹簧道具没有问题后，我们继续补充竹蜻蜓道具的逻辑，并在生成新的平台时引入竹蜻蜓道具。</p>
<figure data-type="image" tabindex="24"><img src="https://www.guanngxu.com/post-images/1774858250656.png" alt=""></figure>
<figure data-type="image" tabindex="25"><img src="https://www.guanngxu.com/post-images/1774858255457.png" alt=""></figure>
<pre class="hljs"><code><span class="hljs-keyword">class</span> <span class="hljs-title class_">Propeller</span>(<span class="hljs-title class_ inherited__">Item</span>):
    probability = <span class="hljs-number">0.8</span> <span class="hljs-comment"># 螺旋桨生成的概率，10%</span>
    animate_speed = <span class="hljs-number">0.1</span> <span class="hljs-comment"># 螺旋桨动画的速度，经验值，表示每帧切换动画的概率</span>
    <span class="hljs-keyword">def</span> <span class="hljs-title function_">__init__</span>(<span class="hljs-params">self, platform</span>):
        <span class="hljs-built_in">super</span>().__init__(platform)
        <span class="hljs-variable language_">self</span>.frames = [Utils.load_img(<span class="hljs-string">f&quot;propeller_<span class="hljs-subst">{i}</span>.png&quot;</span>, (<span class="hljs-number">40</span>, <span class="hljs-number">20</span>)) <span class="hljs-keyword">for</span> i <span class="hljs-keyword">in</span> <span class="hljs-built_in">range</span>(<span class="hljs-number">2</span>)] <span class="hljs-comment"># 加载螺旋桨的两帧动画</span>
        <span class="hljs-variable language_">self</span>.rect = <span class="hljs-variable language_">self</span>.frames[<span class="hljs-number">0</span>].get_rect() <span class="hljs-comment"># 获取螺旋桨图片的矩形区域，用于碰撞检测和位置管理</span>
        <span class="hljs-variable language_">self</span>.sound = Utils.load_sound(<span class="hljs-string">&quot;propeller&quot;</span>) <span class="hljs-comment"># 加载螺旋桨音效</span>
        <span class="hljs-variable language_">self</span>.fly_duration_timer = <span class="hljs-number">150</span> <span class="hljs-comment"># 竹蜻蜓效果持续的帧数（约2.5秒）</span>
        <span class="hljs-variable language_">self</span>.player = <span class="hljs-literal">None</span> <span class="hljs-comment"># 记录被螺旋桨影响的玩家实例，方便在 update 中处理竹蜻蜓效果</span>

    <span class="hljs-keyword">def</span> <span class="hljs-title function_">update</span>(<span class="hljs-params">self</span>):
        <span class="hljs-keyword">if</span> <span class="hljs-keyword">not</span> <span class="hljs-variable language_">self</span>.has_used:
            <span class="hljs-built_in">super</span>().update() <span class="hljs-comment"># 调用父类的 update 方法，保持物品跟随平台移动</span>
        <span class="hljs-keyword">else</span>:
            <span class="hljs-keyword">if</span> <span class="hljs-variable language_">self</span>.has_used <span class="hljs-keyword">and</span> <span class="hljs-variable language_">self</span>.fly_duration_timer &gt; <span class="hljs-number">0</span>:
                <span class="hljs-variable language_">self</span>.player.speed_y = -<span class="hljs-number">12</span> - GRAVITY <span class="hljs-comment"># 竹蜻蜓给予玩家一个持续的向上的速度，模拟竹蜻蜓效果，同时考虑重力影响</span>
                <span class="hljs-variable language_">self</span>.rect.midbottom = <span class="hljs-variable language_">self</span>.player.rect.midtop <span class="hljs-comment"># 竹蜻蜓效果期间，物品跟随玩家移动，保持在玩家头顶</span>
                <span class="hljs-variable language_">self</span>.rect.centerx -= <span class="hljs-number">5</span> <span class="hljs-comment"># 细节微调位置</span>
                <span class="hljs-variable language_">self</span>.rect.centery -= <span class="hljs-number">5</span>
                <span class="hljs-comment"># TODO：因为对应的平台被回收了，对应没有调用 item 指定的 draw 等方案</span>
                <span class="hljs-built_in">print</span>(<span class="hljs-variable language_">self</span>.fly_duration_timer)
                <span class="hljs-variable language_">self</span>.fly_duration_timer -= <span class="hljs-number">1</span> <span class="hljs-comment"># 竹蜻蜓效果持续期间，减少计时器</span>
            <span class="hljs-keyword">else</span>:
                <span class="hljs-variable language_">self</span>.player = <span class="hljs-literal">None</span> <span class="hljs-comment"># 竹蜻蜓效果结束，重置玩家引用</span>

    <span class="hljs-keyword">def</span> <span class="hljs-title function_">apply_effect</span>(<span class="hljs-params">self, player</span>):
        <span class="hljs-keyword">if</span> <span class="hljs-variable language_">self</span>.has_used: <span class="hljs-keyword">return</span> <span class="hljs-comment"># 如果已经被使用过，直接返回，避免重复使用</span>
        <span class="hljs-variable language_">self</span>.has_used = <span class="hljs-literal">True</span> <span class="hljs-comment"># 标记为已使用</span>
        player.speed_y = -<span class="hljs-number">12</span> <span class="hljs-comment"># 螺旋桨给予玩家一个持续的向上的速度，模拟竹蜻蜓效果</span>
        <span class="hljs-variable language_">self</span>.player = player <span class="hljs-comment"># 记录被螺旋桨影响的玩家实例</span>
        <span class="hljs-keyword">if</span> <span class="hljs-variable language_">self</span>.sound:
            <span class="hljs-variable language_">self</span>.sound.play() <span class="hljs-comment"># 播放螺旋桨音效</span>
    
    <span class="hljs-keyword">def</span> <span class="hljs-title function_">animate</span>(<span class="hljs-params">self</span>):
        <span class="hljs-keyword">if</span> <span class="hljs-variable language_">self</span>.has_used:
            <span class="hljs-variable language_">self</span>.animate_timer += <span class="hljs-variable language_">self</span>.animate_speed <span class="hljs-comment"># 增加动画计时器</span>
            <span class="hljs-keyword">if</span> <span class="hljs-variable language_">self</span>.animate_timer &gt;= <span class="hljs-number">1</span>: <span class="hljs-comment"># 如果计时器达到切换动画的条件</span>
                <span class="hljs-variable language_">self</span>.animate_timer = <span class="hljs-number">0</span> <span class="hljs-comment"># 重置动画计时器</span>
                <span class="hljs-variable language_">self</span>.current_frame_index = (<span class="hljs-variable language_">self</span>.current_frame_index + <span class="hljs-number">1</span>) % <span class="hljs-built_in">len</span>(<span class="hljs-variable language_">self</span>.frames) <span class="hljs-comment"># 切换到下一帧动画</span>

<span class="hljs-keyword">class</span> <span class="hljs-title class_">GameSession</span>:
    <span class="hljs-keyword">def</span> <span class="hljs-title function_">update_scroll</span>(<span class="hljs-params">self</span>):
        <span class="hljs-comment"># 省略部分代码</span>
        <span class="hljs-keyword">while</span> <span class="hljs-built_in">len</span>(<span class="hljs-variable language_">self</span>.platforms) &lt; <span class="hljs-number">8</span>: <span class="hljs-comment"># 保持屏幕上至少有8个平台</span>
            new_platform = Platform(random.randint(<span class="hljs-number">0</span>, SCREEN_WIDTH - <span class="hljs-number">70</span>), random.randint(-<span class="hljs-number">100</span>, -<span class="hljs-number">40</span>))
            <span class="hljs-keyword">if</span> Utils.hit_probability(Spring.probability): <span class="hljs-comment"># 根据弹簧的生成概率决定是否在新平台上生成弹簧</span>
                spring = Spring(new_platform)
                new_platform.item = spring <span class="hljs-comment"># 将弹簧作为平台的一个属性，方便后续碰撞检测和更新</span>
            <span class="hljs-keyword">elif</span> Utils.hit_probability(Propeller.probability): <span class="hljs-comment"># 根据螺旋桨的生成概率决定是否在新平台上生成螺旋桨</span>
                propeller = Propeller(new_platform)
                new_platform.item = propeller <span class="hljs-comment"># 将螺旋桨作为平台的一个属性，方便后续碰撞检测和更新</span>
            <span class="hljs-variable language_">self</span>.platforms.append(new_platform)
</code></pre>
<p>现在当玩家碰到竹蜻蜓道具后，即会触发飞行效果，但是生效的时间并不符合我们的预期，分析之后确认原因在于道具始终和平台绑定，当平台被移除后道具则跟着消失，因此对于竹蜻蜓这样的道具需要在玩家碰到它后，让其和玩家绑定才不会消失。</p>
<figure data-type="image" tabindex="26"><img src="https://www.guanngxu.com/post-images/1774858915697.png" alt=""></figure>
<figure data-type="image" tabindex="27"><img src="https://www.guanngxu.com/post-images/1774858920932.png" alt=""></figure>
<figure data-type="image" tabindex="28"><img src="https://www.guanngxu.com/post-images/1774858926069.png" alt=""></figure>
<pre class="hljs"><code><span class="hljs-keyword">class</span> <span class="hljs-title class_">Player</span>:
    <span class="hljs-keyword">def</span> <span class="hljs-title function_">__init__</span>(<span class="hljs-params">self</span>):
        <span class="hljs-comment"># 省略部分代码</span>
        <span class="hljs-variable language_">self</span>.active_item = <span class="hljs-literal">None</span> <span class="hljs-comment"># 记录当前正在影响玩家的道具实例，方便在 update 中处理道具效果</span>

    <span class="hljs-keyword">def</span> <span class="hljs-title function_">update</span>(<span class="hljs-params">self</span>):
        <span class="hljs-keyword">if</span> <span class="hljs-variable language_">self</span>.active_item:
            <span class="hljs-variable language_">self</span>.active_item.update() <span class="hljs-comment"># 如果有正在影响玩家的道具，更新道具状态，处理道具效果</span>
            <span class="hljs-variable language_">self</span>.active_item.animate() <span class="hljs-comment"># 如果有正在影响玩家的道具，执行道具动画</span>
            <span class="hljs-variable language_">self</span>.active_item.rect.midbottom = <span class="hljs-variable language_">self</span>.rect.midtop <span class="hljs-comment"># 竹蜻蜓效果期间，物品跟随玩家移动，保持在玩家头顶</span>
            <span class="hljs-variable language_">self</span>.active_item.rect.centerx -= <span class="hljs-number">5</span> <span class="hljs-comment"># 细节微调位置</span>
            <span class="hljs-variable language_">self</span>.active_item.rect.centery -= <span class="hljs-number">5</span>
            <span class="hljs-variable language_">self</span>.speed_y = <span class="hljs-variable language_">self</span>.active_item.fly_velocity <span class="hljs-comment"># 竹蜻蜓效果期间，玩家获得一个持续的向上的速度，模拟竹蜻蜓效果</span>
            <span class="hljs-keyword">if</span> <span class="hljs-variable language_">self</span>.active_item.fly_duration_timer &lt;= <span class="hljs-number">0</span>: <span class="hljs-comment"># 竹蜻蜓效果结束</span>
                <span class="hljs-variable language_">self</span>.active_item = <span class="hljs-literal">None</span> <span class="hljs-comment"># 重置当前正在影响玩家的道具实例，结束竹蜻蜓效果</span>

        <span class="hljs-variable language_">self</span>.speed_y += GRAVITY <span class="hljs-comment"># 每帧增加重力加速度</span>
        <span class="hljs-variable language_">self</span>.rect.y += <span class="hljs-variable language_">self</span>.speed_y <span class="hljs-comment"># 根据速度更新玩家的 y 坐标</span>
<span class="hljs-comment"># 省略部分代码</span>

    <span class="hljs-keyword">def</span> <span class="hljs-title function_">draw</span>(<span class="hljs-params">self, screen</span>):
        screen.blit(<span class="hljs-variable language_">self</span>.image, <span class="hljs-variable language_">self</span>.rect) <span class="hljs-comment"># 将玩家图片绘制在屏幕底部中央位置</span>
        <span class="hljs-keyword">if</span> <span class="hljs-variable language_">self</span>.active_item:
            <span class="hljs-variable language_">self</span>.active_item.draw(screen) <span class="hljs-comment"># 如果有正在影响玩家的道具，绘制道具</span>

<span class="hljs-keyword">class</span> <span class="hljs-title class_">Propeller</span>(<span class="hljs-title class_ inherited__">Item</span>):
    probability = <span class="hljs-number">0.8</span> <span class="hljs-comment"># 螺旋桨生成的概率，10%</span>
    animate_speed = <span class="hljs-number">0.1</span> <span class="hljs-comment"># 螺旋桨动画的速度，经验值，表示每帧切换动画的概率</span>
    <span class="hljs-keyword">def</span> <span class="hljs-title function_">__init__</span>(<span class="hljs-params">self, platform</span>):
        <span class="hljs-comment"># 省略部分代码</span>
        <span class="hljs-variable language_">self</span>.fly_velocity = -<span class="hljs-number">12</span> <span class="hljs-comment"># 竹蜻蜓给予玩家的持续向上的速度，经验值，表示比普通跳跃更高的跳跃效果</span>

    <span class="hljs-keyword">def</span> <span class="hljs-title function_">update</span>(<span class="hljs-params">self</span>):
        <span class="hljs-keyword">if</span> <span class="hljs-keyword">not</span> <span class="hljs-variable language_">self</span>.has_used:
            <span class="hljs-built_in">super</span>().update() <span class="hljs-comment"># 调用父类的 update 方法，保持物品跟随平台移动</span>
        <span class="hljs-keyword">else</span>:
            <span class="hljs-keyword">if</span> <span class="hljs-variable language_">self</span>.has_used <span class="hljs-keyword">and</span> <span class="hljs-variable language_">self</span>.fly_duration_timer &gt; <span class="hljs-number">0</span>:
                <span class="hljs-variable language_">self</span>.fly_duration_timer -= <span class="hljs-number">1</span> <span class="hljs-comment"># 竹蜻蜓效果持续期间，减少计时器</span>

    <span class="hljs-keyword">def</span> <span class="hljs-title function_">apply_effect</span>(<span class="hljs-params">self, player</span>):
        <span class="hljs-keyword">if</span> <span class="hljs-variable language_">self</span>.has_used: <span class="hljs-keyword">return</span> <span class="hljs-comment"># 如果已经被使用过，直接返回，避免重复使用</span>
        <span class="hljs-variable language_">self</span>.has_used = <span class="hljs-literal">True</span> <span class="hljs-comment"># 标记为已使用</span>
        player.active_item = <span class="hljs-variable language_">self</span> <span class="hljs-comment"># 记录当前正在影响玩家的道具实例，方便在 update 中处理道具效果</span>
        <span class="hljs-keyword">if</span> <span class="hljs-variable language_">self</span>.sound:
            <span class="hljs-variable language_">self</span>.sound.play() <span class="hljs-comment"># 播放螺旋桨音效</span>
</code></pre>
<p>考虑到后续可能还有火箭喷气背包等道具也需要做同样的处理，需要一直跟随着玩家，并且也需要一直循环播放动画，因此我们可以抽出来<code>follow_player</code>和<code>animate</code>方法。另外我们本次更新考虑将代码进行极小部分重构，以及修复可能存在的 bug。</p>
<figure data-type="image" tabindex="29"><img src="https://www.guanngxu.com/post-images/1774860270773.png" alt=""></figure>
<figure data-type="image" tabindex="30"><img src="https://www.guanngxu.com/post-images/1774860280275.png" alt=""></figure>
<figure data-type="image" tabindex="31"><img src="https://www.guanngxu.com/post-images/1774860284478.png" alt=""></figure>
<figure data-type="image" tabindex="32"><img src="https://www.guanngxu.com/post-images/1774860288903.png" alt=""></figure>
<figure data-type="image" tabindex="33"><img src="https://www.guanngxu.com/post-images/1774860293170.png" alt=""></figure>
<figure data-type="image" tabindex="34"><img src="https://www.guanngxu.com/post-images/1774860297752.png" alt=""></figure>
<pre class="hljs"><code><span class="hljs-keyword">class</span> <span class="hljs-title class_">Player</span>:
    <span class="hljs-keyword">def</span> <span class="hljs-title function_">update</span>(<span class="hljs-params">self</span>):
        <span class="hljs-keyword">if</span> <span class="hljs-variable language_">self</span>.active_item:
            <span class="hljs-variable language_">self</span>.active_item.update() <span class="hljs-comment"># 如果有正在影响玩家的道具，更新道具状态，处理道具效果</span>
            <span class="hljs-variable language_">self</span>.active_item.animate() <span class="hljs-comment"># 如果有正在影响玩家的道具，执行道具动画</span>
            <span class="hljs-variable language_">self</span>.active_item.follow_player(<span class="hljs-variable language_">self</span>) <span class="hljs-comment"># 如果有正在影响玩家的道具，执行跟随玩家的逻辑，保持道具与玩家位置同步</span>
            <span class="hljs-variable language_">self</span>.speed_y = <span class="hljs-variable language_">self</span>.active_item.fly_velocity - GRAVITY <span class="hljs-comment"># 竹蜻蜓效果期间，玩家获得一个持续的向上的速度，模拟竹蜻蜓效果</span>
            <span class="hljs-keyword">if</span> <span class="hljs-variable language_">self</span>.active_item.fly_duration_timer &lt;= <span class="hljs-number">0</span>: <span class="hljs-comment"># 竹蜻蜓效果结束</span>
                <span class="hljs-variable language_">self</span>.active_item = <span class="hljs-literal">None</span> <span class="hljs-comment"># 重置当前正在影响玩家的道具实例，结束竹蜻蜓效果</span>
    <span class="hljs-comment"># 省略部分代码......</span>

<span class="hljs-keyword">class</span> <span class="hljs-title class_">Item</span>:
    <span class="hljs-comment"># 某些道具需要跟随玩家移动，比如竹蜻蜓，这个方法可以用来实现跟随玩家的逻辑</span>
    <span class="hljs-keyword">def</span> <span class="hljs-title function_">follow_player</span>(<span class="hljs-params">self, player</span>):
        <span class="hljs-keyword">pass</span>

    <span class="hljs-keyword">def</span> <span class="hljs-title function_">animate</span>(<span class="hljs-params">self</span>):
        <span class="hljs-keyword">pass</span>
    
<span class="hljs-keyword">class</span> <span class="hljs-title class_">Propeller</span>(<span class="hljs-title class_ inherited__">Item</span>):
    probability = <span class="hljs-number">0.1</span> <span class="hljs-comment"># 螺旋桨生成的概率，10%</span>
    animate_speed = <span class="hljs-number">0.1</span> <span class="hljs-comment"># 螺旋桨动画的速度，经验值，表示每帧切换动画的概率</span>

    <span class="hljs-keyword">def</span> <span class="hljs-title function_">follow_player</span>(<span class="hljs-params">self, player</span>):
        <span class="hljs-variable language_">self</span>.rect.midbottom = player.rect.midtop <span class="hljs-comment"># 竹蜻蜓效果期间，物品跟随玩家移动，保持在玩家头顶</span>
        <span class="hljs-variable language_">self</span>.rect.centerx -= <span class="hljs-number">5</span> <span class="hljs-comment"># 细节微调位置，让它更居中一些</span>
        <span class="hljs-variable language_">self</span>.rect.centery -= <span class="hljs-number">5</span>

    <span class="hljs-keyword">def</span> <span class="hljs-title function_">apply_effect</span>(<span class="hljs-params">self, player</span>):
        <span class="hljs-keyword">if</span> <span class="hljs-variable language_">self</span>.has_used: <span class="hljs-keyword">return</span> <span class="hljs-comment"># 如果已经被使用过，直接返回，避免重复使用</span>
        <span class="hljs-variable language_">self</span>.platform.item = <span class="hljs-literal">None</span> <span class="hljs-comment"># 使用后将平台上的道具引用清除，避免重复使用</span>
        <span class="hljs-variable language_">self</span>.has_used = <span class="hljs-literal">True</span> <span class="hljs-comment"># 标记为已使用</span>
        player.active_item = <span class="hljs-variable language_">self</span> <span class="hljs-comment"># 记录当前正在影响玩家的道具实例，方便在 update 中处理道具效果</span>
        <span class="hljs-keyword">if</span> <span class="hljs-variable language_">self</span>.sound:
            <span class="hljs-variable language_">self</span>.sound.play() <span class="hljs-comment"># 播放螺旋桨音效</span>

<span class="hljs-keyword">class</span> <span class="hljs-title class_">GameSession</span>:
    <span class="hljs-keyword">def</span> <span class="hljs-title function_">__init__</span>(<span class="hljs-params">self</span>):
        <span class="hljs-comment"># 省略部分代码......</span>
        <span class="hljs-variable language_">self</span>.font = pygame.font.SysFont(<span class="hljs-string">&quot;Arial&quot;</span>, <span class="hljs-number">24</span>) <span class="hljs-comment"># 初始化字体对象，用于绘制分数</span>
        <span class="hljs-variable language_">self</span>._init_platforms() <span class="hljs-comment"># 初始化平台列表</span>

    <span class="hljs-keyword">def</span> <span class="hljs-title function_">update_scroll</span>(<span class="hljs-params">self</span>):
        <span class="hljs-comment"># 省略部分代码</span>
        <span class="hljs-keyword">while</span> <span class="hljs-built_in">len</span>(<span class="hljs-variable language_">self</span>.platforms) &lt; <span class="hljs-number">8</span>: <span class="hljs-comment"># 保持屏幕上至少有8个平台</span>
            new_platform = Platform(random.randint(<span class="hljs-number">0</span>, SCREEN_WIDTH - <span class="hljs-number">70</span>), random.randint(-<span class="hljs-number">100</span>, -<span class="hljs-number">40</span>))
            <span class="hljs-variable language_">self</span>.generate_item(new_platform)
            <span class="hljs-variable language_">self</span>.platforms.append(new_platform)

    <span class="hljs-keyword">def</span> <span class="hljs-title function_">generate_item</span>(<span class="hljs-params">self, platform</span>):
        <span class="hljs-keyword">if</span> Utils.hit_probability(Spring.probability): <span class="hljs-comment"># 根据弹簧的生成概率决定是否在平台上生成弹簧</span>
            spring = Spring(platform)
            platform.item = spring <span class="hljs-comment"># 将弹簧作为平台的一个属性，方便后续碰撞检测和更新</span>
        <span class="hljs-keyword">elif</span> Utils.hit_probability(Propeller.probability): <span class="hljs-comment"># 根据螺旋桨的生成概率决定是否在平台上生成螺旋桨</span>
            propeller = Propeller(platform)
            platform.item = propeller <span class="hljs-comment"># 将螺旋桨作为平台的一个属性，方便后续碰撞检测和更新</span>
    <span class="hljs-comment"># 检测玩家与平台的碰撞，并处理跳跃逻辑</span>
    <span class="hljs-keyword">def</span> <span class="hljs-title function_">platform_colliderect</span>(<span class="hljs-params">self, platform</span>):
        <span class="hljs-comment"># 省略部分代码......</span>
                    <span class="hljs-keyword">if</span> keys[pygame.K_SPACE]: <span class="hljs-comment"># 只有按空格才跳跃</span>
                        <span class="hljs-variable language_">self</span>.player.jump() <span class="hljs-comment"># 调用玩家的 jump 方法，执行跳跃逻辑</span>
                    <span class="hljs-keyword">return</span> <span class="hljs-literal">True</span> <span class="hljs-comment"># 碰撞后返回 True，表示玩家成功站在平台上</span>

    <span class="hljs-keyword">def</span> <span class="hljs-title function_">update</span>(<span class="hljs-params">self</span>): <span class="hljs-comment"># 更新游戏数据</span>
        <span class="hljs-comment"># 省略部分代码......</span>
            <span class="hljs-keyword">for</span> platform <span class="hljs-keyword">in</span> <span class="hljs-variable language_">self</span>.platforms:
                <span class="hljs-keyword">if</span> platform.item: <span class="hljs-comment"># 如果平台上有道具，检测玩家与道具的碰撞，并处理道具效果</span>
                    <span class="hljs-variable language_">self</span>.item_colliderect(platform.item)
                <span class="hljs-keyword">if</span> <span class="hljs-variable language_">self</span>.platform_colliderect(platform): <span class="hljs-comment"># 检测玩家与平台的碰撞，并处理跳跃逻辑</span>
                    <span class="hljs-keyword">break</span> <span class="hljs-comment"># 如果已经检测到玩家与一个平台发生碰撞并处理了跳跃逻辑，就不再继续检测其他平台，避免多重碰撞导致的跳跃问题</span>
            

    <span class="hljs-keyword">def</span> <span class="hljs-title function_">draw</span>(<span class="hljs-params">self</span>): <span class="hljs-comment"># 绘制游戏画面</span>
        <span class="hljs-comment"># 省略部分代码</span>
        scrore_text = <span class="hljs-variable language_">self</span>.font.render(<span class="hljs-string">f&quot;Score: <span class="hljs-subst">{self.score}</span>&quot;</span>, <span class="hljs-literal">True</span>, (<span class="hljs-number">0</span>, <span class="hljs-number">0</span>, <span class="hljs-number">0</span>))
        <span class="hljs-variable language_">self</span>.screen.blit(scrore_text, (<span class="hljs-number">10</span>, <span class="hljs-number">10</span>)) <span class="hljs-comment"># 在屏幕左</span>
</code></pre>
<p>下面我们引入菜单页面，用于提示玩家玩法，标注作者信息。</p>
<figure data-type="image" tabindex="35"><img src="https://www.guanngxu.com/post-images/1774861074289.png" alt=""></figure>
<figure data-type="image" tabindex="36"><img src="https://www.guanngxu.com/post-images/1774861078903.png" alt=""></figure>
<figure data-type="image" tabindex="37"><img src="https://www.guanngxu.com/post-images/1774861083587.png" alt=""></figure>
<figure data-type="image" tabindex="38"><img src="https://www.guanngxu.com/post-images/1774861087357.png" alt=""></figure>
<pre class="hljs"><code>WHITE, BLACK, GRAY = (<span class="hljs-number">255</span>, <span class="hljs-number">255</span>, <span class="hljs-number">255</span>), (<span class="hljs-number">0</span>, <span class="hljs-number">0</span>, <span class="hljs-number">0</span>), (<span class="hljs-number">100</span>, <span class="hljs-number">100</span>, <span class="hljs-number">100</span>) <span class="hljs-comment"># 颜色常量（RGB）</span>
TITLE_COLOR = (<span class="hljs-number">255</span>, <span class="hljs-number">120</span>, <span class="hljs-number">0</span>)             <span class="hljs-comment"># 主界面标题的颜色</span>

<span class="hljs-keyword">class</span> <span class="hljs-title class_">GameSession</span>:
    <span class="hljs-keyword">def</span> <span class="hljs-title function_">__init__</span>(<span class="hljs-params">self</span>):
        <span class="hljs-comment"># 省略部分代码......</span>
        <span class="hljs-variable language_">self</span>.score = <span class="hljs-number">0</span> <span class="hljs-comment"># 初始化分数</span>
        <span class="hljs-variable language_">self</span>.score_font = pygame.font.SysFont(<span class="hljs-string">&quot;Arial&quot;</span>, <span class="hljs-number">24</span>, bold=<span class="hljs-literal">True</span>) <span class="hljs-comment"># 初始化字体对象，用于绘制分数</span>
        <span class="hljs-variable language_">self</span>.title_font = pygame.font.SysFont(<span class="hljs-string">&quot;Comic Sans MS&quot;</span>, <span class="hljs-number">55</span>, bold=<span class="hljs-literal">True</span>)
        <span class="hljs-variable language_">self</span>.author_font = pygame.font.SysFont(<span class="hljs-string">&quot;Arial&quot;</span>, <span class="hljs-number">22</span>, italic=<span class="hljs-literal">True</span>)
        <span class="hljs-variable language_">self</span>.start_font = pygame.font.SysFont(<span class="hljs-string">&quot;Arial&quot;</span>, <span class="hljs-number">26</span>, bold=<span class="hljs-literal">True</span>)
        <span class="hljs-variable language_">self</span>.state = <span class="hljs-string">&quot;menu&quot;</span> <span class="hljs-comment"># 游戏状态，初始为菜单界面</span>
        <span class="hljs-variable language_">self</span>._init_platforms() <span class="hljs-comment"># 初始化平台列表</span>
    <span class="hljs-keyword">def</span> <span class="hljs-title function_">update</span>(<span class="hljs-params">self</span>): <span class="hljs-comment"># 更新游戏数据</span>
        <span class="hljs-variable language_">self</span>.update_scroll() <span class="hljs-comment"># 更新滚动逻辑</span>

        <span class="hljs-keyword">for</span> platform <span class="hljs-keyword">in</span> <span class="hljs-variable language_">self</span>.platforms:
            platform.update() <span class="hljs-comment"># 更新平台状态</span>
        <span class="hljs-variable language_">self</span>.player.update() <span class="hljs-comment"># 更新玩家状态</span>

        <span class="hljs-keyword">if</span> <span class="hljs-variable language_">self</span>.player.speed_y &gt;= <span class="hljs-number">0</span>: <span class="hljs-comment"># 只有当玩家正在向下移动时才检测碰撞，向上移动时不检测</span>
            <span class="hljs-comment"># 检测玩家与平台的碰撞，并处理跳跃逻辑，同时检测玩家与道具的碰撞，并处理道具效果</span>
            <span class="hljs-keyword">for</span> platform <span class="hljs-keyword">in</span> <span class="hljs-variable language_">self</span>.platforms:
                <span class="hljs-keyword">if</span> platform.item: <span class="hljs-comment"># 如果平台上有道具，检测玩家与道具的碰撞，并处理道具效果</span>
                    <span class="hljs-variable language_">self</span>.item_colliderect(platform.item)
                <span class="hljs-keyword">if</span> <span class="hljs-variable language_">self</span>.platform_colliderect(platform): <span class="hljs-comment"># 检测玩家与平台的碰撞，并处理跳跃逻辑</span>
                    <span class="hljs-keyword">break</span> <span class="hljs-comment"># 如果已经检测到玩家与一个平台发生碰撞并处理了跳跃逻辑，就不再继续检测其他平台，避免多重碰撞导致的跳跃问题</span>
        
        <span class="hljs-keyword">if</span> <span class="hljs-variable language_">self</span>.player.rect.top &gt; SCREEN_HEIGHT: <span class="hljs-comment"># 如果玩家掉出屏幕底部，游戏结束，重置游戏状态</span>
            <span class="hljs-variable language_">self</span>.__init__() <span class="hljs-comment"># 重新初始化游戏状态，回到菜单界面</span>

    <span class="hljs-keyword">def</span> <span class="hljs-title function_">draw</span>(<span class="hljs-params">self</span>): <span class="hljs-comment"># 绘制游戏画面</span>
        <span class="hljs-variable language_">self</span>.screen.blit(<span class="hljs-variable language_">self</span>.bg, (<span class="hljs-number">0</span>, <span class="hljs-number">0</span>)) <span class="hljs-comment"># 绘制背景图片</span>
        <span class="hljs-keyword">for</span> platform <span class="hljs-keyword">in</span> <span class="hljs-variable language_">self</span>.platforms:
            platform.draw(<span class="hljs-variable language_">self</span>.screen) <span class="hljs-comment"># 绘制平台</span>
        <span class="hljs-variable language_">self</span>.player.draw(<span class="hljs-variable language_">self</span>.screen) <span class="hljs-comment"># 绘制玩家</span>
        scrore_text = <span class="hljs-variable language_">self</span>.score_font.render(<span class="hljs-string">f&quot;Score: <span class="hljs-subst">{self.score}</span>&quot;</span>, <span class="hljs-literal">True</span>, BLACK)
        <span class="hljs-variable language_">self</span>.screen.blit(scrore_text, (<span class="hljs-number">10</span>, <span class="hljs-number">10</span>)) <span class="hljs-comment"># 在屏幕左</span>

    <span class="hljs-keyword">def</span> <span class="hljs-title function_">draw_menu</span>(<span class="hljs-params">self</span>):
        <span class="hljs-variable language_">self</span>.screen.blit(<span class="hljs-variable language_">self</span>.bg, (<span class="hljs-number">0</span>, <span class="hljs-number">0</span>)) <span class="hljs-comment"># 绘制背景图片</span>
        <span class="hljs-comment"># 渲染标题</span>
        title_surf = <span class="hljs-variable language_">self</span>.title_font.render(<span class="hljs-string">&quot;Doodle Jump&quot;</span>, <span class="hljs-literal">True</span>, TITLE_COLOR)
        <span class="hljs-variable language_">self</span>.screen.blit(title_surf, (SCREEN_WIDTH//<span class="hljs-number">2</span> - title_surf.get_width()//<span class="hljs-number">2</span>, <span class="hljs-number">120</span>))
        <span class="hljs-comment"># 渲染作者信息</span>
        author_surf = <span class="hljs-variable language_">self</span>.author_font.render(<span class="hljs-string">&quot;Author: Guanngxu&quot;</span>, <span class="hljs-literal">True</span>, BLACK)
        <span class="hljs-variable language_">self</span>.screen.blit(author_surf, (SCREEN_WIDTH//<span class="hljs-number">2</span> - author_surf.get_width()//<span class="hljs-number">2</span>, <span class="hljs-number">210</span>))
        <span class="hljs-comment"># 渲染提示文字</span>
        msg = <span class="hljs-variable language_">self</span>.start_font.render(<span class="hljs-string">&quot;Press [ SPACE ] to Start&quot;</span>, <span class="hljs-literal">True</span>, (<span class="hljs-number">50</span>, <span class="hljs-number">50</span>, <span class="hljs-number">50</span>))
        <span class="hljs-variable language_">self</span>.screen.blit(msg, (SCREEN_WIDTH//<span class="hljs-number">2</span> - msg.get_width()//<span class="hljs-number">2</span>, <span class="hljs-number">380</span>))
        pygame.display.flip()

    <span class="hljs-keyword">def</span> <span class="hljs-title function_">run</span>(<span class="hljs-params">self</span>):
        <span class="hljs-keyword">while</span> <span class="hljs-literal">True</span>:
            <span class="hljs-keyword">for</span> event <span class="hljs-keyword">in</span> pygame.event.get():
                <span class="hljs-keyword">if</span> event.<span class="hljs-built_in">type</span> == pygame.QUIT:
                    pygame.quit()
                    sys.exit()
            
            <span class="hljs-keyword">if</span> <span class="hljs-variable language_">self</span>.state == <span class="hljs-string">&quot;menu&quot;</span>:
                <span class="hljs-variable language_">self</span>.draw_menu()
                keys = pygame.key.get_pressed()
                <span class="hljs-keyword">if</span> keys[pygame.K_SPACE]: <span class="hljs-comment"># 在菜单界面按空格开始游戏</span>
                    <span class="hljs-variable language_">self</span>.state = <span class="hljs-string">&quot;playing&quot;</span>
            <span class="hljs-keyword">elif</span> <span class="hljs-variable language_">self</span>.state == <span class="hljs-string">&quot;playing&quot;</span>:
                <span class="hljs-variable language_">self</span>.update()
                <span class="hljs-variable language_">self</span>.draw()

            <span class="hljs-variable language_">self</span>.clock.tick(FPS)  <span class="hljs-comment"># 控制游戏循环以每秒FPS帧的速度运行</span>
            pygame.display.flip()  <span class="hljs-comment"># 更新屏幕显示</span>
</code></pre>
<p>运行后确认效果如预期，继续引入背景白云朵朵，只需要加入<code>Cloud</code>类后，在合适的地方实例化并调用其更新方法即可。</p>
<figure data-type="image" tabindex="39"><img src="https://www.guanngxu.com/post-images/1774862062574.png" alt=""></figure>
<figure data-type="image" tabindex="40"><img src="https://www.guanngxu.com/post-images/1774862067655.png" alt=""></figure>
<figure data-type="image" tabindex="41"><img src="https://www.guanngxu.com/post-images/1774862071647.png" alt=""></figure>
<figure data-type="image" tabindex="42"><img src="https://www.guanngxu.com/post-images/1774862076458.png" alt=""></figure>
<pre class="hljs"><code><span class="hljs-keyword">class</span> <span class="hljs-title class_">Cloud</span>():
    <span class="hljs-keyword">def</span> <span class="hljs-title function_">__init__</span>(<span class="hljs-params">self</span>):
        size = random.randint(<span class="hljs-number">50</span>, <span class="hljs-number">100</span>) <span class="hljs-comment"># 云朵的随机大小</span>
        <span class="hljs-variable language_">self</span>.frame = Utils.load_img(<span class="hljs-string">&quot;cloud.png&quot;</span>, (size, size // <span class="hljs-number">2</span>)) <span class="hljs-comment"># 加载云朵图片并缩放到随机大小</span>
        <span class="hljs-variable language_">self</span>.rect = <span class="hljs-variable language_">self</span>.frame.get_rect(
            x=random.randint(<span class="hljs-number">0</span>, SCREEN_WIDTH - size), <span class="hljs-comment"># 云朵的随机水平位置</span>
            y=random.randint(<span class="hljs-number">0</span>, SCREEN_HEIGHT // <span class="hljs-number">2</span>) <span class="hljs-comment"># 云朵的随机垂直位置，限制在屏幕上半部分</span>
        )
        <span class="hljs-variable language_">self</span>.direction = random.choice([-<span class="hljs-number">1</span>, <span class="hljs-number">1</span>]) <span class="hljs-comment"># 云朵的移动方向，-1表示向左，1表示向右</span>
        <span class="hljs-variable language_">self</span>.speed = random.uniform(<span class="hljs-number">0.5</span>, <span class="hljs-number">1.5</span>) <span class="hljs-comment"># 云朵的移动速度，随机生成一个经验值</span>
        <span class="hljs-variable language_">self</span>.alpha = random.randint(<span class="hljs-number">100</span>, <span class="hljs-number">255</span>) <span class="hljs-comment"># 云朵的随机透明度，增加视觉层次感</span>

    <span class="hljs-keyword">def</span> <span class="hljs-title function_">update</span>(<span class="hljs-params">self</span>):
        <span class="hljs-variable language_">self</span>.rect.x += <span class="hljs-variable language_">self</span>.direction * <span class="hljs-variable language_">self</span>.speed <span class="hljs-comment"># 云朵以固定速度向左右移动</span>
        <span class="hljs-keyword">if</span> <span class="hljs-variable language_">self</span>.rect.right &lt; <span class="hljs-number">0</span>: <span class="hljs-comment"># 如果云朵完全移出左边界</span>
            <span class="hljs-variable language_">self</span>.rect.left = SCREEN_WIDTH <span class="hljs-comment"># 从右边重新出现</span>
        <span class="hljs-keyword">elif</span> <span class="hljs-variable language_">self</span>.rect.left &gt; SCREEN_WIDTH: <span class="hljs-comment"># 如果云朵完全移出右边界</span>
            <span class="hljs-variable language_">self</span>.rect.right = <span class="hljs-number">0</span> <span class="hljs-comment"># 从左边重新出现</span>
        
        <span class="hljs-variable language_">self</span>.rect.y += random.uniform(-<span class="hljs-number">0.5</span>, <span class="hljs-number">0.5</span>) <span class="hljs-comment"># 云朵在垂直方向上有轻微的随机漂浮效果</span>
        <span class="hljs-keyword">if</span> <span class="hljs-variable language_">self</span>.rect.top &gt; SCREEN_HEIGHT // <span class="hljs-number">2</span>: <span class="hljs-comment"># 限制云朵在屏幕上半部分</span>
            <span class="hljs-variable language_">self</span>.rect.y = random.randint(<span class="hljs-number">0</span>, SCREEN_HEIGHT // <span class="hljs-number">2</span>) <span class="hljs-comment"># 如果云朵漂浮到下半部分，随机重置到上半部分</span>

    <span class="hljs-keyword">def</span> <span class="hljs-title function_">draw</span>(<span class="hljs-params">self, screen</span>):
        <span class="hljs-comment"># 设置透明度</span>
        temp_surface = <span class="hljs-variable language_">self</span>.frame.copy()
        temp_surface.set_alpha(<span class="hljs-variable language_">self</span>.alpha)
        screen.blit(temp_surface, <span class="hljs-variable language_">self</span>.rect) <span class="hljs-comment"># 绘制云朵图片</span>

<span class="hljs-keyword">class</span> <span class="hljs-title class_">GameSession</span>:
    <span class="hljs-keyword">def</span> <span class="hljs-title function_">__init__</span>(<span class="hljs-params">self</span>):
        <span class="hljs-comment"># 省略部分代码......</span>
        <span class="hljs-variable language_">self</span>.clouds = [Cloud() <span class="hljs-keyword">for</span> _ <span class="hljs-keyword">in</span> <span class="hljs-built_in">range</span>(<span class="hljs-number">5</span>)] <span class="hljs-comment"># 初始化云朵列表，创建5朵云</span>

    <span class="hljs-keyword">def</span> <span class="hljs-title function_">update_scroll</span>(<span class="hljs-params">self</span>):
        <span class="hljs-comment"># 省略部分代码......</span>
        <span class="hljs-keyword">for</span> cloud <span class="hljs-keyword">in</span> <span class="hljs-variable language_">self</span>.clouds:
            cloud.rect.y += scroll_amount // <span class="hljs-number">2</span> <span class="hljs-comment"># 云朵以较慢的速度向下滚动</span>

    <span class="hljs-keyword">def</span> <span class="hljs-title function_">update</span>(<span class="hljs-params">self</span>): <span class="hljs-comment"># 更新游戏数据</span>
        <span class="hljs-variable language_">self</span>.update_scroll() <span class="hljs-comment"># 更新滚动逻辑</span>

        <span class="hljs-keyword">for</span> cloud <span class="hljs-keyword">in</span> <span class="hljs-variable language_">self</span>.clouds:
            cloud.update() <span class="hljs-comment"># 更新云朵状态</span>
        <span class="hljs-comment"># 省略部分代码......</span>

    <span class="hljs-keyword">def</span> <span class="hljs-title function_">draw</span>(<span class="hljs-params">self</span>): <span class="hljs-comment"># 绘制游戏画面</span>
        <span class="hljs-variable language_">self</span>.screen.blit(<span class="hljs-variable language_">self</span>.bg, (<span class="hljs-number">0</span>, <span class="hljs-number">0</span>)) <span class="hljs-comment"># 绘制背景图片</span>
        <span class="hljs-keyword">for</span> cloud <span class="hljs-keyword">in</span> <span class="hljs-variable language_">self</span>.clouds:
            cloud.draw(<span class="hljs-variable language_">self</span>.screen) <span class="hljs-comment"># 绘制云朵</span>
        <span class="hljs-comment"># 省略部分代码......</span>
</code></pre>
<p>考虑到当玩家掉到屏幕外时直接调用<code>__init__</code>方法可能存在一些风险，比如重复初始化 Pygame 导致系统资源分配异常或某些模块状态错乱；每次都会重新创建<code>pygame.display.set_mode</code>和多个<code>pygame.font.SysFont</code>对象，旧的对象如果没有被 Python 的垃圾回收机制及时清理，会导致内存占用不断上升。所以单独抽取出来<code>reset_game</code>方法，只初始化游戏内的对象数据。</p>
<figure data-type="image" tabindex="43"><img src="https://www.guanngxu.com/post-images/1774862905256.png" alt=""></figure>
<figure data-type="image" tabindex="44"><img src="https://www.guanngxu.com/post-images/1774862911305.png" alt=""></figure>
<pre class="hljs"><code><span class="hljs-keyword">class</span> <span class="hljs-title class_">Cloud</span>():
    <span class="hljs-keyword">def</span> <span class="hljs-title function_">update</span>(<span class="hljs-params">self</span>):
        <span class="hljs-comment"># 省略部分代码......</span>
        <span class="hljs-keyword">if</span> <span class="hljs-variable language_">self</span>.rect.top &gt; SCREEN_HEIGHT:
            <span class="hljs-variable language_">self</span>.rect.y = random.randint(<span class="hljs-number">0</span>, SCREEN_HEIGHT // <span class="hljs-number">2</span>) <span class="hljs-comment"># 如果云朵漂浮到下半部分，随机重置到上半部分</span>

<span class="hljs-keyword">class</span> <span class="hljs-title class_">GameSession</span>:
    <span class="hljs-keyword">def</span> <span class="hljs-title function_">reset_game</span>(<span class="hljs-params">self</span>):
        <span class="hljs-comment"># 重新初始化游戏内的对象数据</span>
        <span class="hljs-variable language_">self</span>.player = Player() 
        <span class="hljs-variable language_">self</span>.platforms = [] 
        <span class="hljs-variable language_">self</span>.clouds = [Cloud() <span class="hljs-keyword">for</span> _ <span class="hljs-keyword">in</span> <span class="hljs-built_in">range</span>(<span class="hljs-number">5</span>)] 
        <span class="hljs-variable language_">self</span>.score = <span class="hljs-number">0</span>
        <span class="hljs-variable language_">self</span>.state = <span class="hljs-string">&quot;menu&quot;</span> <span class="hljs-comment"># 回到菜单界面</span>
        <span class="hljs-variable language_">self</span>._init_platforms() 

    <span class="hljs-keyword">def</span> <span class="hljs-title function_">update</span>(<span class="hljs-params">self</span>): <span class="hljs-comment"># 更新游戏数据</span>
        <span class="hljs-comment"># 省略部分代码......</span>
        <span class="hljs-keyword">if</span> <span class="hljs-variable language_">self</span>.player.rect.top &gt; SCREEN_HEIGHT: <span class="hljs-comment"># 如果玩家掉出屏幕底部，游戏结束，重置游戏状态</span>
            <span class="hljs-variable language_">self</span>.reset_game() <span class="hljs-comment"># 重新初始化游戏状态，回到菜单界面</span>
</code></pre>
<p>潜在问题都解决后，最后我们引入火箭喷气背包道具，它的大部分代码应是和竹蜻蜓道具一样。需要注意的是由于素材问题，需要将玩家进行翻转，以确保火箭喷气背包可以背在玩家右侧。</p>
<figure data-type="image" tabindex="45"><img src="https://www.guanngxu.com/post-images/1774863445730.png" alt=""></figure>
<figure data-type="image" tabindex="46"><img src="https://www.guanngxu.com/post-images/1774863451089.png" alt=""></figure>
<figure data-type="image" tabindex="47"><img src="https://www.guanngxu.com/post-images/1774863455519.png" alt=""></figure>
<figure data-type="image" tabindex="48"><img src="https://www.guanngxu.com/post-images/1774863505337.png" alt=""></figure>
<pre class="hljs"><code><span class="hljs-keyword">class</span> <span class="hljs-title class_">Player</span>:
    <span class="hljs-keyword">def</span> <span class="hljs-title function_">__init__</span>(<span class="hljs-params">self</span>):
        <span class="hljs-variable language_">self</span>.image = Utils.load_img(<span class="hljs-string">&quot;player.png&quot;</span>, (<span class="hljs-number">40</span>, <span class="hljs-number">40</span>)) <span class="hljs-comment"># 加载玩家图片并缩放到40x40像素</span>
        <span class="hljs-variable language_">self</span>.image = pygame.transform.flip(<span class="hljs-variable language_">self</span>.image, flip_x=<span class="hljs-literal">True</span>, flip_y=<span class="hljs-literal">False</span>) <span class="hljs-comment"># 将玩家镜像翻转，方便背上火箭道具</span>
        <span class="hljs-comment"># 省略部分代码......</span>

<span class="hljs-keyword">class</span> <span class="hljs-title class_">Item</span>:
    <span class="hljs-keyword">def</span> <span class="hljs-title function_">apply_effect</span>(<span class="hljs-params">self, player</span>):
        <span class="hljs-keyword">if</span> <span class="hljs-variable language_">self</span>.has_used: <span class="hljs-keyword">return</span> <span class="hljs-comment"># 如果已经被使用过，直接返回，避免重复使用</span>
        <span class="hljs-variable language_">self</span>.platform.item = <span class="hljs-literal">None</span> <span class="hljs-comment"># 使用后将平台上的道具引用清除，避免重复使用</span>
        <span class="hljs-variable language_">self</span>.has_used = <span class="hljs-literal">True</span> <span class="hljs-comment"># 标记为已使用</span>
        player.active_item = <span class="hljs-variable language_">self</span> <span class="hljs-comment"># 记录当前正在影响玩家的道具实例，方便在 update 中处理道具效果</span>
        <span class="hljs-keyword">if</span> <span class="hljs-variable language_">self</span>.sound:
            <span class="hljs-variable language_">self</span>.sound.play() <span class="hljs-comment"># 播放螺旋桨音效</span>

    <span class="hljs-keyword">def</span> <span class="hljs-title function_">animate</span>(<span class="hljs-params">self</span>):
        <span class="hljs-keyword">if</span> <span class="hljs-variable language_">self</span>.has_used:
            <span class="hljs-variable language_">self</span>.animate_timer += <span class="hljs-variable language_">self</span>.animate_speed <span class="hljs-comment"># 增加动画计时器</span>
            <span class="hljs-keyword">if</span> <span class="hljs-variable language_">self</span>.animate_timer &gt;= <span class="hljs-number">1</span>: <span class="hljs-comment"># 如果计时器达到切换动画的条件</span>
                <span class="hljs-variable language_">self</span>.animate_timer = <span class="hljs-number">0</span> <span class="hljs-comment"># 重置动画计时器</span>
                <span class="hljs-variable language_">self</span>.current_frame_index = (<span class="hljs-variable language_">self</span>.current_frame_index + <span class="hljs-number">1</span>) % <span class="hljs-built_in">len</span>(<span class="hljs-variable language_">self</span>.frames) <span class="hljs-comment"># 切换到下一帧动画</span>

<span class="hljs-keyword">class</span> <span class="hljs-title class_">Rocket</span>(<span class="hljs-title class_ inherited__">Item</span>):
    probability = <span class="hljs-number">0.05</span> <span class="hljs-comment"># 火箭生成的概率，5%</span>
    animate_speed = <span class="hljs-number">0.5</span> <span class="hljs-comment"># 火箭动画的速度，经验值，表示每帧切换动画的概率</span>
    <span class="hljs-keyword">def</span> <span class="hljs-title function_">__init__</span>(<span class="hljs-params">self, platform</span>):
        <span class="hljs-built_in">super</span>().__init__(platform)
        <span class="hljs-variable language_">self</span>._init_frames()
        <span class="hljs-variable language_">self</span>.rect = <span class="hljs-variable language_">self</span>.frames[<span class="hljs-number">0</span>].get_rect() <span class="hljs-comment"># 获取火箭图片的矩形区域，用于碰撞检测和位置管理</span>
        <span class="hljs-variable language_">self</span>.sound = Utils.load_sound(<span class="hljs-string">&quot;rocket&quot;</span>) <span class="hljs-comment"># 加载火箭音效</span>
        <span class="hljs-variable language_">self</span>.fly_duration_timer = <span class="hljs-number">200</span> <span class="hljs-comment"># 火箭效果持续的帧数（约3.3秒）</span>
        <span class="hljs-variable language_">self</span>.fly_velocity = -<span class="hljs-number">18</span> <span class="hljs-comment"># 火箭给予玩家的持续向上的速度，经验值，表示比竹蜻蜓更高的跳跃效果</span>

    <span class="hljs-keyword">def</span> <span class="hljs-title function_">_init_frames</span>(<span class="hljs-params">self</span>):
        img_sheet = Utils.load_img(<span class="hljs-string">&quot;rocket.png&quot;</span>, (<span class="hljs-number">160</span>, <span class="hljs-number">240</span>)) <span class="hljs-comment"># 加载火箭图片</span>
        frame_width = <span class="hljs-number">40</span>
        frame_height = <span class="hljs-number">80</span>
        <span class="hljs-comment"># 计算当前帧的位置：(x, y, width, height)</span>
        <span class="hljs-keyword">for</span> i <span class="hljs-keyword">in</span> <span class="hljs-built_in">range</span>(<span class="hljs-number">3</span>):
            <span class="hljs-keyword">for</span> j <span class="hljs-keyword">in</span> <span class="hljs-built_in">range</span>(<span class="hljs-number">4</span>):
                frame = img_sheet.subsurface((j * frame_width, i * frame_height, frame_width, frame_height)) <span class="hljs-comment"># 从图片中提取每一帧</span>
                <span class="hljs-variable language_">self</span>.frames.append(frame) <span class="hljs-comment"># 将每一帧添加到动画帧列表中</span>
        <span class="hljs-variable language_">self</span>.frames.remove(<span class="hljs-variable language_">self</span>.frames[<span class="hljs-built_in">len</span>(<span class="hljs-variable language_">self</span>.frames) - <span class="hljs-number">1</span>]) <span class="hljs-comment"># 移除最后一帧，因为它是空白的</span>
        <span class="hljs-variable language_">self</span>.frames.remove(<span class="hljs-variable language_">self</span>.frames[<span class="hljs-built_in">len</span>(<span class="hljs-variable language_">self</span>.frames) - <span class="hljs-number">1</span>]) <span class="hljs-comment"># 再次移除最后一帧，因为它是空白的</span>

    <span class="hljs-keyword">def</span> <span class="hljs-title function_">follow_player</span>(<span class="hljs-params">self, player</span>):
        <span class="hljs-variable language_">self</span>.rect.y = player.rect.y - <span class="hljs-number">25</span>
        <span class="hljs-comment"># 因为玩家的宽度是20</span>
        <span class="hljs-variable language_">self</span>.rect.x = player.rect.x + <span class="hljs-number">28</span> <span class="hljs-comment"># 细节微调位置</span>

    <span class="hljs-keyword">def</span> <span class="hljs-title function_">update</span>(<span class="hljs-params">self</span>):
        <span class="hljs-keyword">if</span> <span class="hljs-keyword">not</span> <span class="hljs-variable language_">self</span>.has_used:
            <span class="hljs-built_in">super</span>().update() <span class="hljs-comment"># 调用父类的 update 方法，保持物品跟随平台移动</span>
            <span class="hljs-variable language_">self</span>.rect.y += <span class="hljs-number">22</span> <span class="hljs-comment"># 调整图片位置</span>
        <span class="hljs-keyword">else</span>:
            <span class="hljs-keyword">if</span> <span class="hljs-variable language_">self</span>.has_used <span class="hljs-keyword">and</span> <span class="hljs-variable language_">self</span>.fly_duration_timer &gt; <span class="hljs-number">0</span>:
                <span class="hljs-variable language_">self</span>.fly_duration_timer -= <span class="hljs-number">1</span> <span class="hljs-comment"># 火箭效果持续期间，减少计时器</span>

<span class="hljs-keyword">class</span> <span class="hljs-title class_">GameSession</span>:
    <span class="hljs-keyword">def</span> <span class="hljs-title function_">generate_item</span>(<span class="hljs-params">self, platform</span>):
        <span class="hljs-keyword">if</span> Utils.hit_probability(Spring.probability): <span class="hljs-comment"># 根据弹簧的生成概率决定是否在平台上生成弹簧</span>
            spring = Spring(platform)
            platform.item = spring <span class="hljs-comment"># 将弹簧作为平台的一个属性，方便后续碰撞检测和更新</span>
        <span class="hljs-keyword">elif</span> Utils.hit_probability(Propeller.probability): <span class="hljs-comment"># 根据螺旋桨的生成概率决定是否在平台上生成螺旋桨</span>
            propeller = Propeller(platform)
            platform.item = propeller <span class="hljs-comment"># 将螺旋桨作为平台的一个属性，方便后续碰撞检测和更新</span>
        <span class="hljs-keyword">elif</span> Utils.hit_probability(Rocket.probability): <span class="hljs-comment"># 根据火箭的生成概率决定是否在新平台上生成火箭</span>
            rocket = Rocket(platform)
            platform.item = rocket <span class="hljs-comment"># 将火箭作为平台的一个属性，方便后续碰撞检测和更新</span>
</code></pre>
<h3 id="程序打包">程序打包</h3><p>程序完成后需要进行打包方可给到用户使用，我们打包工具使用 <a href="https://pyinstaller.org/en/stable/">Pyinstaller</a>。当 PyInstaller 把所有东西打包进一个<code>.exe</code>时，运行时它会把资源解压到一个临时的文件夹（通常叫<code>_MEIPASS</code>）。但我们的代码里写的是死路径<code>./images/</code>，程序会去<code>.exe</code>所在的文件夹找，而不是去临时文件夹找，会导致报错。</p>
<p>我们需要修改<code>Utils</code>类，添加一个路径转换函数，之后方可进行打包。</p>
<figure data-type="image" tabindex="49"><img src="https://www.guanngxu.com/post-images/1774864124668.png" alt=""></figure>
<pre class="hljs"><code><span class="hljs-keyword">class</span> <span class="hljs-title class_">Utils</span>:
    <span class="hljs-comment"># --- 资源加载助手函数 ---</span>
<span class="hljs-meta">    @staticmethod</span>
    <span class="hljs-keyword">def</span> <span class="hljs-title function_">resource_path</span>(<span class="hljs-params">relative_path</span>):
        <span class="hljs-keyword">try</span>:
            <span class="hljs-comment"># PyInstaller创建临时文件夹,将路径存储于_MEIPASS</span>
            base_path = sys._MEIPASS
        <span class="hljs-keyword">except</span> Exception:
            base_path = os.path.abspath(<span class="hljs-string">&quot;.&quot;</span>)
        <span class="hljs-keyword">return</span> os.path.join(base_path, relative_path)
    
<span class="hljs-meta">    @staticmethod</span>
    <span class="hljs-keyword">def</span> <span class="hljs-title function_">load_img</span>(<span class="hljs-params">name, scale=<span class="hljs-literal">None</span></span>):
        <span class="hljs-comment"># path = os.path.join(&quot;./images/&quot;, name) # 拼接图片路径</span>
        path = Utils.resource_path(os.path.join(<span class="hljs-string">&quot;images&quot;</span>, name)) <span class="hljs-comment"># 获取资源路径，兼容打包后的路径</span>
        <span class="hljs-keyword">try</span>:
            img = pygame.image.load(path).convert_alpha() <span class="hljs-comment"># 加载图片并转换Alpha通道（透明度优化）</span>
            <span class="hljs-keyword">if</span> scale: img = pygame.transform.scale(img, scale) <span class="hljs-comment"># 如果指定了尺寸，则进行缩放</span>
            <span class="hljs-keyword">return</span> img
        <span class="hljs-keyword">except</span>:
            <span class="hljs-comment"># 如果图片丢失，生成一个占位用的灰色方块，确保程序不崩溃</span>
            surf = pygame.Surface(scale <span class="hljs-keyword">if</span> scale <span class="hljs-keyword">else</span> (<span class="hljs-number">30</span>, <span class="hljs-number">30</span>))
            surf.fill((<span class="hljs-number">200</span>, <span class="hljs-number">200</span>, <span class="hljs-number">200</span>))
            <span class="hljs-keyword">return</span> surf
    
<span class="hljs-meta">    @staticmethod</span>
    <span class="hljs-keyword">def</span> <span class="hljs-title function_">load_sound</span>(<span class="hljs-params">name</span>):
        base_path = <span class="hljs-string">&quot;./sounds/&quot;</span>
        <span class="hljs-keyword">for</span> ext <span class="hljs-keyword">in</span> [<span class="hljs-string">&#x27;.wav&#x27;</span>, <span class="hljs-string">&#x27;.mp3&#x27;</span>, <span class="hljs-string">&#x27;.ogg&#x27;</span>]: <span class="hljs-comment"># 遍历常见的音频格式</span>
            <span class="hljs-comment"># full_path = os.path.join(base_path, name + ext)</span>
            full_path = Utils.resource_path(os.path.join(base_path, name + ext)) <span class="hljs-comment"># 获取资源路径，兼容打包后的路径</span>
            <span class="hljs-keyword">if</span> os.path.exists(full_path):
                <span class="hljs-keyword">try</span>: <span class="hljs-keyword">return</span> pygame.mixer.Sound(full_path)
                <span class="hljs-keyword">except</span>: <span class="hljs-keyword">continue</span>
        <span class="hljs-keyword">return</span> <span class="hljs-literal">None</span> <span class="hljs-comment"># 如果没有找到任何格式的音效文件，返回 None</span>
</code></pre>
<p>随后安装 Pyinstaller，打开终端（Command Prompt 或 PowerShell），运行以下命令即可。</p>
<pre class="hljs"><code>pip install pyinstaller
</code></pre>
<p>随后在项目根目录下（即<code>main.py</code>所在的文件夹），输入以下命令。需要注意的是 Windows 下资源路径的分隔符是分号<code>;</code>，命令运行完成后可以看到会生成<code>dist</code>文件夹，其中的<code>main.exe</code>就是单文件游戏。</p>
<pre class="hljs"><code>pyinstaller --onefile --noconsole --add-data &quot;images;images&quot; --add-data &quot;sounds;sounds&quot; main.py
</code></pre>
<ul>
<li><code>--onefile</code>(或<code>-F</code>): 将所有内容打包成一个单一的<code>.exe</code>文件；</li>
<li><code>--noconsole</code>(或<code>-w</code>): 运行游戏时不显示黑色的控制台窗口；</li>
<li><code>--add-data &quot;源文件夹;目标文件夹&quot;</code>: 这是核心！它告诉 PyInstaller 把<code>images</code>和<code>sounds</code>文件夹里的内容也塞进<code>.exe</code>里；</li>
<li><code>main.py</code>: 我们的的主程序文件名。</li>
</ul>
<hr>
<p><strong>附完整代码</strong></p>
<pre class="hljs"><code><span class="hljs-keyword">import</span> pygame
<span class="hljs-keyword">import</span> random
<span class="hljs-keyword">import</span> sys
<span class="hljs-keyword">import</span> os


SCREEN_WIDTH, SCREEN_HEIGHT = <span class="hljs-number">400</span>, <span class="hljs-number">600</span>  <span class="hljs-comment"># 定义游戏窗口的宽度和高度</span>
FPS = <span class="hljs-number">60</span>                                <span class="hljs-comment"># 每秒刷新的帧数，控制游戏运行流畅度</span>
GRAVITY = <span class="hljs-number">0.7</span>                           <span class="hljs-comment"># 模拟物理重力，每帧给玩家增加的向下速度</span>
WHITE, BLACK, GRAY = (<span class="hljs-number">255</span>, <span class="hljs-number">255</span>, <span class="hljs-number">255</span>), (<span class="hljs-number">0</span>, <span class="hljs-number">0</span>, <span class="hljs-number">0</span>), (<span class="hljs-number">100</span>, <span class="hljs-number">100</span>, <span class="hljs-number">100</span>) <span class="hljs-comment"># 颜色常量（RGB）</span>
TITLE_COLOR = (<span class="hljs-number">255</span>, <span class="hljs-number">120</span>, <span class="hljs-number">0</span>)             <span class="hljs-comment"># 主界面标题的颜色</span>

<span class="hljs-keyword">class</span> <span class="hljs-title class_">Utils</span>:
    <span class="hljs-comment"># --- 资源加载助手函数 ---</span>
<span class="hljs-meta">    @staticmethod</span>
    <span class="hljs-keyword">def</span> <span class="hljs-title function_">resource_path</span>(<span class="hljs-params">relative_path</span>):
        <span class="hljs-keyword">try</span>:
            <span class="hljs-comment"># PyInstaller创建临时文件夹,将路径存储于_MEIPASS</span>
            base_path = sys._MEIPASS
        <span class="hljs-keyword">except</span> Exception:
            base_path = os.path.abspath(<span class="hljs-string">&quot;.&quot;</span>)
        <span class="hljs-keyword">return</span> os.path.join(base_path, relative_path)
    
<span class="hljs-meta">    @staticmethod</span>
    <span class="hljs-keyword">def</span> <span class="hljs-title function_">load_img</span>(<span class="hljs-params">name, scale=<span class="hljs-literal">None</span></span>):
        <span class="hljs-comment"># path = os.path.join(&quot;./images/&quot;, name) # 拼接图片路径</span>
        path = Utils.resource_path(os.path.join(<span class="hljs-string">&quot;images&quot;</span>, name)) <span class="hljs-comment"># 获取资源路径，兼容打包后的路径</span>
        <span class="hljs-keyword">try</span>:
            img = pygame.image.load(path).convert_alpha() <span class="hljs-comment"># 加载图片并转换Alpha通道（透明度优化）</span>
            <span class="hljs-keyword">if</span> scale: img = pygame.transform.scale(img, scale) <span class="hljs-comment"># 如果指定了尺寸，则进行缩放</span>
            <span class="hljs-keyword">return</span> img
        <span class="hljs-keyword">except</span>:
            <span class="hljs-comment"># 如果图片丢失，生成一个占位用的灰色方块，确保程序不崩溃</span>
            surf = pygame.Surface(scale <span class="hljs-keyword">if</span> scale <span class="hljs-keyword">else</span> (<span class="hljs-number">30</span>, <span class="hljs-number">30</span>))
            surf.fill((<span class="hljs-number">200</span>, <span class="hljs-number">200</span>, <span class="hljs-number">200</span>))
            <span class="hljs-keyword">return</span> surf
    
<span class="hljs-meta">    @staticmethod</span>
    <span class="hljs-keyword">def</span> <span class="hljs-title function_">load_sound</span>(<span class="hljs-params">name</span>):
        base_path = <span class="hljs-string">&quot;./sounds/&quot;</span>
        <span class="hljs-keyword">for</span> ext <span class="hljs-keyword">in</span> [<span class="hljs-string">&#x27;.wav&#x27;</span>, <span class="hljs-string">&#x27;.mp3&#x27;</span>, <span class="hljs-string">&#x27;.ogg&#x27;</span>]: <span class="hljs-comment"># 遍历常见的音频格式</span>
            <span class="hljs-comment"># full_path = os.path.join(base_path, name + ext)</span>
            full_path = Utils.resource_path(os.path.join(base_path, name + ext)) <span class="hljs-comment"># 获取资源路径，兼容打包后的路径</span>
            <span class="hljs-keyword">if</span> os.path.exists(full_path):
                <span class="hljs-keyword">try</span>: <span class="hljs-keyword">return</span> pygame.mixer.Sound(full_path)
                <span class="hljs-keyword">except</span>: <span class="hljs-keyword">continue</span>
        <span class="hljs-keyword">return</span> <span class="hljs-literal">None</span> <span class="hljs-comment"># 如果没有找到任何格式的音效文件，返回 None</span>

<span class="hljs-meta">    @staticmethod</span>
    <span class="hljs-keyword">def</span> <span class="hljs-title function_">hit_probability</span>(<span class="hljs-params">prob</span>):
        <span class="hljs-keyword">return</span> random.random() &lt; prob <span class="hljs-comment"># 返回一个布尔值，表示是否以给定概率命中</span>

<span class="hljs-keyword">class</span> <span class="hljs-title class_">Player</span>:
    <span class="hljs-keyword">def</span> <span class="hljs-title function_">__init__</span>(<span class="hljs-params">self</span>):
        <span class="hljs-variable language_">self</span>.image = Utils.load_img(<span class="hljs-string">&quot;player.png&quot;</span>, (<span class="hljs-number">40</span>, <span class="hljs-number">40</span>)) <span class="hljs-comment"># 加载玩家图片并缩放到40x40像素</span>
        <span class="hljs-variable language_">self</span>.image = pygame.transform.flip(<span class="hljs-variable language_">self</span>.image, flip_x=<span class="hljs-literal">True</span>, flip_y=<span class="hljs-literal">False</span>) <span class="hljs-comment"># 将玩家镜像翻转，方便背上火箭道具</span>
        <span class="hljs-comment"># 第一个平台离 y 轴 50，平台高度 20，所以减去 70</span>
        <span class="hljs-variable language_">self</span>.rect = <span class="hljs-variable language_">self</span>.image.get_rect(center=(SCREEN_WIDTH // <span class="hljs-number">2</span>, SCREEN_HEIGHT - <span class="hljs-number">70</span>)) <span class="hljs-comment"># 获取玩家图片的矩形区域，用于碰撞检测和位置管理</span>
        <span class="hljs-variable language_">self</span>.speed_y = <span class="hljs-number">0</span> <span class="hljs-comment"># 玩家在 y 轴上的速度，初始为0</span>
        <span class="hljs-variable language_">self</span>.speed_x = <span class="hljs-number">8</span> <span class="hljs-comment"># 玩家在 x 轴上的速度，固定为8</span>
        <span class="hljs-variable language_">self</span>.jump_sound = Utils.load_sound(<span class="hljs-string">&quot;jump&quot;</span>) <span class="hljs-comment"># 加载跳跃音效</span>
        <span class="hljs-variable language_">self</span>.active_item = <span class="hljs-literal">None</span> <span class="hljs-comment"># 记录当前正在影响玩家的道具实例，方便在 update 中处理道具效果</span>

    <span class="hljs-keyword">def</span> <span class="hljs-title function_">jump</span>(<span class="hljs-params">self</span>):
        <span class="hljs-variable language_">self</span>.speed_y = -<span class="hljs-number">15</span> <span class="hljs-comment"># 碰撞后给予玩家一个向上的速度，模拟跳跃效果</span>
        <span class="hljs-keyword">if</span> <span class="hljs-variable language_">self</span>.jump_sound:
            <span class="hljs-variable language_">self</span>.jump_sound.play() <span class="hljs-comment"># 播放跳跃音效</span>

    <span class="hljs-keyword">def</span> <span class="hljs-title function_">update</span>(<span class="hljs-params">self</span>):
        <span class="hljs-keyword">if</span> <span class="hljs-variable language_">self</span>.active_item:
            <span class="hljs-variable language_">self</span>.active_item.update() <span class="hljs-comment"># 如果有正在影响玩家的道具，更新道具状态，处理道具效果</span>
            <span class="hljs-variable language_">self</span>.active_item.animate() <span class="hljs-comment"># 如果有正在影响玩家的道具，执行道具动画</span>
            <span class="hljs-variable language_">self</span>.active_item.follow_player(<span class="hljs-variable language_">self</span>) <span class="hljs-comment"># 如果有正在影响玩家的道具，执行跟随玩家的逻辑，保持道具与玩家位置同步</span>
            <span class="hljs-variable language_">self</span>.speed_y = <span class="hljs-variable language_">self</span>.active_item.fly_velocity - GRAVITY <span class="hljs-comment"># 竹蜻蜓效果期间，玩家获得一个持续的向上的速度，模拟竹蜻蜓效果</span>
            <span class="hljs-keyword">if</span> <span class="hljs-variable language_">self</span>.active_item.fly_duration_timer &lt;= <span class="hljs-number">0</span>: <span class="hljs-comment"># 竹蜻蜓效果结束</span>
                <span class="hljs-variable language_">self</span>.active_item = <span class="hljs-literal">None</span> <span class="hljs-comment"># 重置当前正在影响玩家的道具实例，结束竹蜻蜓效果</span>

        <span class="hljs-variable language_">self</span>.speed_y += GRAVITY <span class="hljs-comment"># 每帧增加重力加速度</span>
        <span class="hljs-variable language_">self</span>.rect.y += <span class="hljs-variable language_">self</span>.speed_y <span class="hljs-comment"># 根据速度更新玩家的 y 坐标</span>
        
        <span class="hljs-comment"># 处理键盘左右键输入</span>
        keys = pygame.key.get_pressed()
        <span class="hljs-keyword">if</span> keys[pygame.K_LEFT]:
            <span class="hljs-variable language_">self</span>.rect.x -= <span class="hljs-variable language_">self</span>.speed_x <span class="hljs-comment"># 向左移动</span>
        <span class="hljs-keyword">if</span> keys[pygame.K_RIGHT]:
            <span class="hljs-variable language_">self</span>.rect.x += <span class="hljs-variable language_">self</span>.speed_x <span class="hljs-comment"># 向右移动</span>

        <span class="hljs-keyword">if</span> <span class="hljs-variable language_">self</span>.rect.right &lt; <span class="hljs-number">0</span>: <span class="hljs-comment"># 如果玩家完全移出左边界</span>
            <span class="hljs-variable language_">self</span>.rect.left = SCREEN_WIDTH <span class="hljs-comment"># 从右边重新出现</span>
        <span class="hljs-keyword">elif</span> <span class="hljs-variable language_">self</span>.rect.left &gt; SCREEN_WIDTH: <span class="hljs-comment"># 如果玩家完全移出右边界</span>
            <span class="hljs-variable language_">self</span>.rect.right = <span class="hljs-number">0</span> <span class="hljs-comment"># 从左边重新出现</span>

    <span class="hljs-keyword">def</span> <span class="hljs-title function_">draw</span>(<span class="hljs-params">self, screen</span>):
        screen.blit(<span class="hljs-variable language_">self</span>.image, <span class="hljs-variable language_">self</span>.rect) <span class="hljs-comment"># 将玩家图片绘制在屏幕底部中央位置</span>
        <span class="hljs-keyword">if</span> <span class="hljs-variable language_">self</span>.active_item:
            <span class="hljs-variable language_">self</span>.active_item.draw(screen) <span class="hljs-comment"># 如果有正在影响玩家的道具，绘制道具</span>

<span class="hljs-keyword">class</span> <span class="hljs-title class_">Platform</span>:
    <span class="hljs-keyword">def</span> <span class="hljs-title function_">__init__</span>(<span class="hljs-params">self, x, y</span>):
        <span class="hljs-variable language_">self</span>.image = Utils.load_img(<span class="hljs-string">&quot;platform.png&quot;</span>, (<span class="hljs-number">70</span>, <span class="hljs-number">20</span>)) <span class="hljs-comment"># 加载平台图片并缩放到70x20像素</span>
        <span class="hljs-variable language_">self</span>.rect = <span class="hljs-variable language_">self</span>.image.get_rect(topleft=(x, y)) <span class="hljs-comment"># 获取平台图片的矩形区域，用于碰撞检测和位置管理</span>
        <span class="hljs-variable language_">self</span>.item = <span class="hljs-literal">None</span> <span class="hljs-comment"># 平台上可能有一个道具，初始为 None</span>

    <span class="hljs-keyword">def</span> <span class="hljs-title function_">update</span>(<span class="hljs-params">self</span>):
        <span class="hljs-keyword">if</span> <span class="hljs-variable language_">self</span>.item:
            <span class="hljs-variable language_">self</span>.item.update() <span class="hljs-comment"># 如果平台上有道具，更新道具状态</span>
            <span class="hljs-variable language_">self</span>.item.animate() <span class="hljs-comment"># 如果平台上有道具，执行道具动画</span>

    <span class="hljs-keyword">def</span> <span class="hljs-title function_">draw</span>(<span class="hljs-params">self, screen</span>):
        screen.blit(<span class="hljs-variable language_">self</span>.image, <span class="hljs-variable language_">self</span>.rect) <span class="hljs-comment"># 将平台图片绘制在屏幕上</span>
        <span class="hljs-keyword">if</span> <span class="hljs-variable language_">self</span>.item:
            <span class="hljs-variable language_">self</span>.item.draw(screen) <span class="hljs-comment"># 如果平台上有道具，绘制道具</span>

<span class="hljs-keyword">class</span> <span class="hljs-title class_">Item</span>:
    probability = <span class="hljs-number">0.3</span> <span class="hljs-comment"># 物品生成的概率，默认30%</span>
    animate_speed = <span class="hljs-number">0.2</span> <span class="hljs-comment"># 物品动画的速度，经验值，表示每帧切换动画的概率</span>
    
    <span class="hljs-keyword">def</span> <span class="hljs-title function_">__init__</span>(<span class="hljs-params">self, platform</span>):
        <span class="hljs-variable language_">self</span>.platform = platform <span class="hljs-comment"># 物品所在的平台</span>
        <span class="hljs-variable language_">self</span>.frames = [] <span class="hljs-comment"># 存储动画帧的列表</span>
        <span class="hljs-variable language_">self</span>.current_frame_index = <span class="hljs-number">0</span> <span class="hljs-comment"># 当前动画帧的索引</span>
        <span class="hljs-variable language_">self</span>.animate_timer = <span class="hljs-number">0</span> <span class="hljs-comment"># 动画计时器，用于控制动画切换速度</span>
        <span class="hljs-variable language_">self</span>.rect = <span class="hljs-literal">None</span> <span class="hljs-comment"># 物品的矩形区域，用于碰撞检测和位置管理</span>
        <span class="hljs-variable language_">self</span>.has_used = <span class="hljs-literal">False</span> <span class="hljs-comment"># 物品是否已经被玩家使用过，避免重复使用</span>

    <span class="hljs-comment"># 某些道具需要跟随玩家移动，比如竹蜻蜓，这个方法可以用来实现跟随玩家的逻辑</span>
    <span class="hljs-keyword">def</span> <span class="hljs-title function_">follow_player</span>(<span class="hljs-params">self, player</span>):
        <span class="hljs-keyword">pass</span>

    <span class="hljs-keyword">def</span> <span class="hljs-title function_">apply_effect</span>(<span class="hljs-params">self, player</span>):
        <span class="hljs-keyword">if</span> <span class="hljs-variable language_">self</span>.has_used: <span class="hljs-keyword">return</span> <span class="hljs-comment"># 如果已经被使用过，直接返回，避免重复使用</span>
        <span class="hljs-variable language_">self</span>.platform.item = <span class="hljs-literal">None</span> <span class="hljs-comment"># 使用后将平台上的道具引用清除，避免重复使用</span>
        <span class="hljs-variable language_">self</span>.has_used = <span class="hljs-literal">True</span> <span class="hljs-comment"># 标记为已使用</span>
        player.active_item = <span class="hljs-variable language_">self</span> <span class="hljs-comment"># 记录当前正在影响玩家的道具实例，方便在 update 中处理道具效果</span>
        <span class="hljs-keyword">if</span> <span class="hljs-variable language_">self</span>.sound:
            <span class="hljs-variable language_">self</span>.sound.play() <span class="hljs-comment"># 播放螺旋桨音效</span>

    <span class="hljs-keyword">def</span> <span class="hljs-title function_">animate</span>(<span class="hljs-params">self</span>):
        <span class="hljs-keyword">if</span> <span class="hljs-variable language_">self</span>.has_used:
            <span class="hljs-variable language_">self</span>.animate_timer += <span class="hljs-variable language_">self</span>.animate_speed <span class="hljs-comment"># 增加动画计时器</span>
            <span class="hljs-keyword">if</span> <span class="hljs-variable language_">self</span>.animate_timer &gt;= <span class="hljs-number">1</span>: <span class="hljs-comment"># 如果计时器达到切换动画的条件</span>
                <span class="hljs-variable language_">self</span>.animate_timer = <span class="hljs-number">0</span> <span class="hljs-comment"># 重置动画计时器</span>
                <span class="hljs-variable language_">self</span>.current_frame_index = (<span class="hljs-variable language_">self</span>.current_frame_index + <span class="hljs-number">1</span>) % <span class="hljs-built_in">len</span>(<span class="hljs-variable language_">self</span>.frames) <span class="hljs-comment"># 切换到下一帧动画</span>
    
    <span class="hljs-keyword">def</span> <span class="hljs-title function_">update</span>(<span class="hljs-params">self</span>):
        <span class="hljs-variable language_">self</span>.rect.midbottom = <span class="hljs-variable language_">self</span>.platform.rect.midtop <span class="hljs-comment"># 物品始终跟随平台移动，保持在平台顶部</span>

    <span class="hljs-keyword">def</span> <span class="hljs-title function_">draw</span>(<span class="hljs-params">self, screen</span>):
        <span class="hljs-keyword">if</span> <span class="hljs-variable language_">self</span>.frames:
            screen.blit(<span class="hljs-variable language_">self</span>.frames[<span class="hljs-variable language_">self</span>.current_frame_index], <span class="hljs-variable language_">self</span>.rect) <span class="hljs-comment"># 绘制当前动画帧</span>

<span class="hljs-keyword">class</span> <span class="hljs-title class_">Spring</span>(<span class="hljs-title class_ inherited__">Item</span>):
    probability = <span class="hljs-number">0.2</span> <span class="hljs-comment"># 弹簧生成的概率，20%</span>

    <span class="hljs-keyword">def</span> <span class="hljs-title function_">__init__</span>(<span class="hljs-params">self, platform</span>):
        <span class="hljs-built_in">super</span>().__init__(platform)
        <span class="hljs-variable language_">self</span>.frames = [Utils.load_img(<span class="hljs-string">f&quot;spring_<span class="hljs-subst">{i}</span>.png&quot;</span>, (<span class="hljs-number">30</span>, <span class="hljs-number">30</span>)) <span class="hljs-keyword">for</span> i <span class="hljs-keyword">in</span> <span class="hljs-built_in">range</span>(<span class="hljs-number">2</span>)] <span class="hljs-comment"># 加载弹簧的两帧动画</span>
        <span class="hljs-variable language_">self</span>.rect = <span class="hljs-variable language_">self</span>.frames[<span class="hljs-number">0</span>].get_rect() <span class="hljs-comment"># 获取弹簧图片的矩形区域，用于碰撞检测和位置管理</span>
        <span class="hljs-variable language_">self</span>.sound = Utils.load_sound(<span class="hljs-string">&quot;spring&quot;</span>) <span class="hljs-comment"># 加载弹簧音效</span>
        <span class="hljs-variable language_">self</span>.animate_played = <span class="hljs-literal">False</span> <span class="hljs-comment"># 标记动画是否已经播放过，避免重复播放</span>

    <span class="hljs-keyword">def</span> <span class="hljs-title function_">apply_effect</span>(<span class="hljs-params">self, player</span>):
        <span class="hljs-keyword">if</span> <span class="hljs-variable language_">self</span>.has_used: <span class="hljs-keyword">return</span> <span class="hljs-comment"># 如果已经被使用过，直接返回，避免重复使用</span>
        <span class="hljs-variable language_">self</span>.has_used = <span class="hljs-literal">True</span> <span class="hljs-comment"># 标记为已使用</span>
        player.speed_y = -<span class="hljs-number">20</span> <span class="hljs-comment"># 弹簧给予玩家一个更强的向上的速度，模拟更高的跳跃效果</span>
        <span class="hljs-keyword">if</span> <span class="hljs-variable language_">self</span>.sound:
            <span class="hljs-variable language_">self</span>.sound.play() <span class="hljs-comment"># 播放弹簧音效</span>
    
    <span class="hljs-keyword">def</span> <span class="hljs-title function_">animate</span>(<span class="hljs-params">self</span>):
        <span class="hljs-keyword">if</span> <span class="hljs-variable language_">self</span>.has_used <span class="hljs-keyword">and</span> <span class="hljs-keyword">not</span> <span class="hljs-variable language_">self</span>.animate_played:
            <span class="hljs-variable language_">self</span>.animate_timer += <span class="hljs-variable language_">self</span>.animate_speed <span class="hljs-comment"># 增加动画计时器</span>
            <span class="hljs-keyword">if</span> <span class="hljs-variable language_">self</span>.animate_timer &gt;= <span class="hljs-number">1</span>: <span class="hljs-comment"># 如果计时器达到切换动画的条件</span>
                <span class="hljs-variable language_">self</span>.animate_timer = <span class="hljs-number">0</span> <span class="hljs-comment"># 重置动画计时器</span>
                <span class="hljs-variable language_">self</span>.current_frame_index = (<span class="hljs-variable language_">self</span>.current_frame_index + <span class="hljs-number">1</span>) % <span class="hljs-built_in">len</span>(<span class="hljs-variable language_">self</span>.frames) <span class="hljs-comment"># 切换到下一帧动画</span>
                <span class="hljs-keyword">if</span> <span class="hljs-variable language_">self</span>.current_frame_index == <span class="hljs-built_in">len</span>(<span class="hljs-variable language_">self</span>.frames) - <span class="hljs-number">1</span>: <span class="hljs-comment"># 如果动画已经播放到最后一帧，标记动画已经播放过</span>
                    <span class="hljs-variable language_">self</span>.animate_played = <span class="hljs-literal">True</span>
        <span class="hljs-keyword">elif</span> <span class="hljs-keyword">not</span> <span class="hljs-variable language_">self</span>.has_used:
            <span class="hljs-variable language_">self</span>.current_frame_index = <span class="hljs-number">0</span> <span class="hljs-comment"># 如果没有被使用过，保持在第一帧动画</span>

<span class="hljs-keyword">class</span> <span class="hljs-title class_">Propeller</span>(<span class="hljs-title class_ inherited__">Item</span>):
    probability = <span class="hljs-number">0.1</span> <span class="hljs-comment"># 螺旋桨生成的概率，10%</span>
    animate_speed = <span class="hljs-number">0.1</span> <span class="hljs-comment"># 螺旋桨动画的速度，经验值，表示每帧切换动画的概率</span>
    <span class="hljs-keyword">def</span> <span class="hljs-title function_">__init__</span>(<span class="hljs-params">self, platform</span>):
        <span class="hljs-built_in">super</span>().__init__(platform)
        <span class="hljs-variable language_">self</span>.frames = [Utils.load_img(<span class="hljs-string">f&quot;propeller_<span class="hljs-subst">{i}</span>.png&quot;</span>, (<span class="hljs-number">40</span>, <span class="hljs-number">20</span>)) <span class="hljs-keyword">for</span> i <span class="hljs-keyword">in</span> <span class="hljs-built_in">range</span>(<span class="hljs-number">2</span>)] <span class="hljs-comment"># 加载螺旋桨的两帧动画</span>
        <span class="hljs-variable language_">self</span>.rect = <span class="hljs-variable language_">self</span>.frames[<span class="hljs-number">0</span>].get_rect() <span class="hljs-comment"># 获取螺旋桨图片的矩形区域，用于碰撞检测和位置管理</span>
        <span class="hljs-variable language_">self</span>.sound = Utils.load_sound(<span class="hljs-string">&quot;propeller&quot;</span>) <span class="hljs-comment"># 加载螺旋桨音效</span>
        <span class="hljs-variable language_">self</span>.fly_duration_timer = <span class="hljs-number">150</span> <span class="hljs-comment"># 竹蜻蜓效果持续的帧数（约2.5秒）</span>
        <span class="hljs-variable language_">self</span>.fly_velocity = -<span class="hljs-number">12</span> <span class="hljs-comment"># 竹蜻蜓给予玩家的持续向上的速度，经验值，表示比普通跳跃更高的跳跃效果</span>

    <span class="hljs-keyword">def</span> <span class="hljs-title function_">follow_player</span>(<span class="hljs-params">self, player</span>):
        <span class="hljs-variable language_">self</span>.rect.midbottom = player.rect.midtop <span class="hljs-comment"># 竹蜻蜓效果期间，物品跟随玩家移动，保持在玩家头顶</span>
        <span class="hljs-variable language_">self</span>.rect.centerx += <span class="hljs-number">5</span> <span class="hljs-comment"># 细节微调位置，让它更居中一些</span>
        <span class="hljs-variable language_">self</span>.rect.centery -= <span class="hljs-number">5</span>

    <span class="hljs-keyword">def</span> <span class="hljs-title function_">update</span>(<span class="hljs-params">self</span>):
        <span class="hljs-keyword">if</span> <span class="hljs-keyword">not</span> <span class="hljs-variable language_">self</span>.has_used:
            <span class="hljs-built_in">super</span>().update() <span class="hljs-comment"># 调用父类的 update 方法，保持物品跟随平台移动</span>
        <span class="hljs-keyword">else</span>:
            <span class="hljs-keyword">if</span> <span class="hljs-variable language_">self</span>.has_used <span class="hljs-keyword">and</span> <span class="hljs-variable language_">self</span>.fly_duration_timer &gt; <span class="hljs-number">0</span>:
                <span class="hljs-variable language_">self</span>.fly_duration_timer -= <span class="hljs-number">1</span> <span class="hljs-comment"># 竹蜻蜓效果持续期间，减少计时器</span>
    
<span class="hljs-keyword">class</span> <span class="hljs-title class_">Rocket</span>(<span class="hljs-title class_ inherited__">Item</span>):
    probability = <span class="hljs-number">0.05</span> <span class="hljs-comment"># 火箭生成的概率，5%</span>
    animate_speed = <span class="hljs-number">0.5</span> <span class="hljs-comment"># 火箭动画的速度，经验值，表示每帧切换动画的概率</span>
    <span class="hljs-keyword">def</span> <span class="hljs-title function_">__init__</span>(<span class="hljs-params">self, platform</span>):
        <span class="hljs-built_in">super</span>().__init__(platform)
        <span class="hljs-variable language_">self</span>._init_frames()
        <span class="hljs-variable language_">self</span>.rect = <span class="hljs-variable language_">self</span>.frames[<span class="hljs-number">0</span>].get_rect() <span class="hljs-comment"># 获取火箭图片的矩形区域，用于碰撞检测和位置管理</span>
        <span class="hljs-variable language_">self</span>.sound = Utils.load_sound(<span class="hljs-string">&quot;rocket&quot;</span>) <span class="hljs-comment"># 加载火箭音效</span>
        <span class="hljs-variable language_">self</span>.fly_duration_timer = <span class="hljs-number">200</span> <span class="hljs-comment"># 火箭效果持续的帧数（约3.3秒）</span>
        <span class="hljs-variable language_">self</span>.fly_velocity = -<span class="hljs-number">18</span> <span class="hljs-comment"># 火箭给予玩家的持续向上的速度，经验值，表示比竹蜻蜓更高的跳跃效果</span>

    <span class="hljs-keyword">def</span> <span class="hljs-title function_">_init_frames</span>(<span class="hljs-params">self</span>):
        img_sheet = Utils.load_img(<span class="hljs-string">&quot;rocket.png&quot;</span>, (<span class="hljs-number">160</span>, <span class="hljs-number">240</span>)) <span class="hljs-comment"># 加载火箭图片</span>
        frame_width = <span class="hljs-number">40</span>
        frame_height = <span class="hljs-number">80</span>
        <span class="hljs-comment"># 计算当前帧的位置：(x, y, width, height)</span>
        <span class="hljs-keyword">for</span> i <span class="hljs-keyword">in</span> <span class="hljs-built_in">range</span>(<span class="hljs-number">3</span>):
            <span class="hljs-keyword">for</span> j <span class="hljs-keyword">in</span> <span class="hljs-built_in">range</span>(<span class="hljs-number">4</span>):
                frame = img_sheet.subsurface((j * frame_width, i * frame_height, frame_width, frame_height)) <span class="hljs-comment"># 从图片中提取每一帧</span>
                <span class="hljs-variable language_">self</span>.frames.append(frame) <span class="hljs-comment"># 将每一帧添加到动画帧列表中</span>
        <span class="hljs-variable language_">self</span>.frames.remove(<span class="hljs-variable language_">self</span>.frames[<span class="hljs-built_in">len</span>(<span class="hljs-variable language_">self</span>.frames) - <span class="hljs-number">1</span>]) <span class="hljs-comment"># 移除最后一帧，因为它是空白的</span>
        <span class="hljs-variable language_">self</span>.frames.remove(<span class="hljs-variable language_">self</span>.frames[<span class="hljs-built_in">len</span>(<span class="hljs-variable language_">self</span>.frames) - <span class="hljs-number">1</span>]) <span class="hljs-comment"># 再次移除最后一帧，因为它是空白的</span>

    <span class="hljs-keyword">def</span> <span class="hljs-title function_">follow_player</span>(<span class="hljs-params">self, player</span>):
        <span class="hljs-variable language_">self</span>.rect.y = player.rect.y - <span class="hljs-number">25</span>
        <span class="hljs-comment"># 因为玩家的宽度是20</span>
        <span class="hljs-variable language_">self</span>.rect.x = player.rect.x + <span class="hljs-number">28</span> <span class="hljs-comment"># 细节微调位置</span>

    <span class="hljs-keyword">def</span> <span class="hljs-title function_">update</span>(<span class="hljs-params">self</span>):
        <span class="hljs-keyword">if</span> <span class="hljs-keyword">not</span> <span class="hljs-variable language_">self</span>.has_used:
            <span class="hljs-built_in">super</span>().update() <span class="hljs-comment"># 调用父类的 update 方法，保持物品跟随平台移动</span>
            <span class="hljs-variable language_">self</span>.rect.y += <span class="hljs-number">22</span> <span class="hljs-comment"># 调整图片位置</span>
        <span class="hljs-keyword">else</span>:
            <span class="hljs-keyword">if</span> <span class="hljs-variable language_">self</span>.has_used <span class="hljs-keyword">and</span> <span class="hljs-variable language_">self</span>.fly_duration_timer &gt; <span class="hljs-number">0</span>:
                <span class="hljs-variable language_">self</span>.fly_duration_timer -= <span class="hljs-number">1</span> <span class="hljs-comment"># 火箭效果持续期间，减少计时器</span>

<span class="hljs-keyword">class</span> <span class="hljs-title class_">Cloud</span>():
    <span class="hljs-keyword">def</span> <span class="hljs-title function_">__init__</span>(<span class="hljs-params">self</span>):
        size = random.randint(<span class="hljs-number">50</span>, <span class="hljs-number">100</span>) <span class="hljs-comment"># 云朵的随机大小</span>
        <span class="hljs-variable language_">self</span>.frame = Utils.load_img(<span class="hljs-string">&quot;cloud.png&quot;</span>, (size, size // <span class="hljs-number">2</span>)) <span class="hljs-comment"># 加载云朵图片并缩放到随机大小</span>
        <span class="hljs-variable language_">self</span>.rect = <span class="hljs-variable language_">self</span>.frame.get_rect(
            x=random.randint(<span class="hljs-number">0</span>, SCREEN_WIDTH - size), <span class="hljs-comment"># 云朵的随机水平位置</span>
            y=random.randint(<span class="hljs-number">0</span>, SCREEN_HEIGHT // <span class="hljs-number">2</span>) <span class="hljs-comment"># 云朵的随机垂直位置，限制在屏幕上半部分</span>
        )
        <span class="hljs-variable language_">self</span>.direction = random.choice([-<span class="hljs-number">1</span>, <span class="hljs-number">1</span>]) <span class="hljs-comment"># 云朵的移动方向，-1表示向左，1表示向右</span>
        <span class="hljs-variable language_">self</span>.speed = random.uniform(<span class="hljs-number">0.5</span>, <span class="hljs-number">1.5</span>) <span class="hljs-comment"># 云朵的移动速度，随机生成一个经验值</span>
        <span class="hljs-variable language_">self</span>.alpha = random.randint(<span class="hljs-number">100</span>, <span class="hljs-number">255</span>) <span class="hljs-comment"># 云朵的随机透明度，增加视觉层次感</span>

    <span class="hljs-keyword">def</span> <span class="hljs-title function_">update</span>(<span class="hljs-params">self</span>):
        <span class="hljs-variable language_">self</span>.rect.x += <span class="hljs-variable language_">self</span>.direction * <span class="hljs-variable language_">self</span>.speed <span class="hljs-comment"># 云朵以固定速度向左右移动</span>
        <span class="hljs-keyword">if</span> <span class="hljs-variable language_">self</span>.rect.right &lt; <span class="hljs-number">0</span>: <span class="hljs-comment"># 如果云朵完全移出左边界</span>
            <span class="hljs-variable language_">self</span>.rect.left = SCREEN_WIDTH <span class="hljs-comment"># 从右边重新出现</span>
        <span class="hljs-keyword">elif</span> <span class="hljs-variable language_">self</span>.rect.left &gt; SCREEN_WIDTH: <span class="hljs-comment"># 如果云朵完全移出右边界</span>
            <span class="hljs-variable language_">self</span>.rect.right = <span class="hljs-number">0</span> <span class="hljs-comment"># 从左边重新出现</span>
        
        <span class="hljs-keyword">if</span> <span class="hljs-variable language_">self</span>.rect.top &gt; SCREEN_HEIGHT:
            <span class="hljs-variable language_">self</span>.rect.y = random.randint(<span class="hljs-number">0</span>, SCREEN_HEIGHT // <span class="hljs-number">2</span>) <span class="hljs-comment"># 如果云朵漂浮到下半部分，随机重置到上半部分</span>

    <span class="hljs-keyword">def</span> <span class="hljs-title function_">draw</span>(<span class="hljs-params">self, screen</span>):
        <span class="hljs-comment"># 设置透明度</span>
        temp_surface = <span class="hljs-variable language_">self</span>.frame.copy()
        temp_surface.set_alpha(<span class="hljs-variable language_">self</span>.alpha)
        screen.blit(temp_surface, <span class="hljs-variable language_">self</span>.rect) <span class="hljs-comment"># 绘制云朵图片</span>

<span class="hljs-keyword">class</span> <span class="hljs-title class_">GameSession</span>:
    <span class="hljs-keyword">def</span> <span class="hljs-title function_">__init__</span>(<span class="hljs-params">self</span>):
        pygame.init()
        <span class="hljs-variable language_">self</span>.screen = pygame.display.set_mode((SCREEN_WIDTH, SCREEN_HEIGHT))
        pygame.display.set_caption(<span class="hljs-string">&quot;Doodle Jump By Guanngxu&quot;</span>)
        <span class="hljs-variable language_">self</span>.clock = pygame.time.Clock()
        <span class="hljs-variable language_">self</span>.bg = Utils.load_img(<span class="hljs-string">&quot;background.png&quot;</span>, (SCREEN_WIDTH, SCREEN_HEIGHT)) <span class="hljs-comment"># 加载背景图片</span>
        <span class="hljs-variable language_">self</span>.player = Player() <span class="hljs-comment"># 创建玩家实例</span>
        <span class="hljs-variable language_">self</span>.platforms = [] <span class="hljs-comment"># 初始化平台列表</span>
        <span class="hljs-variable language_">self</span>.clouds = [Cloud() <span class="hljs-keyword">for</span> _ <span class="hljs-keyword">in</span> <span class="hljs-built_in">range</span>(<span class="hljs-number">5</span>)] <span class="hljs-comment"># 初始化云朵列表，创建5朵云</span>
        <span class="hljs-variable language_">self</span>.score = <span class="hljs-number">0</span> <span class="hljs-comment"># 初始化分数</span>
        <span class="hljs-variable language_">self</span>.score_font = pygame.font.SysFont(<span class="hljs-string">&quot;Arial&quot;</span>, <span class="hljs-number">24</span>, bold=<span class="hljs-literal">True</span>) <span class="hljs-comment"># 初始化字体对象，用于绘制分数</span>
        <span class="hljs-variable language_">self</span>.title_font = pygame.font.SysFont(<span class="hljs-string">&quot;Comic Sans MS&quot;</span>, <span class="hljs-number">55</span>, bold=<span class="hljs-literal">True</span>)
        <span class="hljs-variable language_">self</span>.author_font = pygame.font.SysFont(<span class="hljs-string">&quot;Arial&quot;</span>, <span class="hljs-number">22</span>, italic=<span class="hljs-literal">True</span>)
        <span class="hljs-variable language_">self</span>.start_font = pygame.font.SysFont(<span class="hljs-string">&quot;Arial&quot;</span>, <span class="hljs-number">26</span>, bold=<span class="hljs-literal">True</span>)
        <span class="hljs-variable language_">self</span>.state = <span class="hljs-string">&quot;menu&quot;</span> <span class="hljs-comment"># 游戏状态，初始为菜单界面</span>
        <span class="hljs-variable language_">self</span>._init_platforms() <span class="hljs-comment"># 初始化平台列表</span>

    <span class="hljs-keyword">def</span> <span class="hljs-title function_">_init_platforms</span>(<span class="hljs-params">self</span>):
        <span class="hljs-comment"># 在屏幕底部中央创建一个初始平台，确保玩家有地方跳</span>
        <span class="hljs-comment"># platform 的 width 为 70，所以 x 坐标需要减去 35 来居中</span>
        platform = Platform(SCREEN_WIDTH // <span class="hljs-number">2</span> - <span class="hljs-number">35</span>, SCREEN_HEIGHT - <span class="hljs-number">50</span>)
        <span class="hljs-variable language_">self</span>.platforms.append(platform)
        <span class="hljs-comment"># 初始化一些平台，确保玩家有地方跳</span>
        <span class="hljs-comment"># 初始化的平台没有道具</span>
        <span class="hljs-keyword">for</span> i <span class="hljs-keyword">in</span> <span class="hljs-built_in">range</span>(<span class="hljs-number">8</span>):
            platform = Platform(random.randint(<span class="hljs-number">0</span>, SCREEN_WIDTH - <span class="hljs-number">70</span>), SCREEN_HEIGHT - (i * <span class="hljs-number">80</span>) - <span class="hljs-number">150</span>)
            <span class="hljs-variable language_">self</span>.platforms.append(platform)

    <span class="hljs-keyword">def</span> <span class="hljs-title function_">update_scroll</span>(<span class="hljs-params">self</span>):
        scroll_threshold = SCREEN_HEIGHT // <span class="hljs-number">2</span> <span class="hljs-comment"># 定义一个滚动阈值，当玩家超过这个高度时，平台开始向下滚动</span>
        scroll_amount = <span class="hljs-number">0</span> <span class="hljs-comment"># 初始化滚动量</span>
        <span class="hljs-keyword">if</span> <span class="hljs-variable language_">self</span>.player.rect.top &lt; scroll_threshold:
            scroll_amount = scroll_threshold - <span class="hljs-variable language_">self</span>.player.rect.top <span class="hljs-comment"># 计算需要滚动的距离</span>
            <span class="hljs-variable language_">self</span>.player.rect.top = scroll_threshold <span class="hljs-comment"># 将玩家位置固定在滚动阈值上</span>
            <span class="hljs-keyword">for</span> platform <span class="hljs-keyword">in</span> <span class="hljs-variable language_">self</span>.platforms:
                platform.rect.y += scroll_amount <span class="hljs-comment"># 平台向下滚动</span>

        <span class="hljs-comment"># 平台向下滚动后，移除那些已经完全移出屏幕底部的平台，并在顶部生成新的平台</span>
        <span class="hljs-variable language_">self</span>.platforms = [p <span class="hljs-keyword">for</span> p <span class="hljs-keyword">in</span> <span class="hljs-variable language_">self</span>.platforms <span class="hljs-keyword">if</span> p.rect.top &lt; SCREEN_HEIGHT]
        <span class="hljs-variable language_">self</span>.score += scroll_amount // <span class="hljs-number">10</span> <span class="hljs-comment"># 根据滚动距离增加分数，10 是一个经验值，表示每滚动10像素得1分</span>
        <span class="hljs-keyword">while</span> <span class="hljs-built_in">len</span>(<span class="hljs-variable language_">self</span>.platforms) &lt; <span class="hljs-number">8</span>: <span class="hljs-comment"># 保持屏幕上至少有8个平台</span>
            new_platform = Platform(random.randint(<span class="hljs-number">0</span>, SCREEN_WIDTH - <span class="hljs-number">70</span>), random.randint(-<span class="hljs-number">100</span>, -<span class="hljs-number">40</span>))
            <span class="hljs-variable language_">self</span>.generate_item(new_platform)
            <span class="hljs-variable language_">self</span>.platforms.append(new_platform)

        <span class="hljs-keyword">for</span> cloud <span class="hljs-keyword">in</span> <span class="hljs-variable language_">self</span>.clouds:
            cloud.rect.y += scroll_amount // <span class="hljs-number">2</span> <span class="hljs-comment"># 云朵以较慢的速度向下滚动</span>

    <span class="hljs-keyword">def</span> <span class="hljs-title function_">generate_item</span>(<span class="hljs-params">self, platform</span>):
        <span class="hljs-keyword">if</span> Utils.hit_probability(Spring.probability): <span class="hljs-comment"># 根据弹簧的生成概率决定是否在平台上生成弹簧</span>
            spring = Spring(platform)
            platform.item = spring <span class="hljs-comment"># 将弹簧作为平台的一个属性，方便后续碰撞检测和更新</span>
        <span class="hljs-keyword">elif</span> Utils.hit_probability(Propeller.probability): <span class="hljs-comment"># 根据螺旋桨的生成概率决定是否在平台上生成螺旋桨</span>
            propeller = Propeller(platform)
            platform.item = propeller <span class="hljs-comment"># 将螺旋桨作为平台的一个属性，方便后续碰撞检测和更新</span>
        <span class="hljs-keyword">elif</span> Utils.hit_probability(Rocket.probability): <span class="hljs-comment"># 根据火箭的生成概率决定是否在新平台上生成火箭</span>
            rocket = Rocket(platform)
            platform.item = rocket <span class="hljs-comment"># 将火箭作为平台的一个属性，方便后续碰撞检测和更新</span>

    <span class="hljs-comment"># 检测玩家与道具的碰撞，并处理道具效果</span>
    <span class="hljs-keyword">def</span> <span class="hljs-title function_">item_colliderect</span>(<span class="hljs-params">self, item</span>):
        <span class="hljs-comment"># 检测玩家是否与道具发生碰撞，并且道具没有被使用过</span>
        <span class="hljs-keyword">if</span> <span class="hljs-variable language_">self</span>.player.rect.colliderect(item.rect) <span class="hljs-keyword">and</span> <span class="hljs-keyword">not</span> item.has_used:
            item.apply_effect(<span class="hljs-variable language_">self</span>.player) <span class="hljs-comment"># 调用 apply_effect 方法</span>

    <span class="hljs-comment"># 检测玩家与平台的碰撞，并处理跳跃逻辑</span>
    <span class="hljs-keyword">def</span> <span class="hljs-title function_">platform_colliderect</span>(<span class="hljs-params">self, platform</span>):
        <span class="hljs-comment"># 检测玩家是否与平台发生碰撞，并且玩家正在向下移动时检测，向上移动时不检测</span>
        <span class="hljs-keyword">if</span> <span class="hljs-variable language_">self</span>.player.rect.colliderect(platform.rect) <span class="hljs-keyword">and</span> <span class="hljs-variable language_">self</span>.player.speed_y &gt; <span class="hljs-number">0</span>:
            <span class="hljs-comment"># 15 是一个经验值，表示玩家底部与平台顶部的碰撞距离，如果小于这个值才算真正的站在平台上，避免侧面碰撞误判</span>
            <span class="hljs-keyword">if</span> <span class="hljs-variable language_">self</span>.player.rect.bottom - platform.rect.top &lt; <span class="hljs-number">15</span>: <span class="hljs-comment"># 碰撞时只检测玩家底部与平台顶部的碰撞，避免侧面碰撞误判</span>
                <span class="hljs-variable language_">self</span>.player.rect.bottom = platform.rect.top <span class="hljs-comment"># 碰撞后将玩家的底部位置调整到平台的顶部，避免玩家穿过平台</span>
                <span class="hljs-variable language_">self</span>.player.speed_y = <span class="hljs-number">0</span> <span class="hljs-comment"># 碰撞后将玩家的垂直速度重置为0，模拟站在平台上的效果</span>
                keys = pygame.key.get_pressed()
                <span class="hljs-keyword">if</span> keys[pygame.K_SPACE]: <span class="hljs-comment"># 只有按空格才跳跃</span>
                    <span class="hljs-variable language_">self</span>.player.jump() <span class="hljs-comment"># 调用玩家的 jump 方法，执行跳跃逻辑</span>
                <span class="hljs-keyword">return</span> <span class="hljs-literal">True</span> <span class="hljs-comment"># 碰撞后返回 True，表示玩家成功站在平台上</span>

    <span class="hljs-keyword">def</span> <span class="hljs-title function_">reset_game</span>(<span class="hljs-params">self</span>):
        <span class="hljs-comment"># 重新初始化游戏内的对象数据</span>
        <span class="hljs-variable language_">self</span>.player = Player() 
        <span class="hljs-variable language_">self</span>.platforms = [] 
        <span class="hljs-variable language_">self</span>.clouds = [Cloud() <span class="hljs-keyword">for</span> _ <span class="hljs-keyword">in</span> <span class="hljs-built_in">range</span>(<span class="hljs-number">5</span>)] 
        <span class="hljs-variable language_">self</span>.score = <span class="hljs-number">0</span>
        <span class="hljs-variable language_">self</span>.state = <span class="hljs-string">&quot;menu&quot;</span> <span class="hljs-comment"># 回到菜单界面</span>
        <span class="hljs-variable language_">self</span>._init_platforms() 

    <span class="hljs-keyword">def</span> <span class="hljs-title function_">update</span>(<span class="hljs-params">self</span>): <span class="hljs-comment"># 更新游戏数据</span>
        <span class="hljs-variable language_">self</span>.update_scroll() <span class="hljs-comment"># 更新滚动逻辑</span>

        <span class="hljs-keyword">for</span> cloud <span class="hljs-keyword">in</span> <span class="hljs-variable language_">self</span>.clouds:
            cloud.update() <span class="hljs-comment"># 更新云朵状态</span>

        <span class="hljs-keyword">for</span> platform <span class="hljs-keyword">in</span> <span class="hljs-variable language_">self</span>.platforms:
            platform.update() <span class="hljs-comment"># 更新平台状态</span>
        <span class="hljs-variable language_">self</span>.player.update() <span class="hljs-comment"># 更新玩家状态</span>

        <span class="hljs-keyword">if</span> <span class="hljs-variable language_">self</span>.player.speed_y &gt;= <span class="hljs-number">0</span>: <span class="hljs-comment"># 只有当玩家正在向下移动时才检测碰撞，向上移动时不检测</span>
            <span class="hljs-comment"># 检测玩家与平台的碰撞，并处理跳跃逻辑，同时检测玩家与道具的碰撞，并处理道具效果</span>
            <span class="hljs-keyword">for</span> platform <span class="hljs-keyword">in</span> <span class="hljs-variable language_">self</span>.platforms:
                <span class="hljs-keyword">if</span> platform.item: <span class="hljs-comment"># 如果平台上有道具，检测玩家与道具的碰撞，并处理道具效果</span>
                    <span class="hljs-variable language_">self</span>.item_colliderect(platform.item)
                <span class="hljs-keyword">if</span> <span class="hljs-variable language_">self</span>.platform_colliderect(platform): <span class="hljs-comment"># 检测玩家与平台的碰撞，并处理跳跃逻辑</span>
                    <span class="hljs-keyword">break</span> <span class="hljs-comment"># 如果已经检测到玩家与一个平台发生碰撞并处理了跳跃逻辑，就不再继续检测其他平台，避免多重碰撞导致的跳跃问题</span>
        
        <span class="hljs-keyword">if</span> <span class="hljs-variable language_">self</span>.player.rect.top &gt; SCREEN_HEIGHT: <span class="hljs-comment"># 如果玩家掉出屏幕底部，游戏结束，重置游戏状态</span>
            <span class="hljs-variable language_">self</span>.reset_game() <span class="hljs-comment"># 重新初始化游戏状态，回到菜单界面</span>

    <span class="hljs-keyword">def</span> <span class="hljs-title function_">draw</span>(<span class="hljs-params">self</span>): <span class="hljs-comment"># 绘制游戏画面</span>
        <span class="hljs-variable language_">self</span>.screen.blit(<span class="hljs-variable language_">self</span>.bg, (<span class="hljs-number">0</span>, <span class="hljs-number">0</span>)) <span class="hljs-comment"># 绘制背景图片</span>
        <span class="hljs-keyword">for</span> cloud <span class="hljs-keyword">in</span> <span class="hljs-variable language_">self</span>.clouds:
            cloud.draw(<span class="hljs-variable language_">self</span>.screen) <span class="hljs-comment"># 绘制云朵</span>
        <span class="hljs-keyword">for</span> platform <span class="hljs-keyword">in</span> <span class="hljs-variable language_">self</span>.platforms:
            platform.draw(<span class="hljs-variable language_">self</span>.screen) <span class="hljs-comment"># 绘制平台</span>
        <span class="hljs-variable language_">self</span>.player.draw(<span class="hljs-variable language_">self</span>.screen) <span class="hljs-comment"># 绘制玩家</span>
        scrore_text = <span class="hljs-variable language_">self</span>.score_font.render(<span class="hljs-string">f&quot;Score: <span class="hljs-subst">{self.score}</span>&quot;</span>, <span class="hljs-literal">True</span>, BLACK)
        <span class="hljs-variable language_">self</span>.screen.blit(scrore_text, (<span class="hljs-number">10</span>, <span class="hljs-number">10</span>)) <span class="hljs-comment"># 在屏幕左</span>

    <span class="hljs-keyword">def</span> <span class="hljs-title function_">draw_menu</span>(<span class="hljs-params">self</span>):
        <span class="hljs-variable language_">self</span>.screen.blit(<span class="hljs-variable language_">self</span>.bg, (<span class="hljs-number">0</span>, <span class="hljs-number">0</span>)) <span class="hljs-comment"># 绘制背景图片</span>
        <span class="hljs-comment"># 渲染标题</span>
        title_surf = <span class="hljs-variable language_">self</span>.title_font.render(<span class="hljs-string">&quot;Doodle Jump&quot;</span>, <span class="hljs-literal">True</span>, TITLE_COLOR)
        <span class="hljs-variable language_">self</span>.screen.blit(title_surf, (SCREEN_WIDTH//<span class="hljs-number">2</span> - title_surf.get_width()//<span class="hljs-number">2</span>, <span class="hljs-number">120</span>))
        <span class="hljs-comment"># 渲染作者信息</span>
        author_surf = <span class="hljs-variable language_">self</span>.author_font.render(<span class="hljs-string">&quot;Author: Guanngxu&quot;</span>, <span class="hljs-literal">True</span>, BLACK)
        <span class="hljs-variable language_">self</span>.screen.blit(author_surf, (SCREEN_WIDTH//<span class="hljs-number">2</span> - author_surf.get_width()//<span class="hljs-number">2</span>, <span class="hljs-number">210</span>))
        <span class="hljs-comment"># 渲染提示文字</span>
        msg = <span class="hljs-variable language_">self</span>.start_font.render(<span class="hljs-string">&quot;Press [ SPACE ] to Start&quot;</span>, <span class="hljs-literal">True</span>, (<span class="hljs-number">50</span>, <span class="hljs-number">50</span>, <span class="hljs-number">50</span>))
        <span class="hljs-variable language_">self</span>.screen.blit(msg, (SCREEN_WIDTH//<span class="hljs-number">2</span> - msg.get_width()//<span class="hljs-number">2</span>, <span class="hljs-number">380</span>))
        pygame.display.flip()

    <span class="hljs-keyword">def</span> <span class="hljs-title function_">run</span>(<span class="hljs-params">self</span>):
        <span class="hljs-keyword">while</span> <span class="hljs-literal">True</span>:
            <span class="hljs-keyword">for</span> event <span class="hljs-keyword">in</span> pygame.event.get():
                <span class="hljs-keyword">if</span> event.<span class="hljs-built_in">type</span> == pygame.QUIT:
                    pygame.quit()
                    sys.exit()
            
            <span class="hljs-keyword">if</span> <span class="hljs-variable language_">self</span>.state == <span class="hljs-string">&quot;menu&quot;</span>:
                <span class="hljs-variable language_">self</span>.draw_menu()
                keys = pygame.key.get_pressed()
                <span class="hljs-keyword">if</span> keys[pygame.K_SPACE]: <span class="hljs-comment"># 在菜单界面按空格开始游戏</span>
                    <span class="hljs-variable language_">self</span>.state = <span class="hljs-string">&quot;playing&quot;</span>
            <span class="hljs-keyword">elif</span> <span class="hljs-variable language_">self</span>.state == <span class="hljs-string">&quot;playing&quot;</span>:
                <span class="hljs-variable language_">self</span>.update()
                <span class="hljs-variable language_">self</span>.draw()

            <span class="hljs-variable language_">self</span>.clock.tick(FPS)  <span class="hljs-comment"># 控制游戏循环以每秒FPS帧的速度运行</span>
            pygame.display.flip()  <span class="hljs-comment"># 更新屏幕显示</span>


<span class="hljs-keyword">if</span> __name__ == <span class="hljs-string">&quot;__main__&quot;</span>:
    GameSession().run()
</code></pre>
]]></content>
        <author>
            <name>Guanngxu</name>
        </author>
    </entry>
    <entry>
        <title type="html"><![CDATA[一文搞懂 TCP 协议核心机制]]></title>
        <id>https://www.guanngxu.com/bXKpR9unB5/</id>
        <link href="https://www.guanngxu.com/bXKpR9unB5/"/>
        <updated>2026-01-12T10:16:16.000Z</updated>
        <summary type="html"><![CDATA[
参考内容：
Transmission Control Protocol (TCP)
TCP - 12 simple ideas to explain the Transmission Control Protocol
TCP (Trans...]]></summary>
        <content type="html"><![CDATA[<blockquote>
<p>参考内容：</p>
<p><a href="https://datatracker.ietf.org/doc/html/rfc9293">Transmission Control Protocol (TCP)</a></p>
<p><a href="https://www.youtube.com/watch?v=JFch3ctY6nE">TCP - 12 simple ideas to explain the Transmission Control Protocol</a></p>
<p><a href="https://www.cloudns.net/blog/tcp-transmission-control-protocol-what-is-it-and-how-does-it-work/">TCP (Transmission Control Protocol) – What is it, and how does it work? </a></p>
<p><a href="https://www.codequoi.com/en/internet-layered-network-architecture/">The Internet's Layered Network Architecture</a></p>
<p>《计算机网络-自顶向下方法（第7版）》</p>
</blockquote>
<p>TCP (Transmission Control Protocol - 传输控制协议) 在过去 40 多年的时间里，一直是互联网通信的核心。在大学学习计算机网络时，大部分同学都只是简单机械的背诵 TCP 是面向连接的，是可靠的传输协议。本文借助 <a href="https://www.wireshark.org/">Wireshark</a> 抓包工具分析 TCP 报文头部，来详细的介绍它的工作原理。</p>
<p>TCP 报文格式如下图所示，我们将重点关注 <code>Sequence Number</code>、<code>Acknowledgment Number</code>、<code>Window</code> 几个字段，以及 <code>TCP Flags</code> 中的 <code>ACK</code>、<code>PSH</code>、<code>RST</code>、<code>SYN</code>、<code>FIN</code>。后续的内容除了建立连接过程外，其它均与服务端客户端无关，即任何一方均可发起相关操作。</p>
<figure data-type="image" tabindex="1"><img src="https://www.guanngxu.com/post-images/1768581870083.png" alt=""></figure>
<h3 id="建立连接">建立连接</h3><p>在 TCP 建立连接时进行抓包，可以发现客户端首先向服务端发送了 <code>SYN</code> 包，服务端向客户端响应了 <code>SYN, ACK</code> 包，最后客户端向服务端发送了 <code>SYN</code> 包，这就是大名鼎鼎的<strong>三次握手</strong>。</p>
<figure data-type="image" tabindex="2"><img src="https://www.guanngxu.com/post-images/1768622290335.png" alt=""></figure>
<p>TCP 建立连接的三次握手过程，有四个事件发生：</p>
<ul>
<li>客户端→服务端：<strong>SYN</strong>chronize（同步）我的初始序列号 <code>X</code>；</li>
<li>服务端→客户端：我收到了你的 SYN，我 <strong>ACK</strong>nowledge（确认）我已经准备好接收 <code>[X+1]</code>；</li>
<li>服务端→客户端：<strong>SYN</strong>chronize（同步）我的初始序列号 <code>Y</code>；</li>
<li>客户端→服务端：我收到了你的 SYN，我 <strong>ACK</strong>nowledge（确认）我已经准备好接收 <code>[Y+1]</code>；</li>
</ul>
<figure data-type="image" tabindex="3"><img src="https://www.guanngxu.com/post-images/1768636172034.png" alt=""></figure>
<h4 id="为什么是三次握手而不是两次握手">为什么是三次握手而不是两次握手？</h4><p>我们用打电话的场景来类比两次握手和三次握手的区别，可以发现在场景一中，B 以为通话已经建立，但其实可能 A 没有听到 B 的回复，而在场景二中双方都确认对方能够听到自己的声音，所以三次握手要更加可靠。</p>
<pre class="hljs"><code>场景一：两次握手
A：喂，听得到吗？（SYN）
B：听得到，你听得到吗？（ACK）

场景二：三次握手
A：喂，听得到吗？（SYN）
B：听得到，你听得到吗？（SYN + ACK）
A：我也听得到！（ACK）
</code></pre>
<h3 id="发送数据">发送数据</h3><p>TCP 连接建立后就可以发送数据了，数据发送过程主要关注  <code>Sequence Number</code>、<code>Acknowledgment Number</code>、<code>Window</code> 几个字段。</p>
<figure data-type="image" tabindex="4"><img src="https://www.guanngxu.com/post-images/1768668203213.png" alt=""></figure>
<p><code>Sequence Number</code> 用于跟踪已经发送的数据，<code>Acknowledgment Number</code> 则用于跟踪已经接收的数据。比如客户端本次发送的数据包为 <code>seq=1001</code> 且数据长度为 200 字节，当服务端接收到这 200 个字节后，将回复 <code>ack=1201</code> 表示已经成功接收数据，下一次客户端发送数据的序列号将为 <code>1201</code>。</p>
<figure data-type="image" tabindex="5"><img src="https://www.guanngxu.com/post-images/1768669790773.png" alt=""></figure>
<p>如果接收方为接收到的每段报文都发送确认信息，那就意味着在线路上传输的报文数量翻了一倍，为了减轻数据传输的负担，TCP 加入了<a href="https://zh.wikipedia.org/wiki/TCP%E5%BB%B6%E8%BF%9F%E7%A1%AE%E8%AE%A4">延迟确认机制</a>。延迟确认机制规定每收到两段报文，或者距离最后一段报文的时间不超过 500ms，至少要回应一次 ACK。</p>
<figure data-type="image" tabindex="6"><img src="https://www.guanngxu.com/post-images/1768752861220.png" alt=""></figure>
<h4 id="超时重传">超时重传</h4><p>TCP 是如何处理丢包情况的呢？实际上每次数据发送时，客户端都会保留一份已经发送的数据副本到缓存，并启动重传超时。如果客户端收到服务端发送的数据包确认，则将缓存中的数据删除即可，因为可以确认服务端已经正确接收了数据包。</p>
<p>如果客户端一直没有收到已发送数据包的确认，重传超时时间一定会在某个时刻到期，此时客户端即会意识到服务端可能没有接收到数据，所以客户端会重新发送丢失的数据包，确保数据可以被服务端接收到。</p>
<figure data-type="image" tabindex="7"><img src="https://www.guanngxu.com/post-images/1768671155947.png" alt=""></figure>
<p>超时重传机制的巧妙之处在于不管在哪个方向丢包，它都能正常工作。比如客户端正常发送了数据包，但是服务端的 ACK 包丢失了，当达到重传超时时间后，客户端会再次发送对应数据包，而对服务端来说即可猜出是 ACK 数据包丢了，服务端不会重复存储该数据包，只需要再次回应对应的 <code>ack</code> 即可。</p>
<figure data-type="image" tabindex="8"><img src="https://www.guanngxu.com/post-images/1768672238333.png" alt=""></figure>
<h4 id="流量控制">流量控制</h4><p>如果发送方一味的发送数据，而接收方可能正忙于其它事务，可能会读取数据相对缓慢，如此就会很容易地使接收缓存溢出。TCP 为应用程序提供了<strong>流量控制</strong>服务以消除发送方使接收方缓存溢出的可能性。流量控制因此是一个速度匹配服务，即发送方的发送速率与接收方应用程序的读取速率相匹配。</p>
<figure data-type="image" tabindex="9"><img src="https://www.guanngxu.com/post-images/1768893394977.png" alt=""></figure>
<p>从上图可以看出 TCP 通过 <code>Window</code> 字段来进行流量控制，通过该字段告知发送方在必须等待接收确认之前可以发送多少数据。即通过控制窗口大小来达到流量控制的目的。</p>
<h4 id="数据包解析">数据包解析</h4><p>将 Wireshark 抓取的 TCP 数据包原始字节数据复制出来如下所示，会发现里面不止有 TCP 报文的头部，还有以太网和 IPv4 的头部。</p>
<pre class="hljs"><code>0000   74 5a 01 b5 29 e4 90 65 84 0b 69 8b 08 00 45 00
0010   00 32 1b bf 40 00 80 06 00 00 c0 a8 00 0a 08 9c
0020   53 29 6a b8 1a 0a 06 6e 20 43 47 9e 92 37 50 18
0030   00 ff 1c 9c 00 00 31 32 33 34 35 36 37 38 39 30



数据链路层：以太网头部（14 Bytes）
74 5a 01 b5 29 e4 90 65 84 0b 69 8b 08 00

目标 MAC：74:5a:01:b5:29:e4
源 MAC：90:65:84:0b:69:8b
类型：08 00 = IPv4



网络层：IPv4 头部 (20 Bytes)
08 00 45 00 00 32 1b bf 40 00 80 06 00 00 c0 a8 00 0a 08 9c 53 29

版本 + IHL：45 = IPv4，头长 20 字节
差分服务字段 (DSCP/ECN)，通常为 0：00
总长度：00 32 = 50 字节（IP 头 20 + TCP 头 20 + 数据 10 字节）
标识符：1b bf，用于数据包分片重组
标志 + 分片偏移：40 00 = DF (表示不分片)
生存时间 TTL：80 = 128，表示数据包最多能经过 128 跳
协议：06 = TCP，即上层协议是 TCP
头部校验和：00 00，这里显示为 0，可能是抓包时未计算，或者故意置零
源 IP：c0 a8 00 0a = 192.168.0.10
目标 IP：08 9c 53 29 = 8.156.83.41



传输层：TCP 头部 (20 Bytes)
6a b8 1a 0a 06 6e 20 43 47 9e 92 37 50 18 00 ff 1c 9c 00 00

源端口：6a b8 = 27320
目标端口：1a 0a = 6666
序列号：06 6e 20 43 = 107,661,379， wireshark 显示的是相对序列号，所以不一样
确认号：47 9e 92 37 = 1,201,353,271，与序列号同理
数据偏移 (4位) + 保留 (3位) + 标志 (9位) (2字节):  50 18 -&gt; 分解：
    数据偏移: 5 -&gt; TCP 头部长度 5 个“32位字” (5 * 4 = 20 字节)。
    标志位: 0x018（二进制 0000 0001 1000）：
        ACK = 1 (确认有效)
        PSH = 1 (推送数据，提示接收端应立即交给上层应用)
        RST, SYN, FIN = 0
窗口大小：00 ff = 65535
校验和：1c 9c
紧急指针：00 00 = 0，未使用



应用层：TCP 载荷数据（10 Bytes）
31 32 33 34 35 36 37 38 39 30

都是 ASCII 码，数据内容为：&quot;1234567890&quot;
</code></pre>
<figure data-type="image" tabindex="10"><img src="https://www.guanngxu.com/post-images/1768753783533.png" alt=""></figure>
<p>从数据包的封装情况即可看出来对网络协议分层的好处，比如工作在链路层的交换机只需要解析出 MAC 地址即可进行数据转发，而不需要再解析网络层中的 IP 地址等信息。同理对工作在网络层的路由器来说，解析出 IP 地址即可转发数据，不需要对数据进行深度解析。</p>
<h3 id="断开连接">断开连接</h3><p>TCP 有两种关闭连接的方式，一种是通过 <code>FIN</code> 来优雅的关闭连接，另一种是通过 <code>RST</code> 来暴力的关闭连接。我们首先来看优雅的关闭方式。</p>
<p>如果一切都进展的很顺利，客户端和服务端建立连接并成功的交换了彼此的数据，它们就可以关闭连接并继续进行各自的工作。与建立连接的过程类似，关闭连接的过程也会发生四个事件，即大家所说的<strong>四次挥手</strong>。</p>
<figure data-type="image" tabindex="11"><img src="https://www.guanngxu.com/post-images/1768894332006.png" alt=""></figure>
<ul>
<li>客户端→服务端：我已经<strong>FIN</strong>ished（完成）了数据发送，我最后的序列号是 <code>X</code>；</li>
<li>服务端→客户端：我 <strong>ACK</strong>nowledge（确认）接收到了你的 FIN 且 <code>ack=[X+1]</code>；</li>
<li>服务端→客户端：我已经<strong>FIN</strong>ished（完成）了数据发送，我最后的序列号是 <code>Y</code>；</li>
<li>客户端→服务端：我 <strong>ACK</strong>nowledge（确认）接收到了你的 FIN 且 <code>ack=[Y+1]</code>；</li>
</ul>
<p>和三次握手一样，中间两个事件可能在同一个数据包中发生，比如下面使用 Wireshark 工具抓到的包。</p>
<figure data-type="image" tabindex="12"><img src="https://www.guanngxu.com/post-images/1768895827506.png" alt=""></figure>
<p>实际情况不会每次都将两个事件合并，这是因为四个事件并不是严格要求必须按照上述顺序进行的，很有可能客户端已经发送完数据并断开连接，但是服务端仍然有数据需要向客户端发送，所以可以只关闭客户端向服务端发送数据的通道，而不关闭服务端向客户端发送数据的通道。请记住 TCP 是全双工通信，一个 FIN-ACK 组合事件只会关闭一个方向的连接。</p>
<figure data-type="image" tabindex="13"><img src="https://www.guanngxu.com/post-images/1768897204753.png" alt=""></figure>
<p>非优雅的暴力关闭连接方式采用 <code>RST</code> Flag 实现，任何一方都可以发送该连接关闭数据包，使用该方式关闭连接意味着 TCP 连接出现了问题，发送方向接收方发送了 <code>RST</code> 数据包后，即将该 TCP 连接相关信息清空，接收方接收到该数据包后也即将该 TCP 连接相关信息清空。</p>
]]></content>
        <author>
            <name>Guanngxu</name>
        </author>
    </entry>
    <entry>
        <title type="html"><![CDATA[Python 使用 pygame 实现人机对战五子棋]]></title>
        <id>https://www.guanngxu.com/4MWnuo8ufo/</id>
        <link href="https://www.guanngxu.com/4MWnuo8ufo/"/>
        <updated>2026-01-04T01:35:57.000Z</updated>
        <summary type="html"><![CDATA[五子棋，是一种两人对弈的纯策略型棋类游戏，通常双方分别使用黑白两色的棋子，轮流下在棋盘直线与横线的交叉点上，先在横线、直线或斜对角线上形成 5 子连线者获胜。因为棋子在落子后不能移动或拿掉，所以也可以用纸和笔来进行游戏。
下面我们使用 py...]]></summary>
        <content type="html"><![CDATA[<p><a href="https://zh.wikipedia.org/wiki/%E4%BA%94%E5%AD%90%E6%A3%8B">五子棋</a>，是一种两人对弈的纯策略型棋类游戏，通常双方分别使用黑白两色的棋子，轮流下在棋盘直线与横线的交叉点上，先在横线、直线或斜对角线上形成 5 子连线者获胜。因为棋子在落子后不能移动或拿掉，所以也可以用纸和笔来进行游戏。</p>
<p>下面我们使用 <a href="https://www.pygame.org/docs/">pygame</a> 来实现一个简约的人机对战版五子棋。考虑到整个程序包括 UI、五子棋规则、AI 智能三个部分，我们就简单使用三个 class 来实现各自具体的功能，大致就是下面这个样子。</p>
<pre class="hljs"><code><span class="hljs-keyword">class</span> <span class="hljs-title class_">GomokuGame</span>:
    <span class="hljs-string">&quot;&quot;&quot;游戏主类，负责游戏流程控制和界面显示&quot;&quot;&quot;</span>
    <span class="hljs-keyword">pass</span>

<span class="hljs-keyword">class</span> <span class="hljs-title class_">Judge</span>:
    <span class="hljs-string">&quot;&quot;&quot;裁判类，负责管理棋盘状态和判断胜负&quot;&quot;&quot;</span>
    <span class="hljs-keyword">pass</span>

<span class="hljs-keyword">class</span> <span class="hljs-title class_">AI</span>:
    <span class="hljs-string">&quot;&quot;&quot;人工智能类，负责AI的下棋逻辑&quot;&quot;&quot;</span>
    <span class="hljs-keyword">pass</span>
</code></pre>
<p>我们首先来完善 <strong>GomokuGame</strong> 类的代码，棋局的显示与更新、黑白棋落子都需要它来搞定。先准备五子棋盘，五子棋的标准棋盘大小是 15x15，即有 15 条横线和 15 条竖线，共有 225 个交叉点可供落子，所以使用 <code>pygame.draw.line</code> 画 15 条竖线、15 条横线即可。</p>
<p>这里特别提醒不要忘了棋盘上面还有五个星位点需要突出显示。考虑到 UI 的显示美观问题，我们横线和竖线与窗口边框还需要预留一定的边距，具体实现如下：</p>
<pre class="hljs"><code><span class="hljs-keyword">import</span> pygame
<span class="hljs-keyword">import</span> sys


WINDOW_SIZE = <span class="hljs-number">736</span> <span class="hljs-comment"># 窗口大小（像素）</span>
BOARD_SIZE = <span class="hljs-number">16</span> <span class="hljs-comment"># 棋盘大小（16x16，包含边框）</span>
GAP = WINDOW_SIZE // BOARD_SIZE <span class="hljs-comment"># 网格间距（每个格子的大小）</span>

<span class="hljs-comment"># 棋盘上的五个星位点（传统五子棋的标准位置）</span>
<span class="hljs-comment"># 坐标从0开始，对应棋盘上的交叉点</span>
POINTS = [(<span class="hljs-number">2</span>, <span class="hljs-number">2</span>), (<span class="hljs-number">2</span>, <span class="hljs-number">12</span>), (<span class="hljs-number">7</span>, <span class="hljs-number">7</span>), (<span class="hljs-number">12</span>, <span class="hljs-number">2</span>), (<span class="hljs-number">12</span>, <span class="hljs-number">12</span>)]

<span class="hljs-comment"># 颜色定义（使用RGB格式）</span>
COLORS = {
    <span class="hljs-string">&quot;background&quot;</span>: (<span class="hljs-number">240</span>, <span class="hljs-number">217</span>, <span class="hljs-number">181</span>),  <span class="hljs-comment"># 背景色（米黄色）</span>
    <span class="hljs-string">&quot;line&quot;</span>: (<span class="hljs-number">0</span>, <span class="hljs-number">0</span>, <span class="hljs-number">0</span>),              <span class="hljs-comment"># 棋盘线颜色（黑色）</span>
    <span class="hljs-string">&quot;black_stone&quot;</span>: (<span class="hljs-number">0</span>, <span class="hljs-number">0</span>, <span class="hljs-number">0</span>),       <span class="hljs-comment"># 黑棋颜色</span>
    <span class="hljs-string">&quot;white_stone&quot;</span>: (<span class="hljs-number">255</span>, <span class="hljs-number">255</span>, <span class="hljs-number">255</span>), <span class="hljs-comment"># 白棋颜色</span>
}


<span class="hljs-keyword">class</span> <span class="hljs-title class_">GomokuGame</span>:
    <span class="hljs-string">&quot;&quot;&quot;游戏主类，负责游戏流程控制和界面显示&quot;&quot;&quot;</span>
    
    <span class="hljs-keyword">def</span> <span class="hljs-title function_">__init__</span>(<span class="hljs-params">self</span>):
        <span class="hljs-string">&quot;&quot;&quot;初始化游戏&quot;&quot;&quot;</span>
        pygame.init()  <span class="hljs-comment"># 初始化Pygame所有模块</span>
        
        <span class="hljs-comment"># 创建游戏窗口，大小为WINDOW_SIZE × WINDOW_SIZE</span>
        <span class="hljs-variable language_">self</span>.window = pygame.display.set_mode((WINDOW_SIZE, WINDOW_SIZE))

        pygame.display.set_caption(<span class="hljs-string">&quot;五子棋人机对战&quot;</span>) <span class="hljs-comment"># 设置窗口标题</span>
        <span class="hljs-variable language_">self</span>.draw_board() <span class="hljs-comment"># 绘制初始棋盘</span>

    <span class="hljs-keyword">def</span> <span class="hljs-title function_">main_loop</span>(<span class="hljs-params">self</span>):
        <span class="hljs-string">&quot;&quot;&quot;游戏主循环，不断处理事件和更新画面&quot;&quot;&quot;</span>
        <span class="hljs-keyword">while</span> <span class="hljs-literal">True</span>:  <span class="hljs-comment"># 无限循环，直到游戏退出</span>
            <span class="hljs-comment"># 获取所有发生的事件（鼠标点击、窗口关闭等）</span>
            <span class="hljs-keyword">for</span> event <span class="hljs-keyword">in</span> pygame.event.get():
                <span class="hljs-comment"># 如果事件是关闭窗口（点击右上角的X）</span>
                <span class="hljs-keyword">if</span> event.<span class="hljs-built_in">type</span> == pygame.QUIT:
                    pygame.quit()  <span class="hljs-comment"># 关闭Pygame</span>
                    sys.exit()     <span class="hljs-comment"># 退出程序</span>
            
            <span class="hljs-comment"># 更新整个游戏窗口的显示</span>
            pygame.display.update()
    
    <span class="hljs-keyword">def</span> <span class="hljs-title function_">draw_board</span>(<span class="hljs-params">self</span>):
        <span class="hljs-string">&quot;&quot;&quot;绘制棋盘背景、网格线和星位点&quot;&quot;&quot;</span>
        <span class="hljs-comment"># 填充背景颜色</span>
        <span class="hljs-variable language_">self</span>.window.fill(COLORS[<span class="hljs-string">&quot;background&quot;</span>])
        
        <span class="hljs-comment"># 绘制棋盘网格线</span>
        <span class="hljs-keyword">for</span> i <span class="hljs-keyword">in</span> <span class="hljs-built_in">range</span>(BOARD_SIZE):
            <span class="hljs-comment"># 绘制水平线：从左到右</span>
            pygame.draw.line(<span class="hljs-variable language_">self</span>.window, COLORS[<span class="hljs-string">&quot;line&quot;</span>],  <span class="hljs-comment"># 表面, 颜色</span>
                            (GAP, GAP * (i + <span class="hljs-number">1</span>)),          <span class="hljs-comment"># 起点坐标</span>
                            (WINDOW_SIZE - GAP, GAP * (i + <span class="hljs-number">1</span>)),  <span class="hljs-comment"># 终点坐标</span>
                            <span class="hljs-number">1</span>)                             <span class="hljs-comment"># 线宽（像素）</span>
            
            <span class="hljs-comment"># 绘制垂直线：从上到下</span>
            pygame.draw.line(<span class="hljs-variable language_">self</span>.window, COLORS[<span class="hljs-string">&quot;line&quot;</span>],  <span class="hljs-comment"># 表面, 颜色</span>
                            (GAP * (i + <span class="hljs-number">1</span>), GAP),          <span class="hljs-comment"># 起点坐标</span>
                            (GAP * (i + <span class="hljs-number">1</span>), WINDOW_SIZE - GAP),  <span class="hljs-comment"># 终点坐标</span>
                            <span class="hljs-number">1</span>)                             <span class="hljs-comment"># 线宽</span>
            
        <span class="hljs-comment"># 绘制五个星位点（棋盘上的小黑点）</span>
        <span class="hljs-keyword">for</span> point <span class="hljs-keyword">in</span> POINTS:
            <span class="hljs-comment"># 计算星位点的像素坐标</span>
            <span class="hljs-comment"># point[0]和point[1]是网格坐标，需要转换为像素坐标</span>
            <span class="hljs-comment"># 注意：point坐标是0-based，但棋盘有边框，所以要+1</span>
            pixel_x = GAP * (point[<span class="hljs-number">0</span>] + <span class="hljs-number">1</span>)
            pixel_y = GAP * (point[<span class="hljs-number">1</span>] + <span class="hljs-number">1</span>)
            
            <span class="hljs-comment"># 绘制小黑点：表面, 颜色, 圆心坐标, 半径</span>
            pygame.draw.circle(<span class="hljs-variable language_">self</span>.window, COLORS[<span class="hljs-string">&quot;line&quot;</span>], 
                              (pixel_x, pixel_y), <span class="hljs-number">5</span>)
</code></pre>
<p>棋盘准备好后我们需要实现落棋子的逻辑，落单个棋子本身的逻辑很容易实现，使用 <code>pygame.draw.circle</code> 即可完成。需要注意的是窗口使用的坐标是<strong>像素</strong>，而我们落棋子的坐标是 15x15 的网格坐标，因此需要实现将鼠标点击的像素坐标转换为网格坐标后，才可进行落子。还需要特别注意的是前文给窗口留了内边距，因此网格的实际有效坐标是从 1 开始的，像素坐标转换为的网格坐标应在 [1, 15] 范围内。</p>
<pre class="hljs"><code>STONE_SIZE = <span class="hljs-number">15</span> <span class="hljs-comment"># 棋子半径（像素）</span>

<span class="hljs-keyword">class</span> <span class="hljs-title class_">GomokuGame</span>:
    <span class="hljs-string">&quot;&quot;&quot;游戏主类，负责游戏流程控制和界面显示&quot;&quot;&quot;</span>

    <span class="hljs-keyword">def</span> <span class="hljs-title function_">__init__</span>(<span class="hljs-params">self</span>):
        <span class="hljs-string">&quot;&quot;&quot;初始化游戏&quot;&quot;&quot;</span>

        <span class="hljs-comment"># 省略部分代码 ......        </span>

        <span class="hljs-comment"># 当前回合的棋子颜色，1=黑棋（玩家先手），2=白棋（AI）</span>
        <span class="hljs-variable language_">self</span>.cur_color = <span class="hljs-number">1</span>

    <span class="hljs-keyword">def</span> <span class="hljs-title function_">main_loop</span>(<span class="hljs-params">self</span>):
        <span class="hljs-string">&quot;&quot;&quot;游戏主循环，不断处理事件和更新画面&quot;&quot;&quot;</span>
        <span class="hljs-keyword">while</span> <span class="hljs-literal">True</span>:  <span class="hljs-comment"># 无限循环，直到游戏退出</span>
            <span class="hljs-comment"># 获取所有发生的事件（鼠标点击、窗口关闭等）</span>
            <span class="hljs-keyword">for</span> event <span class="hljs-keyword">in</span> pygame.event.get():

                <span class="hljs-comment"># 省略部分代码 ......      </span>

                <span class="hljs-comment"># 如果事件是鼠标按钮按下（玩家点击落子）</span>
                <span class="hljs-keyword">if</span> event.<span class="hljs-built_in">type</span> == pygame.MOUSEBUTTONDOWN:
                    <span class="hljs-comment"># 获取鼠标点击的像素坐标</span>
                    x, y = event.pos
                    <span class="hljs-comment"># 将像素坐标转换为棋盘网格坐标</span>
                    grid_x, grid_y = <span class="hljs-variable language_">self</span>.compute_grid_position(x, y)
                    <span class="hljs-comment"># 处理玩家落子，如果成功返回True</span>
                    ret = <span class="hljs-variable language_">self</span>.make_move(grid_x, grid_y)
            
            <span class="hljs-comment"># 更新整个游戏窗口的显示</span>
            pygame.display.update()

    <span class="hljs-keyword">def</span> <span class="hljs-title function_">place_stone</span>(<span class="hljs-params">self, grid_x, grid_y, color</span>):
        <span class="hljs-string">&quot;&quot;&quot;
        在棋盘上绘制一个棋子
        
        参数:
            grid_x: 网格行坐标
            grid_y: 网格列坐标
            color: 棋子颜色，1=黑棋，2=白棋
        &quot;&quot;&quot;</span>
        <span class="hljs-comment"># 根据颜色选择棋子颜色</span>
        stone_color = COLORS[<span class="hljs-string">&quot;black_stone&quot;</span>] <span class="hljs-keyword">if</span> color == <span class="hljs-number">1</span> <span class="hljs-keyword">else</span> COLORS[<span class="hljs-string">&quot;white_stone&quot;</span>]
        <span class="hljs-comment"># 绘制圆形棋子</span>
        pygame.draw.circle(<span class="hljs-variable language_">self</span>.window, stone_color, (grid_x * GAP, grid_y * GAP), STONE_SIZE)

    <span class="hljs-keyword">def</span> <span class="hljs-title function_">compute_grid_position</span>(<span class="hljs-params">self, x, y</span>):
        <span class="hljs-string">&quot;&quot;&quot;
        将鼠标点击的像素坐标转换为棋盘网格坐标
        
        参数:
            x: 像素X坐标
            y: 像素Y坐标
            
        返回:
            (grid_x, grid_y): 网格坐标
        &quot;&quot;&quot;</span>
        <span class="hljs-comment"># 计算最近的网格坐标：像素坐标 ÷ 网格间距，四舍五入</span>
        grid_x = <span class="hljs-built_in">round</span>(x / GAP)
        grid_y = <span class="hljs-built_in">round</span>(y / GAP)
        
        <span class="hljs-comment"># 确保坐标在有效范围内（1到BOARD_SIZE-1）</span>
        grid_x = <span class="hljs-built_in">max</span>(<span class="hljs-number">1</span>, <span class="hljs-built_in">min</span>(BOARD_SIZE - <span class="hljs-number">1</span>, grid_x))
        grid_y = <span class="hljs-built_in">max</span>(<span class="hljs-number">1</span>, <span class="hljs-built_in">min</span>(BOARD_SIZE - <span class="hljs-number">1</span>, grid_y))

        <span class="hljs-keyword">return</span> grid_x, grid_y

    <span class="hljs-keyword">def</span> <span class="hljs-title function_">make_move</span>(<span class="hljs-params">self, grid_x, grid_y</span>):
        <span class="hljs-string">&quot;&quot;&quot;
        处理棋子落子，包括玩家和AI
        
        参数:
            grid_x: 网格行坐标
            grid_y: 网格列坐标
            
        返回:
            True: 落子成功
            False: 落子失败（位置无效或已有棋子）
        &quot;&quot;&quot;</span>
        <span class="hljs-comment"># 检查坐标是否在有效范围内（1到BOARD_SIZE-1）</span>
        <span class="hljs-keyword">if</span> <span class="hljs-number">0</span> &lt; grid_x &lt; BOARD_SIZE <span class="hljs-keyword">and</span> <span class="hljs-number">0</span> &lt; grid_y &lt; BOARD_SIZE:
            <span class="hljs-comment"># 检查这个位置是否为空</span>
            <span class="hljs-keyword">if</span> <span class="hljs-variable language_">self</span>.judge.board[grid_x][grid_y] == <span class="hljs-number">0</span>:
                <span class="hljs-comment"># 在棋盘上绘制棋子</span>
                <span class="hljs-variable language_">self</span>.place_stone(grid_x, grid_y, <span class="hljs-variable language_">self</span>.cur_color)
                
                <span class="hljs-comment"># 切换当前回合：黑棋变白棋，白棋变黑棋</span>
                <span class="hljs-variable language_">self</span>.cur_color = <span class="hljs-number">2</span> <span class="hljs-keyword">if</span> <span class="hljs-variable language_">self</span>.cur_color == <span class="hljs-number">1</span> <span class="hljs-keyword">else</span> <span class="hljs-number">1</span>
                <span class="hljs-keyword">return</span> <span class="hljs-literal">True</span>  <span class="hljs-comment"># 落子成功</span>
        <span class="hljs-keyword">return</span> <span class="hljs-literal">False</span>  <span class="hljs-comment"># 落子失败</span>
</code></pre>
<p>接下来来完善 <strong>Judge</strong> 类的代码。Judge 类在此处主要充当一个裁判的角色，回想一下各种比赛中裁判主要干什么？裁判的作用就是记录比赛当前得分与判断输赢的，我们把五子棋当前的棋局称之为当前得分，即裁判需要记录每时每刻棋盘的样子。</p>
<p>直接使用一个二维数组来表示整个棋盘即可。0 表示未落子；1 表示已落黑子；2 表示已落白子。那么检查输赢的逻辑就可以抽象为判断二维数组中是否在四个方向（横向、纵向、斜向、反斜）中任意方向存在连续相同的 5 个元素；判断平局的逻辑即可抽象为检查二维数组中是否还存在 0。</p>
<pre class="hljs"><code><span class="hljs-keyword">class</span> <span class="hljs-title class_">Judge</span>:
    <span class="hljs-string">&quot;&quot;&quot;裁判类，负责管理棋盘状态和判断胜负&quot;&quot;&quot;</span>
    
    <span class="hljs-keyword">def</span> <span class="hljs-title function_">__init__</span>(<span class="hljs-params">self</span>):
        <span class="hljs-string">&quot;&quot;&quot;初始化棋盘&quot;&quot;&quot;</span>
        <span class="hljs-comment"># 创建棋盘二维数组，所有位置初始化为0（空）</span>
        <span class="hljs-comment"># 棋盘大小：BOARD_SIZE × BOARD_SIZE（第1行和第一列没有使用）</span>
        <span class="hljs-comment"># 0 = 空，1 = 黑棋（玩家），2 = 白棋（AI）</span>
        <span class="hljs-variable language_">self</span>.board = [[<span class="hljs-number">0</span> <span class="hljs-keyword">for</span> _ <span class="hljs-keyword">in</span> <span class="hljs-built_in">range</span>(BOARD_SIZE)] <span class="hljs-keyword">for</span> _ <span class="hljs-keyword">in</span> <span class="hljs-built_in">range</span>(BOARD_SIZE)]
    
    <span class="hljs-keyword">def</span> <span class="hljs-title function_">update_board</span>(<span class="hljs-params">self, x, y, color</span>):
        <span class="hljs-string">&quot;&quot;&quot;
        更新棋盘并检查游戏是否结束
        
        参数:
            x: 落子的行坐标
            y: 落子的列坐标
            color: 棋子颜色，1或2
            
        返回:
            True: 更新成功
            False: 更新失败（传入位置有棋子）
        &quot;&quot;&quot;</span>

        <span class="hljs-comment"># 检查要落子的位置是否为空</span>
        <span class="hljs-keyword">if</span> <span class="hljs-variable language_">self</span>.board[x][y] == <span class="hljs-number">0</span>:
            <span class="hljs-comment"># 在棋盘上放置棋子</span>
            <span class="hljs-variable language_">self</span>.board[x][y] = color
            
            <span class="hljs-comment"># 检查是否获胜（五子连珠）</span>
            <span class="hljs-keyword">if</span> <span class="hljs-variable language_">self</span>.check_win(x, y):
                winner = <span class="hljs-string">&quot;玩家 (黑色)&quot;</span> <span class="hljs-keyword">if</span> color == <span class="hljs-number">1</span> <span class="hljs-keyword">else</span> <span class="hljs-string">&quot;AI (白色)&quot;</span>
                <span class="hljs-built_in">print</span>(<span class="hljs-string">f&quot;<span class="hljs-subst">{winner}</span> 赢了&quot;</span>)

            <span class="hljs-comment"># 检查是否平局（棋盘满了）</span>
            <span class="hljs-keyword">if</span> <span class="hljs-variable language_">self</span>.is_full():
                <span class="hljs-built_in">print</span>(<span class="hljs-string">&quot;平局 ！！！&quot;</span>)

            <span class="hljs-comment"># 返回更新成功</span>
            <span class="hljs-keyword">return</span> <span class="hljs-literal">True</span>
        <span class="hljs-comment"># 位置已有棋子，更新失败</span>
        <span class="hljs-keyword">return</span> <span class="hljs-literal">False</span>
    
    <span class="hljs-keyword">def</span> <span class="hljs-title function_">check_win</span>(<span class="hljs-params">self, x, y</span>):
        <span class="hljs-string">&quot;&quot;&quot;
        检查是否五子连珠（获胜条件）
        
        参数:
            x: 最后落子的行坐标
            y: 最后落子的列坐标
            
        返回:
            True: 有五子连珠，获胜
            False: 没有五子连珠
        &quot;&quot;&quot;</span>
        <span class="hljs-comment"># 四个检查方向：(行增量, 列增量)</span>
        directions = [(<span class="hljs-number">1</span>, <span class="hljs-number">0</span>),   <span class="hljs-comment"># 水平方向（右/左）</span>
                      (<span class="hljs-number">0</span>, <span class="hljs-number">1</span>),   <span class="hljs-comment"># 垂直方向（下/上）</span>
                      (<span class="hljs-number">1</span>, <span class="hljs-number">1</span>),   <span class="hljs-comment"># 右下/左上对角线</span>
                      (<span class="hljs-number">1</span>, -<span class="hljs-number">1</span>)]  <span class="hljs-comment"># 左下/右上对角线</span>
        
        <span class="hljs-comment"># 获取最后落子的颜色</span>
        cur_color = <span class="hljs-variable language_">self</span>.board[x][y]
        
        <span class="hljs-comment"># 检查每个方向</span>
        <span class="hljs-keyword">for</span> dx, dy <span class="hljs-keyword">in</span> directions:
            stone_count = <span class="hljs-number">1</span>  <span class="hljs-comment"># 从当前棋子开始计数，初始为1</span>
            
            <span class="hljs-comment"># 向两个方向检查：正向和反向</span>
            <span class="hljs-keyword">for</span> sign <span class="hljs-keyword">in</span> (<span class="hljs-number">1</span>, -<span class="hljs-number">1</span>):
                <span class="hljs-comment"># 从当前位置向指定方向移动一步</span>
                cur_x, cur_y = x + dx * sign, y + dy * sign
                
                <span class="hljs-comment"># 沿着这个方向连续检查相同颜色的棋子</span>
                <span class="hljs-keyword">while</span> (<span class="hljs-number">0</span> &lt; cur_x &lt; BOARD_SIZE <span class="hljs-keyword">and</span>      <span class="hljs-comment"># 检查行坐标是否在边界内</span>
                       <span class="hljs-number">0</span> &lt; cur_y &lt; BOARD_SIZE <span class="hljs-keyword">and</span>      <span class="hljs-comment"># 检查列坐标是否在边界内</span>
                       <span class="hljs-variable language_">self</span>.board[cur_x][cur_y] == cur_color):  <span class="hljs-comment"># 检查颜色是否相同</span>
                    stone_count += <span class="hljs-number">1</span>  <span class="hljs-comment"># 发现相同颜色棋子，计数加1</span>
                    <span class="hljs-comment"># 继续向同一方向移动，检查下一个位置</span>
                    cur_x += dx * sign
                    cur_y += dy * sign
            
            <span class="hljs-comment"># 如果连续相同颜色的棋子数达到5个，获胜！</span>
            <span class="hljs-keyword">if</span> stone_count &gt;= <span class="hljs-number">5</span>:
                <span class="hljs-keyword">return</span> <span class="hljs-literal">True</span>
        
        <span class="hljs-comment"># 所有方向都检查完毕，没有找到五子连珠</span>
        <span class="hljs-keyword">return</span> <span class="hljs-literal">False</span>
    
    <span class="hljs-keyword">def</span> <span class="hljs-title function_">is_full</span>(<span class="hljs-params">self</span>):
        <span class="hljs-string">&quot;&quot;&quot;
        检查棋盘是否已满（平局条件）
        
        返回:
            True: 棋盘已满，平局
            False: 棋盘还有空位
        &quot;&quot;&quot;</span>
        <span class="hljs-comment"># 遍历棋盘的每一行</span>
        <span class="hljs-keyword">for</span> row <span class="hljs-keyword">in</span> <span class="hljs-variable language_">self</span>.board:
            <span class="hljs-comment"># 检查这一行是否还有空位（0表示空位）</span>
            <span class="hljs-keyword">if</span> <span class="hljs-number">0</span> <span class="hljs-keyword">in</span> row:
                <span class="hljs-keyword">return</span> <span class="hljs-literal">False</span>  <span class="hljs-comment"># 发现空位，棋盘未满</span>
        
        <span class="hljs-comment"># 所有位置都非空，棋盘已满</span>
        <span class="hljs-keyword">return</span> <span class="hljs-literal">True</span>
</code></pre>
<p>对于 <strong>AI</strong> 类，需要考虑 AI 使用何种方式进行「思考」？网上有基于搜索的<a href="https://zh.wikipedia.org/zh-sg/%E6%9E%81%E5%B0%8F%E5%8C%96%E6%9E%81%E5%A4%A7%E7%AE%97%E6%B3%95">极小化极大算法（MiniMax算法）</a>和 <a href="https://zh.wikipedia.org/wiki/Alpha-beta%E5%89%AA%E6%9E%9D">Alpha-beta 剪枝</a>，也有使用 <a href="https://zh.wikipedia.org/wiki/Q%E5%AD%A6%E4%B9%A0">Q-learning</a> 等强化学习算法优化决策的方法。此处我们尽量实现的简单一些，采用预存储所有的赢法来加快评估速度，暂不考虑引入深度搜索来预见多步之后的变化。</p>
<p>那么所有赢法如何进行存储呢？我们把所有能连成五子的情况都列举出来，考虑到棋盘上某一个交叉点可能同时属于好几种五子连珠（赢法）的问题，需要采用三维数组进行所有赢法的枚举。</p>
<figure data-type="image" tabindex="1"><img src="https://www.guanngxu.com/post-images/1767500754691.png" alt=""></figure>
<p>四个方向的五子连珠的所有情况全部都存储下来，使用数组 <code>win_patterns[x][y][k]</code> 来表示，其中下标 <code>x, y</code> 用来表示网格坐标点，下标 <code>k</code> 用来表示属于第 k 种赢法。</p>
<pre class="hljs"><code><span class="hljs-keyword">class</span> <span class="hljs-title class_">AI</span>:
    <span class="hljs-string">&quot;&quot;&quot;人工智能类，负责AI的下棋逻辑&quot;&quot;&quot;</span>
    
    <span class="hljs-keyword">def</span> <span class="hljs-title function_">__init__</span>(<span class="hljs-params">self</span>):
        <span class="hljs-string">&quot;&quot;&quot;初始化AI&quot;&quot;&quot;</span>
        <span class="hljs-comment"># 三维数组：记录每个棋盘位置属于哪些获胜模式</span>
        <span class="hljs-comment"># win_patterns[x][y][k] = True 表示位置(x,y)属于第k个获胜模式</span>
        <span class="hljs-variable language_">self</span>.win_patterns = [[[<span class="hljs-literal">False</span> <span class="hljs-keyword">for</span> _ <span class="hljs-keyword">in</span> <span class="hljs-built_in">range</span>(TOTAL_WIN_PATTERNS)] 
                     <span class="hljs-keyword">for</span> _ <span class="hljs-keyword">in</span> <span class="hljs-built_in">range</span>(BOARD_SIZE)] 
                     <span class="hljs-keyword">for</span> _ <span class="hljs-keyword">in</span> <span class="hljs-built_in">range</span>(BOARD_SIZE)]
        <span class="hljs-comment"># 当前已记录的获胜模式数量</span>
        <span class="hljs-variable language_">self</span>.win_pattern_count = <span class="hljs-number">0</span>

        <span class="hljs-comment"># 初始化所有可能的获胜模式</span>
        <span class="hljs-variable language_">self</span>.init_win_patterns()
    
    <span class="hljs-keyword">def</span> <span class="hljs-title function_">add_win_pattern</span>(<span class="hljs-params">self, start_i, start_j, di, dj</span>):
        <span class="hljs-string">&quot;&quot;&quot;
        添加一个获胜模式
        
        参数:
            start_i: 起始行
            start_j: 起始列
            di: 行方向增量
            dj: 列方向增量（与di配合定义方向）
        &quot;&quot;&quot;</span>
        <span class="hljs-comment"># 一个获胜模式包含连续的5个位置</span>
        <span class="hljs-keyword">for</span> k <span class="hljs-keyword">in</span> <span class="hljs-built_in">range</span>(<span class="hljs-number">5</span>):
            <span class="hljs-comment"># 标记这个获胜模式包含的所有位置</span>
            <span class="hljs-variable language_">self</span>.win_patterns[start_i + k * di][start_j + k * dj][<span class="hljs-variable language_">self</span>.win_pattern_count] = <span class="hljs-literal">True</span>
        
        <span class="hljs-comment"># 获胜模式计数器加1</span>
        <span class="hljs-variable language_">self</span>.win_pattern_count += <span class="hljs-number">1</span>

    <span class="hljs-keyword">def</span> <span class="hljs-title function_">init_win_patterns</span>(<span class="hljs-params">self</span>):
        <span class="hljs-string">&quot;&quot;&quot;初始化所有可能的获胜模式（所有可能的五子连珠位置）&quot;&quot;&quot;</span>

        <span class="hljs-comment"># 横向和纵向的所有赢法</span>
        <span class="hljs-keyword">for</span> i <span class="hljs-keyword">in</span> <span class="hljs-built_in">range</span>(<span class="hljs-number">1</span>, BOARD_SIZE):
            <span class="hljs-keyword">for</span> j <span class="hljs-keyword">in</span> <span class="hljs-built_in">range</span>(<span class="hljs-number">1</span>, BOARD_SIZE - <span class="hljs-number">4</span>):
                <span class="hljs-comment"># 水平方向的获胜模式</span>
                <span class="hljs-variable language_">self</span>.add_win_pattern(i, j, <span class="hljs-number">0</span>, <span class="hljs-number">1</span>)
                <span class="hljs-comment"># 垂直方向的获胜模式</span>
                <span class="hljs-variable language_">self</span>.add_win_pattern(j, i, <span class="hljs-number">1</span>, <span class="hljs-number">0</span>)
        
        <span class="hljs-comment"># 对角线方向的所有赢法</span>
        <span class="hljs-keyword">for</span> i <span class="hljs-keyword">in</span> <span class="hljs-built_in">range</span>(<span class="hljs-number">1</span>, BOARD_SIZE - <span class="hljs-number">4</span>):
            <span class="hljs-keyword">for</span> j <span class="hljs-keyword">in</span> <span class="hljs-built_in">range</span>(<span class="hljs-number">1</span>, BOARD_SIZE - <span class="hljs-number">4</span>):
                <span class="hljs-comment"># 右下对角线获胜模式</span>
                <span class="hljs-variable language_">self</span>.add_win_pattern(i, j, <span class="hljs-number">1</span>, <span class="hljs-number">1</span>)
                <span class="hljs-comment"># 左下对角线获胜模式</span>
                <span class="hljs-variable language_">self</span>.add_win_pattern(i, BOARD_SIZE - j, <span class="hljs-number">1</span>, -<span class="hljs-number">1</span>)
</code></pre>
<p>所有的赢法列举并存储下来了怎么使用呢？到现在还没有明确使用方式。我们再回顾一下玩五子棋的场景，人类落下一颗棋子后，此时 AI 需要「计算」需要把棋子落到哪个位置可以获取到最大收益。如何计算最大收益？自然是需要一个评估函数计算出每个位置的收益值再进行比较，方可找到最大收益值的位置。</p>
<p>现在的问题就变成了如何设计评估函数？因为前文我们已经存储了所有赢法的信息，此处我们直接采用记录玩家在第 k 种赢法上已经落下了多少个棋子来进行评估，自然第 k 种赢法落下的棋子越多得分就越高。值得提醒的是，只要对手在第 k 种赢法上面随便落一子，那么本方在第 k 种赢法上就不再可能获胜。</p>
<p>当任何一方玩家落子时，我们即遍历所有赢法，并将该落子位置所对应的赢法在当次玩家获胜模式占有棋子数进行更新，通过对获胜模式不同棋子数赋予不同得分权重，即可抽象出数学模型（公式）实现计算最大收益位置的效果。</p>
<pre class="hljs"><code><span class="hljs-keyword">class</span> <span class="hljs-title class_">AI</span>:
    <span class="hljs-string">&quot;&quot;&quot;人工智能类，负责AI的下棋逻辑&quot;&quot;&quot;</span>
    
    <span class="hljs-keyword">def</span> <span class="hljs-title function_">__init__</span>(<span class="hljs-params">self</span>):
        <span class="hljs-string">&quot;&quot;&quot;初始化AI&quot;&quot;&quot;</span>
        <span class="hljs-comment"># 省略部分代码......</span>
        <span class="hljs-comment"># 记录每个获胜模式中AI已占有的棋子数</span>
        <span class="hljs-variable language_">self</span>.ai_win_count = [<span class="hljs-number">0</span> <span class="hljs-keyword">for</span> _ <span class="hljs-keyword">in</span> <span class="hljs-built_in">range</span>(TOTAL_WIN_PATTERNS)]
        
        <span class="hljs-comment"># 记录每个获胜模式中玩家已占有的棋子数</span>
        <span class="hljs-variable language_">self</span>.human_win_count = [<span class="hljs-number">0</span> <span class="hljs-keyword">for</span> _ <span class="hljs-keyword">in</span> <span class="hljs-built_in">range</span>(TOTAL_WIN_PATTERNS)]
        
    <span class="hljs-keyword">def</span> <span class="hljs-title function_">update_win_counts</span>(<span class="hljs-params">self, x, y, color</span>):
        <span class="hljs-string">&quot;&quot;&quot;
        更新获胜模式计数（当棋子落下时调用）
        
        参数:
            x: 行坐标
            y: 列坐标
            color: 棋子颜色（1:玩家/黑棋，2:AI/白棋）
        &quot;&quot;&quot;</span>
        <span class="hljs-comment"># 遍历所有获胜模式</span>
        <span class="hljs-keyword">for</span> k <span class="hljs-keyword">in</span> <span class="hljs-built_in">range</span>(TOTAL_WIN_PATTERNS):
            <span class="hljs-comment"># 检查这个位置是否属于第k个获胜模式</span>
            <span class="hljs-keyword">if</span> <span class="hljs-variable language_">self</span>.win_patterns[x][y][k]:
                <span class="hljs-keyword">if</span> color == <span class="hljs-number">2</span>:  <span class="hljs-comment"># AI下棋（白棋）</span>
                    <span class="hljs-comment"># AI在这个获胜模式中增加一子</span>
                    <span class="hljs-variable language_">self</span>.ai_win_count[k] += <span class="hljs-number">1</span>
                    <span class="hljs-comment"># 玩家在这个模式中不可能获胜了（设置为异常值6，超过5）</span>
                    <span class="hljs-variable language_">self</span>.human_win_count[k] = <span class="hljs-number">6</span>
                <span class="hljs-keyword">else</span>:  <span class="hljs-comment"># 玩家下棋（黑棋）</span>
                    <span class="hljs-comment"># 玩家在这个获胜模式中增加一子</span>
                    <span class="hljs-variable language_">self</span>.human_win_count[k] += <span class="hljs-number">1</span>
                    <span class="hljs-comment"># AI在这个模式中不可能获胜了</span>
                    <span class="hljs-variable language_">self</span>.ai_win_count[k] = <span class="hljs-number">6</span>
</code></pre>
<p>至此就可以设计实现评估函数，以期达到自主对弈的效果了。我们为不同长度的的连珠设置不同的分值，并使相同连珠的情况下 AI 得分略高于人类玩家，使 AI 更具备进攻性。当人类落子后，即遍历棋盘所有空位，并计算空位对应的得分，将最高得分的位置返回进行落子即可。</p>
<pre class="hljs"><code><span class="hljs-keyword">class</span> <span class="hljs-title class_">AI</span>:
    <span class="hljs-string">&quot;&quot;&quot;人工智能类，负责AI的下棋逻辑&quot;&quot;&quot;</span>
    
    <span class="hljs-keyword">def</span> <span class="hljs-title function_">__init__</span>(<span class="hljs-params">self</span>):
        <span class="hljs-comment"># 省略部分代码......</span>
        <span class="hljs-comment"># 玩家的得分权重：不同长度的连珠对应不同的分数</span>
        <span class="hljs-comment"># 键：连珠长度，值：对应的分数</span>
        <span class="hljs-variable language_">self</span>.human_score_weights = {
            <span class="hljs-number">1</span>: <span class="hljs-number">200</span>,    <span class="hljs-comment"># 单独一子</span>
            <span class="hljs-number">2</span>: <span class="hljs-number">400</span>,    <span class="hljs-comment"># 两子连珠</span>
            <span class="hljs-number">3</span>: <span class="hljs-number">2000</span>,   <span class="hljs-comment"># 三子连珠</span>
            <span class="hljs-number">4</span>: <span class="hljs-number">10000</span>,  <span class="hljs-comment"># 四子连珠（差一子获胜）</span>
        }
        
        <span class="hljs-comment"># AI的得分权重（略高于玩家，使AI更具攻击性）</span>
        <span class="hljs-variable language_">self</span>.ai_score_weights = {
            <span class="hljs-number">1</span>: <span class="hljs-number">220</span>,    <span class="hljs-comment"># 比玩家略高</span>
            <span class="hljs-number">2</span>: <span class="hljs-number">420</span>,
            <span class="hljs-number">3</span>: <span class="hljs-number">2100</span>,
            <span class="hljs-number">4</span>: <span class="hljs-number">20000</span>,  <span class="hljs-comment"># 四子连珠得分远高于玩家</span>
        }

    <span class="hljs-keyword">def</span> <span class="hljs-title function_">evaluate_position</span>(<span class="hljs-params">self, human_score, ai_score</span>):
        <span class="hljs-string">&quot;&quot;&quot;
        评估位置的得分（综合进攻和防守）
        
        参数:
            human_score: 玩家在这个位置的得分
            ai_score: AI在这个位置的得分
        
        返回:
            综合得分（AI进攻和防守玩家的加权和）
        &quot;&quot;&quot;</span>
        OFFENSIVE_WEIGHT = <span class="hljs-number">1.2</span>  <span class="hljs-comment"># 进攻权重：鼓励AI积极进攻</span>
        DEFENSIVE_WEIGHT = <span class="hljs-number">1.0</span>  <span class="hljs-comment"># 防守权重：阻止玩家连成五子</span>

        <span class="hljs-comment"># 综合得分 = AI进攻得分 * 进攻权重 + 防守玩家得分 * 防守权重</span>
        <span class="hljs-keyword">return</span> ai_score * OFFENSIVE_WEIGHT + human_score * DEFENSIVE_WEIGHT

    <span class="hljs-keyword">def</span> <span class="hljs-title function_">ai_run</span>(<span class="hljs-params">self, board</span>):
        <span class="hljs-string">&quot;&quot;&quot;
        AI主逻辑：选择最佳落子位置
        
        参数:
            board: 当前棋盘状态
        
        返回:
            (best_i, best_j): 最佳落子位置的行列坐标
        &quot;&quot;&quot;</span>
        <span class="hljs-comment"># 初始化最佳位置和最佳得分</span>
        best_pos = (<span class="hljs-number">0</span>, <span class="hljs-number">0</span>)  <span class="hljs-comment"># 最佳位置（默认左上角）</span>
        best_score = -<span class="hljs-number">1</span>    <span class="hljs-comment"># 最佳得分（初始为-1）</span>
        
        <span class="hljs-comment"># 遍历棋盘上的所有位置</span>
        <span class="hljs-keyword">for</span> i <span class="hljs-keyword">in</span> <span class="hljs-built_in">range</span>(<span class="hljs-number">1</span>, BOARD_SIZE):
            <span class="hljs-keyword">for</span> j <span class="hljs-keyword">in</span> <span class="hljs-built_in">range</span>(<span class="hljs-number">1</span>, BOARD_SIZE):
                <span class="hljs-comment"># 如果这个位置是空的</span>
                <span class="hljs-keyword">if</span> board[i][j] == <span class="hljs-number">0</span>:
                    <span class="hljs-comment"># 重置当前位置i,j得分</span>
                    cur_human_score, cur_ai_score = <span class="hljs-number">0</span>, <span class="hljs-number">0</span>
                    
                    <span class="hljs-comment"># 遍历所有获胜模式，计算这个位置的得分</span>
                    <span class="hljs-keyword">for</span> k <span class="hljs-keyword">in</span> <span class="hljs-built_in">range</span>(TOTAL_WIN_PATTERNS):
                        <span class="hljs-comment"># 如果这个位置属于第k个获胜模式</span>
                        <span class="hljs-keyword">if</span> <span class="hljs-variable language_">self</span>.win_patterns[i][j][k]:
                            <span class="hljs-comment"># 累加玩家在这个模式下的得分</span>
                            cur_human_score += <span class="hljs-variable language_">self</span>.human_score_weights.get(
                                <span class="hljs-variable language_">self</span>.human_win_count[k], <span class="hljs-number">0</span>)
                            <span class="hljs-comment"># 累加AI在这个模式下的得分</span>
                            cur_ai_score += <span class="hljs-variable language_">self</span>.ai_score_weights.get(
                                <span class="hljs-variable language_">self</span>.ai_win_count[k], <span class="hljs-number">0</span>)
                    
                    <span class="hljs-comment"># 计算综合得分</span>
                    cur_score = <span class="hljs-variable language_">self</span>.evaluate_position(cur_human_score, cur_ai_score)
                    
                    <span class="hljs-comment"># 如果当前得分更好，更新最佳位置</span>
                    <span class="hljs-keyword">if</span> cur_score &gt;= best_score:
                        best_score = cur_score
                        best_pos = (i, j)
        
        <span class="hljs-comment"># 返回最佳落子位置</span>
        <span class="hljs-keyword">return</span> best_pos
</code></pre>
<p>考虑到赢棋时需要给用户以比较友好的提示，现在采用 <code>print</code> 的方式太过粗糙了，因此再实现一个弹窗类用于显示提示信息。由于 pygame 没有内置的弹窗工具，我们借助 python 自带的 <a href="https://docs.python.org/3/library/tkinter.html">tkinter</a> 实现。</p>
<p>因为 tkinter 中的 <code>messagebox</code> 会阻塞主线程的运行，导致获胜时棋子不能及时显示出来，因此再引入线程来避免程序被阻塞的问题，当然引入了线程自然就会涉及到共享变量、互斥锁等问题，具体实现如下：</p>
<pre class="hljs"><code><span class="hljs-keyword">import</span> time
<span class="hljs-keyword">import</span> threading
<span class="hljs-keyword">from</span> tkinter <span class="hljs-keyword">import</span> Tk, messagebox


<span class="hljs-comment"># 线程锁：用于多线程同步，防止多个线程同时访问共享资源</span>
lock = threading.Lock()

<span class="hljs-comment"># 弹窗状态：标记是否已经弹出获胜提示窗口</span>
show_popup_window = <span class="hljs-literal">False</span>

<span class="hljs-comment"># 获胜者：记录游戏获胜方（玩家或AI）</span>
winner = <span class="hljs-literal">None</span>

<span class="hljs-keyword">class</span> <span class="hljs-title class_">PopupWindow</span>(<span class="hljs-title class_ inherited__">Tk</span>):
    <span class="hljs-string">&quot;&quot;&quot;弹窗类，用于显示游戏结果提示&quot;&quot;&quot;</span>
    
    <span class="hljs-keyword">def</span> <span class="hljs-title function_">__init__</span>(<span class="hljs-params">self</span>):
        <span class="hljs-string">&quot;&quot;&quot;初始化弹窗窗口&quot;&quot;&quot;</span>
        <span class="hljs-comment"># 调用父类Tk的初始化方法</span>
        <span class="hljs-built_in">super</span>().__init__()
        <span class="hljs-comment"># 隐藏主窗口，我们只需要消息框</span>
        <span class="hljs-variable language_">self</span>.wm_withdraw()
    
    <span class="hljs-keyword">def</span> <span class="hljs-title function_">show_message</span>(<span class="hljs-params">self, msg</span>):
        <span class="hljs-string">&quot;&quot;&quot;
        显示消息框
        
        参数:
            msg: 要显示的消息内容
        &quot;&quot;&quot;</span>
        <span class="hljs-comment"># 使用tkinter的messagebox显示提示信息</span>
        <span class="hljs-comment"># &quot;提示！！！&quot;是窗口标题，msg是具体内容</span>
        messagebox.showinfo(<span class="hljs-string">&quot;提示！！！&quot;</span>, msg)

<span class="hljs-keyword">def</span> <span class="hljs-title function_">show_winner_popup</span>():
    <span class="hljs-string">&quot;&quot;&quot;
    显示获胜弹窗的线程函数
    
    这个函数在一个单独的线程中运行，定期检查是否需要显示获胜提示。
    使用多线程可以避免弹窗阻塞游戏主循环。
    &quot;&quot;&quot;</span>
    <span class="hljs-comment"># 声明使用全局变量</span>
    <span class="hljs-keyword">global</span> show_popup_window, winner
    
    <span class="hljs-comment"># 无限循环，持续检查是否需要显示弹窗</span>
    <span class="hljs-keyword">while</span> <span class="hljs-literal">True</span>:
        <span class="hljs-comment"># 使用线程锁，确保安全访问全局变量</span>
        <span class="hljs-keyword">with</span> lock:
            <span class="hljs-comment"># 检查是否需要显示弹窗</span>
            <span class="hljs-keyword">if</span> show_popup_window:
                <span class="hljs-comment"># 创建弹窗对象</span>
                popup = PopupWindow()
                
                <span class="hljs-comment"># 根据获胜者显示不同的消息</span>
                <span class="hljs-keyword">if</span> winner:  <span class="hljs-comment"># winner不为None，表示有获胜者</span>
                    popup.show_message(<span class="hljs-string">f&quot;<span class="hljs-subst">{winner}</span> 获胜!&quot;</span>)
                <span class="hljs-keyword">else</span>:       <span class="hljs-comment"># winner为None，表示平局</span>
                    popup.show_message(<span class="hljs-string">&quot;平局！&quot;</span>)
                
                <span class="hljs-comment"># 重置弹窗标志，避免重复显示</span>
                show_popup_window = <span class="hljs-literal">False</span>
                
                <span class="hljs-comment"># 退出循环（弹窗已经显示）</span>
                <span class="hljs-keyword">break</span>
        
        <span class="hljs-comment"># 暂停0.5秒再检查，避免过度占用CPU</span>
        time.sleep(<span class="hljs-number">0.5</span>)
</code></pre>
<p>最终实现人机对战五子棋完整代码如下，使用 Python 版本为：3.6.8。</p>
<pre class="hljs"><code><span class="hljs-string">&quot;&quot;&quot;
五子棋人机对战游戏
这是一个使用Python的Pygame库实现的五子棋游戏，包含简单的人工智能对手。
主要功能包括：图形化界面、玩家与AI对战、胜负判断、弹窗提示等。
&quot;&quot;&quot;</span>

<span class="hljs-keyword">import</span> pygame
<span class="hljs-keyword">import</span> sys
<span class="hljs-keyword">import</span> time
<span class="hljs-keyword">import</span> threading
<span class="hljs-keyword">from</span> tkinter <span class="hljs-keyword">import</span> Tk, messagebox

<span class="hljs-comment"># ==================== 游戏常量定义 ====================</span>
<span class="hljs-comment"># 这些常量定义了游戏的基本参数，修改它们可以调整游戏的外观和行为</span>

<span class="hljs-comment"># 窗口大小（像素）</span>
WINDOW_SIZE = <span class="hljs-number">736</span>

<span class="hljs-comment"># 棋盘大小（16x16，包含边框）</span>
BOARD_SIZE = <span class="hljs-number">16</span>

<span class="hljs-comment"># 棋子半径（像素）</span>
STONE_SIZE = <span class="hljs-number">15</span>

<span class="hljs-comment"># 总的获胜模式数量（所有可能的五子连珠方向）</span>
<span class="hljs-comment"># 公式解释：棋盘有4个方向（横、竖、两个斜向）</span>
<span class="hljs-comment"># 每个方向在棋盘上可以放置的位置数量</span>
TOTAL_WIN_PATTERNS = <span class="hljs-number">4</span> * (BOARD_SIZE - <span class="hljs-number">4</span>) * (BOARD_SIZE - <span class="hljs-number">2</span>)

<span class="hljs-comment"># 网格间距（每个格子的大小）</span>
GAP = WINDOW_SIZE // BOARD_SIZE

<span class="hljs-comment"># 棋盘上的五个星位点（传统五子棋的标准位置）</span>
<span class="hljs-comment"># 坐标从0开始，对应棋盘上的交叉点</span>
POINTS = [(<span class="hljs-number">2</span>, <span class="hljs-number">2</span>), (<span class="hljs-number">2</span>, <span class="hljs-number">12</span>), (<span class="hljs-number">7</span>, <span class="hljs-number">7</span>), (<span class="hljs-number">12</span>, <span class="hljs-number">2</span>), (<span class="hljs-number">12</span>, <span class="hljs-number">12</span>)]

<span class="hljs-comment"># 颜色定义（使用RGB格式）</span>
COLORS = {
    <span class="hljs-string">&quot;background&quot;</span>: (<span class="hljs-number">240</span>, <span class="hljs-number">217</span>, <span class="hljs-number">181</span>),  <span class="hljs-comment"># 背景色（米黄色）</span>
    <span class="hljs-string">&quot;line&quot;</span>: (<span class="hljs-number">0</span>, <span class="hljs-number">0</span>, <span class="hljs-number">0</span>),              <span class="hljs-comment"># 棋盘线颜色（黑色）</span>
    <span class="hljs-string">&quot;black_stone&quot;</span>: (<span class="hljs-number">0</span>, <span class="hljs-number">0</span>, <span class="hljs-number">0</span>),       <span class="hljs-comment"># 黑棋颜色</span>
    <span class="hljs-string">&quot;white_stone&quot;</span>: (<span class="hljs-number">255</span>, <span class="hljs-number">255</span>, <span class="hljs-number">255</span>), <span class="hljs-comment"># 白棋颜色</span>
}

<span class="hljs-comment"># ==================== 全局变量 ====================</span>
<span class="hljs-comment"># 注意：实际项目中应尽量避免使用全局变量，这里为了简化而使用</span>
<span class="hljs-comment"># 线程锁：用于多线程同步，防止多个线程同时访问共享资源</span>
lock = threading.Lock()

<span class="hljs-comment"># 弹窗状态：标记是否已经弹出获胜提示窗口</span>
show_popup_window = <span class="hljs-literal">False</span>

<span class="hljs-comment"># 获胜者：记录游戏获胜方（玩家或AI）</span>
winner = <span class="hljs-literal">None</span>

<span class="hljs-keyword">class</span> <span class="hljs-title class_">GomokuGame</span>:
    <span class="hljs-string">&quot;&quot;&quot;游戏主类，负责游戏流程控制和界面显示&quot;&quot;&quot;</span>
    
    <span class="hljs-keyword">def</span> <span class="hljs-title function_">__init__</span>(<span class="hljs-params">self</span>):
        <span class="hljs-string">&quot;&quot;&quot;初始化游戏&quot;&quot;&quot;</span>
        pygame.init()  <span class="hljs-comment"># 初始化Pygame所有模块</span>
        
        <span class="hljs-comment"># 创建游戏窗口，大小为WINDOW_SIZE × WINDOW_SIZE</span>
        <span class="hljs-variable language_">self</span>.window = pygame.display.set_mode((WINDOW_SIZE, WINDOW_SIZE))

        pygame.display.set_caption(<span class="hljs-string">&quot;五子棋人机对战&quot;</span>) <span class="hljs-comment"># 设置窗口标题</span>
        
        <span class="hljs-variable language_">self</span>.ai = AI() <span class="hljs-comment"># 创建AI对象</span>
        <span class="hljs-variable language_">self</span>.judge = Judge() <span class="hljs-comment"># 创建裁判对象</span>
        
        <span class="hljs-comment"># 当前回合的棋子颜色，1=黑棋（玩家先手），2=白棋（AI）</span>
        <span class="hljs-variable language_">self</span>.cur_color = <span class="hljs-number">1</span>

        <span class="hljs-variable language_">self</span>.draw_board() <span class="hljs-comment"># 绘制初始棋盘</span>

    <span class="hljs-keyword">def</span> <span class="hljs-title function_">main_loop</span>(<span class="hljs-params">self</span>):
        <span class="hljs-string">&quot;&quot;&quot;游戏主循环，不断处理事件和更新画面&quot;&quot;&quot;</span>
        <span class="hljs-keyword">while</span> <span class="hljs-literal">True</span>:  <span class="hljs-comment"># 无限循环，直到游戏退出</span>
            <span class="hljs-comment"># 获取所有发生的事件（鼠标点击、窗口关闭等）</span>
            <span class="hljs-keyword">for</span> event <span class="hljs-keyword">in</span> pygame.event.get():
                <span class="hljs-comment"># 如果事件是关闭窗口（点击右上角的X）</span>
                <span class="hljs-keyword">if</span> event.<span class="hljs-built_in">type</span> == pygame.QUIT:
                    pygame.quit()  <span class="hljs-comment"># 关闭Pygame</span>
                    sys.exit()     <span class="hljs-comment"># 退出程序</span>
                
                <span class="hljs-comment"># 如果事件是鼠标按钮按下（玩家点击落子）</span>
                <span class="hljs-keyword">elif</span> event.<span class="hljs-built_in">type</span> == pygame.MOUSEBUTTONDOWN:
                    <span class="hljs-comment"># 获取鼠标点击的像素坐标</span>
                    x, y = event.pos
                    
                    <span class="hljs-comment"># 将像素坐标转换为棋盘网格坐标</span>
                    grid_x, grid_y = <span class="hljs-variable language_">self</span>.compute_grid_position(x, y)
                    
                    <span class="hljs-comment"># 处理玩家落子，如果成功返回True</span>
                    ret = <span class="hljs-variable language_">self</span>.make_move(grid_x, grid_y)
                    
                    <span class="hljs-comment"># 如果玩家落子成功，AI进行落子</span>
                    <span class="hljs-keyword">if</span> ret:
                        <span class="hljs-comment"># AI计算最佳落子位置</span>
                        ai_x, ai_y = <span class="hljs-variable language_">self</span>.ai.ai_run(<span class="hljs-variable language_">self</span>.judge.board)
                        
                        <span class="hljs-comment"># 处理AI落子</span>
                        <span class="hljs-variable language_">self</span>.make_move(ai_x, ai_y)
            
            <span class="hljs-comment"># 更新整个游戏窗口的显示</span>
            pygame.display.update()
    
    <span class="hljs-keyword">def</span> <span class="hljs-title function_">make_move</span>(<span class="hljs-params">self, grid_x, grid_y</span>):
        <span class="hljs-string">&quot;&quot;&quot;
        处理棋子落子，包括玩家和AI
        
        参数:
            grid_x: 网格行坐标
            grid_y: 网格列坐标
            
        返回:
            True: 落子成功
            False: 落子失败（位置无效或已有棋子）
        &quot;&quot;&quot;</span>
        <span class="hljs-comment"># 检查坐标是否在有效范围内（1 到 BOARD_SIZE-1）</span>
        <span class="hljs-keyword">if</span> <span class="hljs-number">0</span> &lt; grid_x &lt; BOARD_SIZE <span class="hljs-keyword">and</span> <span class="hljs-number">0</span> &lt; grid_y &lt; BOARD_SIZE:
            <span class="hljs-comment"># 检查这个位置是否为空</span>
            <span class="hljs-keyword">if</span> <span class="hljs-variable language_">self</span>.judge.board[grid_x][grid_y] == <span class="hljs-number">0</span>:
                <span class="hljs-comment"># 在棋盘上绘制棋子</span>
                <span class="hljs-variable language_">self</span>.place_stone(grid_x, grid_y, <span class="hljs-variable language_">self</span>.cur_color)
                
                <span class="hljs-comment"># 更新棋盘状态，并检查是否获胜</span>
                <span class="hljs-variable language_">self</span>.judge.update_board(grid_x, grid_y, <span class="hljs-variable language_">self</span>.cur_color)
                
                <span class="hljs-comment"># 更新AI的获胜模式计数</span>
                <span class="hljs-variable language_">self</span>.ai.update_win_counts(grid_x, grid_y, <span class="hljs-variable language_">self</span>.cur_color)
                
                <span class="hljs-comment"># 切换当前回合：黑棋变白棋，白棋变黑棋</span>
                <span class="hljs-variable language_">self</span>.cur_color = <span class="hljs-number">2</span> <span class="hljs-keyword">if</span> <span class="hljs-variable language_">self</span>.cur_color == <span class="hljs-number">1</span> <span class="hljs-keyword">else</span> <span class="hljs-number">1</span>
                
                <span class="hljs-keyword">return</span> <span class="hljs-literal">True</span>  <span class="hljs-comment"># 落子成功</span>
        
        <span class="hljs-keyword">return</span> <span class="hljs-literal">False</span>  <span class="hljs-comment"># 落子失败</span>

    <span class="hljs-keyword">def</span> <span class="hljs-title function_">place_stone</span>(<span class="hljs-params">self, grid_x, grid_y, color</span>):
        <span class="hljs-string">&quot;&quot;&quot;
        在棋盘上绘制一个棋子
        
        参数:
            grid_x: 网格行坐标
            grid_y: 网格列坐标
            color: 棋子颜色，1=黑棋，2=白棋
        &quot;&quot;&quot;</span>
        <span class="hljs-comment"># 根据颜色选择棋子颜色</span>
        stone_color = COLORS[<span class="hljs-string">&quot;black_stone&quot;</span>] <span class="hljs-keyword">if</span> color == <span class="hljs-number">1</span> <span class="hljs-keyword">else</span> COLORS[<span class="hljs-string">&quot;white_stone&quot;</span>]
        <span class="hljs-comment"># 绘制圆形棋子</span>
        pygame.draw.circle(<span class="hljs-variable language_">self</span>.window, stone_color, (grid_x * GAP, grid_y * GAP), STONE_SIZE)
    
    <span class="hljs-keyword">def</span> <span class="hljs-title function_">compute_grid_position</span>(<span class="hljs-params">self, x, y</span>):
        <span class="hljs-string">&quot;&quot;&quot;
        将鼠标点击的像素坐标转换为棋盘网格坐标
        
        参数:
            x: 像素X坐标
            y: 像素Y坐标
            
        返回:
            (grid_x, grid_y): 网格坐标
        &quot;&quot;&quot;</span>
        <span class="hljs-comment"># 计算最近的网格坐标：像素坐标 ÷ 网格间距，四舍五入</span>
        grid_x = <span class="hljs-built_in">round</span>(x / GAP)
        grid_y = <span class="hljs-built_in">round</span>(y / GAP)
        
        <span class="hljs-comment"># 确保坐标在有效范围内（1到BOARD_SIZE-1）</span>
        grid_x = <span class="hljs-built_in">max</span>(<span class="hljs-number">1</span>, <span class="hljs-built_in">min</span>(BOARD_SIZE - <span class="hljs-number">1</span>, grid_x))
        grid_y = <span class="hljs-built_in">max</span>(<span class="hljs-number">1</span>, <span class="hljs-built_in">min</span>(BOARD_SIZE - <span class="hljs-number">1</span>, grid_y))

        <span class="hljs-keyword">return</span> grid_x, grid_y
    
    <span class="hljs-keyword">def</span> <span class="hljs-title function_">draw_board</span>(<span class="hljs-params">self</span>):
        <span class="hljs-string">&quot;&quot;&quot;绘制棋盘背景、网格线和星位点&quot;&quot;&quot;</span>
        <span class="hljs-comment"># 填充背景颜色</span>
        <span class="hljs-variable language_">self</span>.window.fill(COLORS[<span class="hljs-string">&quot;background&quot;</span>])
        
        <span class="hljs-comment"># 绘制棋盘网格线</span>
        <span class="hljs-keyword">for</span> i <span class="hljs-keyword">in</span> <span class="hljs-built_in">range</span>(BOARD_SIZE):
            <span class="hljs-comment"># 绘制水平线：从左到右</span>
            pygame.draw.line(<span class="hljs-variable language_">self</span>.window, COLORS[<span class="hljs-string">&quot;line&quot;</span>],  <span class="hljs-comment"># 表面, 颜色</span>
                            (GAP, GAP * (i + <span class="hljs-number">1</span>)),          <span class="hljs-comment"># 起点坐标</span>
                            (WINDOW_SIZE - GAP, GAP * (i + <span class="hljs-number">1</span>)),  <span class="hljs-comment"># 终点坐标</span>
                            <span class="hljs-number">1</span>)                             <span class="hljs-comment"># 线宽（像素）</span>
            
            <span class="hljs-comment"># 绘制垂直线：从上到下</span>
            pygame.draw.line(<span class="hljs-variable language_">self</span>.window, COLORS[<span class="hljs-string">&quot;line&quot;</span>],  <span class="hljs-comment"># 表面, 颜色</span>
                            (GAP * (i + <span class="hljs-number">1</span>), GAP),          <span class="hljs-comment"># 起点坐标</span>
                            (GAP * (i + <span class="hljs-number">1</span>), WINDOW_SIZE - GAP),  <span class="hljs-comment"># 终点坐标</span>
                            <span class="hljs-number">1</span>)                             <span class="hljs-comment"># 线宽</span>
            
        <span class="hljs-comment"># 绘制五个星位点（棋盘上的小黑点）</span>
        <span class="hljs-keyword">for</span> point <span class="hljs-keyword">in</span> POINTS:
            <span class="hljs-comment"># 计算星位点的像素坐标</span>
            <span class="hljs-comment"># point[0]和point[1]是网格坐标，需要转换为像素坐标</span>
            <span class="hljs-comment"># 注意：point坐标是0-based，但棋盘有边框，所以要+1</span>
            pixel_x = GAP * (point[<span class="hljs-number">0</span>] + <span class="hljs-number">1</span>)
            pixel_y = GAP * (point[<span class="hljs-number">1</span>] + <span class="hljs-number">1</span>)
            
            <span class="hljs-comment"># 绘制小黑点：表面, 颜色, 圆心坐标, 半径</span>
            pygame.draw.circle(<span class="hljs-variable language_">self</span>.window, COLORS[<span class="hljs-string">&quot;line&quot;</span>], 
                              (pixel_x, pixel_y), <span class="hljs-number">5</span>)

<span class="hljs-keyword">class</span> <span class="hljs-title class_">Judge</span>:
    <span class="hljs-string">&quot;&quot;&quot;裁判类，负责管理棋盘状态和判断胜负&quot;&quot;&quot;</span>
    
    <span class="hljs-keyword">def</span> <span class="hljs-title function_">__init__</span>(<span class="hljs-params">self</span>):
        <span class="hljs-string">&quot;&quot;&quot;初始化棋盘&quot;&quot;&quot;</span>
        <span class="hljs-comment"># 创建棋盘二维数组，所有位置初始化为0（空）</span>
        <span class="hljs-comment"># 棋盘大小：BOARD_SIZE × BOARD_SIZE（第1行和第一列没有使用）</span>
        <span class="hljs-comment"># 0 = 空，1 = 黑棋（玩家），2 = 白棋（AI）</span>
        <span class="hljs-variable language_">self</span>.board = [[<span class="hljs-number">0</span> <span class="hljs-keyword">for</span> _ <span class="hljs-keyword">in</span> <span class="hljs-built_in">range</span>(BOARD_SIZE)] <span class="hljs-keyword">for</span> _ <span class="hljs-keyword">in</span> <span class="hljs-built_in">range</span>(BOARD_SIZE)]
    
    <span class="hljs-keyword">def</span> <span class="hljs-title function_">update_board</span>(<span class="hljs-params">self, x, y, color</span>):
        <span class="hljs-string">&quot;&quot;&quot;
        更新棋盘并检查游戏是否结束
        
        参数:
            x: 落子的行坐标
            y: 落子的列坐标
            color: 棋子颜色，1或2
            
        返回:
            True: 更新成功
            False: 更新失败（传入位置有棋子）
        &quot;&quot;&quot;</span>

        <span class="hljs-comment"># 检查要落子的位置是否为空</span>
        <span class="hljs-keyword">if</span> <span class="hljs-variable language_">self</span>.board[x][y] == <span class="hljs-number">0</span>:
            <span class="hljs-comment"># 在棋盘上放置棋子</span>
            <span class="hljs-variable language_">self</span>.board[x][y] = color
            
            <span class="hljs-comment"># 检查是否获胜（五子连珠）</span>
            <span class="hljs-keyword">if</span> <span class="hljs-variable language_">self</span>.check_win(x, y):
                <span class="hljs-comment"># 使用全局变量记录获胜信息</span>
                <span class="hljs-keyword">global</span> show_popup_window, winner
                <span class="hljs-keyword">with</span> lock: <span class="hljs-comment"># 加锁，确保线程安全</span>
                    show_popup_window = <span class="hljs-literal">True</span> <span class="hljs-comment"># 设置弹窗标志</span>

                    <span class="hljs-comment"># 确定获胜者：1=玩家，2=AI</span>
                    winner = <span class="hljs-string">&quot;玩家 (黑色)&quot;</span> <span class="hljs-keyword">if</span> color == <span class="hljs-number">1</span> <span class="hljs-keyword">else</span> <span class="hljs-string">&quot;AI (白色)&quot;</span>

            <span class="hljs-comment"># 检查是否平局（棋盘满了）</span>
            <span class="hljs-keyword">if</span> <span class="hljs-variable language_">self</span>.is_full():
                show_popup_window = <span class="hljs-literal">True</span>

            <span class="hljs-comment"># 返回更新成功</span>
            <span class="hljs-keyword">return</span> <span class="hljs-literal">True</span>
        <span class="hljs-comment"># 位置已有棋子，更新失败</span>
        <span class="hljs-keyword">return</span> <span class="hljs-literal">False</span>
    
    <span class="hljs-keyword">def</span> <span class="hljs-title function_">check_win</span>(<span class="hljs-params">self, x, y</span>):
        <span class="hljs-string">&quot;&quot;&quot;
        检查是否五子连珠（获胜条件）
        
        参数:
            x: 最后落子的行坐标
            y: 最后落子的列坐标
            
        返回:
            True: 有五子连珠，获胜
            False: 没有五子连珠
        &quot;&quot;&quot;</span>
        <span class="hljs-comment"># 四个检查方向：(行增量, 列增量)</span>
        directions = [(<span class="hljs-number">1</span>, <span class="hljs-number">0</span>),   <span class="hljs-comment"># 水平方向（右/左）</span>
                      (<span class="hljs-number">0</span>, <span class="hljs-number">1</span>),   <span class="hljs-comment"># 垂直方向（下/上）</span>
                      (<span class="hljs-number">1</span>, <span class="hljs-number">1</span>),   <span class="hljs-comment"># 右下/左上对角线</span>
                      (<span class="hljs-number">1</span>, -<span class="hljs-number">1</span>)]  <span class="hljs-comment"># 左下/右上对角线</span>
        
        <span class="hljs-comment"># 获取最后落子的颜色</span>
        cur_color = <span class="hljs-variable language_">self</span>.board[x][y]
        
        <span class="hljs-comment"># 检查每个方向</span>
        <span class="hljs-keyword">for</span> dx, dy <span class="hljs-keyword">in</span> directions:
            stone_count = <span class="hljs-number">1</span>  <span class="hljs-comment"># 从当前棋子开始计数，初始为1</span>
            
            <span class="hljs-comment"># 向两个方向检查：正向和反向</span>
            <span class="hljs-keyword">for</span> sign <span class="hljs-keyword">in</span> (<span class="hljs-number">1</span>, -<span class="hljs-number">1</span>):
                <span class="hljs-comment"># 从当前位置向指定方向移动一步</span>
                cur_x, cur_y = x + dx * sign, y + dy * sign
                
                <span class="hljs-comment"># 沿着这个方向连续检查相同颜色的棋子</span>
                <span class="hljs-keyword">while</span> (<span class="hljs-number">0</span> &lt; cur_x &lt; BOARD_SIZE <span class="hljs-keyword">and</span>      <span class="hljs-comment"># 检查行坐标是否在边界内</span>
                       <span class="hljs-number">0</span> &lt; cur_y &lt; BOARD_SIZE <span class="hljs-keyword">and</span>      <span class="hljs-comment"># 检查列坐标是否在边界内</span>
                       <span class="hljs-variable language_">self</span>.board[cur_x][cur_y] == cur_color):  <span class="hljs-comment"># 检查颜色是否相同</span>
                    stone_count += <span class="hljs-number">1</span>  <span class="hljs-comment"># 发现相同颜色棋子，计数加1</span>
                    <span class="hljs-comment"># 继续向同一方向移动，检查下一个位置</span>
                    cur_x += dx * sign
                    cur_y += dy * sign
            
            <span class="hljs-comment"># 如果连续相同颜色的棋子数达到5个，获胜！</span>
            <span class="hljs-keyword">if</span> stone_count &gt;= <span class="hljs-number">5</span>:
                <span class="hljs-keyword">return</span> <span class="hljs-literal">True</span>
        
        <span class="hljs-comment"># 所有方向都检查完毕，没有找到五子连珠</span>
        <span class="hljs-keyword">return</span> <span class="hljs-literal">False</span>
    
    <span class="hljs-keyword">def</span> <span class="hljs-title function_">is_full</span>(<span class="hljs-params">self</span>):
        <span class="hljs-string">&quot;&quot;&quot;
        检查棋盘是否已满（平局条件）
        
        返回:
            True: 棋盘已满，平局
            False: 棋盘还有空位
        &quot;&quot;&quot;</span>
        <span class="hljs-comment"># 遍历棋盘的每一行</span>
        <span class="hljs-keyword">for</span> row <span class="hljs-keyword">in</span> <span class="hljs-variable language_">self</span>.board:
            <span class="hljs-comment"># 检查这一行是否还有空位（0表示空位）</span>
            <span class="hljs-keyword">if</span> <span class="hljs-number">0</span> <span class="hljs-keyword">in</span> row:
                <span class="hljs-keyword">return</span> <span class="hljs-literal">False</span>  <span class="hljs-comment"># 发现空位，棋盘未满</span>
        
        <span class="hljs-comment"># 所有位置都非空，棋盘已满</span>
        <span class="hljs-keyword">return</span> <span class="hljs-literal">True</span>

<span class="hljs-keyword">class</span> <span class="hljs-title class_">AI</span>:
    <span class="hljs-string">&quot;&quot;&quot;人工智能类，负责AI的下棋逻辑&quot;&quot;&quot;</span>
    
    <span class="hljs-keyword">def</span> <span class="hljs-title function_">__init__</span>(<span class="hljs-params">self</span>):
        <span class="hljs-string">&quot;&quot;&quot;初始化AI&quot;&quot;&quot;</span>
        <span class="hljs-comment"># 三维数组：记录每个棋盘位置属于哪些获胜模式</span>
        <span class="hljs-comment"># win_patterns[x][y][k] = True 表示位置(x,y)属于第k个获胜模式</span>
        <span class="hljs-variable language_">self</span>.win_patterns = [[[<span class="hljs-literal">False</span> <span class="hljs-keyword">for</span> _ <span class="hljs-keyword">in</span> <span class="hljs-built_in">range</span>(TOTAL_WIN_PATTERNS)] 
                     <span class="hljs-keyword">for</span> _ <span class="hljs-keyword">in</span> <span class="hljs-built_in">range</span>(BOARD_SIZE)] 
                     <span class="hljs-keyword">for</span> _ <span class="hljs-keyword">in</span> <span class="hljs-built_in">range</span>(BOARD_SIZE)]
        
        <span class="hljs-comment"># 记录每个获胜模式中AI已占有的棋子数</span>
        <span class="hljs-variable language_">self</span>.ai_win_count = [<span class="hljs-number">0</span> <span class="hljs-keyword">for</span> _ <span class="hljs-keyword">in</span> <span class="hljs-built_in">range</span>(TOTAL_WIN_PATTERNS)]
        
        <span class="hljs-comment"># 记录每个获胜模式中玩家已占有的棋子数</span>
        <span class="hljs-variable language_">self</span>.human_win_count = [<span class="hljs-number">0</span> <span class="hljs-keyword">for</span> _ <span class="hljs-keyword">in</span> <span class="hljs-built_in">range</span>(TOTAL_WIN_PATTERNS)]
        
        <span class="hljs-comment"># 当前已记录的获胜模式数量</span>
        <span class="hljs-variable language_">self</span>.win_pattern_count = <span class="hljs-number">0</span>
        
        <span class="hljs-comment"># 玩家的得分权重：不同长度的连珠对应不同的分数</span>
        <span class="hljs-comment"># 键：连珠长度，值：对应的分数</span>
        <span class="hljs-variable language_">self</span>.human_score_weights = {
            <span class="hljs-number">1</span>: <span class="hljs-number">200</span>,    <span class="hljs-comment"># 单独一子</span>
            <span class="hljs-number">2</span>: <span class="hljs-number">400</span>,    <span class="hljs-comment"># 两子连珠</span>
            <span class="hljs-number">3</span>: <span class="hljs-number">2000</span>,   <span class="hljs-comment"># 三子连珠</span>
            <span class="hljs-number">4</span>: <span class="hljs-number">10000</span>,  <span class="hljs-comment"># 四子连珠（差一子获胜）</span>
        }
        
        <span class="hljs-comment"># AI的得分权重（略高于玩家，使AI更具攻击性）</span>
        <span class="hljs-variable language_">self</span>.ai_score_weights = {
            <span class="hljs-number">1</span>: <span class="hljs-number">220</span>,    <span class="hljs-comment"># 比玩家略高</span>
            <span class="hljs-number">2</span>: <span class="hljs-number">420</span>,
            <span class="hljs-number">3</span>: <span class="hljs-number">2100</span>,
            <span class="hljs-number">4</span>: <span class="hljs-number">20000</span>,  <span class="hljs-comment"># 四子连珠得分远高于玩家</span>
        }
        
        <span class="hljs-comment"># 初始化所有可能的获胜模式</span>
        <span class="hljs-variable language_">self</span>.init_win_patterns()
    
    <span class="hljs-keyword">def</span> <span class="hljs-title function_">add_win_pattern</span>(<span class="hljs-params">self, start_i, start_j, di, dj</span>):
        <span class="hljs-string">&quot;&quot;&quot;
        添加一个获胜模式
        
        参数:
            start_i: 起始行
            start_j: 起始列
            di: 行方向增量
            dj: 列方向增量（与di配合定义方向）
        &quot;&quot;&quot;</span>
        <span class="hljs-comment"># 一个获胜模式包含连续的5个位置</span>
        <span class="hljs-keyword">for</span> k <span class="hljs-keyword">in</span> <span class="hljs-built_in">range</span>(<span class="hljs-number">5</span>):
            <span class="hljs-comment"># 标记这个获胜模式包含的所有位置</span>
            <span class="hljs-variable language_">self</span>.win_patterns[start_i + k * di][start_j + k * dj][<span class="hljs-variable language_">self</span>.win_pattern_count] = <span class="hljs-literal">True</span>
        
        <span class="hljs-comment"># 获胜模式计数器加1</span>
        <span class="hljs-variable language_">self</span>.win_pattern_count += <span class="hljs-number">1</span>

    <span class="hljs-keyword">def</span> <span class="hljs-title function_">init_win_patterns</span>(<span class="hljs-params">self</span>):
        <span class="hljs-string">&quot;&quot;&quot;初始化所有可能的获胜模式（所有可能的五子连珠位置）&quot;&quot;&quot;</span>

        <span class="hljs-comment"># 横向和纵向的所有赢法</span>
        <span class="hljs-keyword">for</span> i <span class="hljs-keyword">in</span> <span class="hljs-built_in">range</span>(<span class="hljs-number">1</span>, BOARD_SIZE):
            <span class="hljs-keyword">for</span> j <span class="hljs-keyword">in</span> <span class="hljs-built_in">range</span>(<span class="hljs-number">1</span>, BOARD_SIZE - <span class="hljs-number">4</span>):
                <span class="hljs-comment"># 水平方向的获胜模式</span>
                <span class="hljs-variable language_">self</span>.add_win_pattern(i, j, <span class="hljs-number">0</span>, <span class="hljs-number">1</span>)
                <span class="hljs-comment"># 垂直方向的获胜模式</span>
                <span class="hljs-variable language_">self</span>.add_win_pattern(j, i, <span class="hljs-number">1</span>, <span class="hljs-number">0</span>)
        
        <span class="hljs-comment"># 对角线方向的所有赢法</span>
        <span class="hljs-keyword">for</span> i <span class="hljs-keyword">in</span> <span class="hljs-built_in">range</span>(<span class="hljs-number">1</span>, BOARD_SIZE - <span class="hljs-number">4</span>):
            <span class="hljs-keyword">for</span> j <span class="hljs-keyword">in</span> <span class="hljs-built_in">range</span>(<span class="hljs-number">1</span>, BOARD_SIZE - <span class="hljs-number">4</span>):
                <span class="hljs-comment"># 右下对角线获胜模式</span>
                <span class="hljs-variable language_">self</span>.add_win_pattern(i, j, <span class="hljs-number">1</span>, <span class="hljs-number">1</span>)
                <span class="hljs-comment"># 左下对角线获胜模式</span>
                <span class="hljs-variable language_">self</span>.add_win_pattern(i, BOARD_SIZE - j, <span class="hljs-number">1</span>, -<span class="hljs-number">1</span>)
    
    <span class="hljs-keyword">def</span> <span class="hljs-title function_">update_win_counts</span>(<span class="hljs-params">self, x, y, color</span>):
        <span class="hljs-string">&quot;&quot;&quot;
        更新获胜模式计数（当棋子落下时调用）
        
        参数:
            x: 行坐标
            y: 列坐标
            color: 棋子颜色（1:玩家/黑棋，2:AI/白棋）
        &quot;&quot;&quot;</span>
        <span class="hljs-comment"># 遍历所有获胜模式</span>
        <span class="hljs-keyword">for</span> k <span class="hljs-keyword">in</span> <span class="hljs-built_in">range</span>(TOTAL_WIN_PATTERNS):
            <span class="hljs-comment"># 检查这个位置是否属于第k个获胜模式</span>
            <span class="hljs-keyword">if</span> <span class="hljs-variable language_">self</span>.win_patterns[x][y][k]:
                <span class="hljs-keyword">if</span> color == <span class="hljs-number">2</span>:  <span class="hljs-comment"># AI下棋（白棋）</span>
                    <span class="hljs-comment"># AI在这个获胜模式中增加一子</span>
                    <span class="hljs-variable language_">self</span>.ai_win_count[k] += <span class="hljs-number">1</span>
                    <span class="hljs-comment"># 玩家在这个模式中不可能获胜了（设置为异常值6，超过5）</span>
                    <span class="hljs-variable language_">self</span>.human_win_count[k] = <span class="hljs-number">6</span>
                <span class="hljs-keyword">else</span>:  <span class="hljs-comment"># 玩家下棋（黑棋）</span>
                    <span class="hljs-comment"># 玩家在这个获胜模式中增加一子</span>
                    <span class="hljs-variable language_">self</span>.human_win_count[k] += <span class="hljs-number">1</span>
                    <span class="hljs-comment"># AI在这个模式中不可能获胜了</span>
                    <span class="hljs-variable language_">self</span>.ai_win_count[k] = <span class="hljs-number">6</span>

    <span class="hljs-keyword">def</span> <span class="hljs-title function_">evaluate_position</span>(<span class="hljs-params">self, human_score, ai_score</span>):
        <span class="hljs-string">&quot;&quot;&quot;
        评估位置的得分（综合进攻和防守）
        
        参数:
            human_score: 玩家在这个位置的得分
            ai_score: AI在这个位置的得分
        
        返回:
            综合得分（AI进攻和防守玩家的加权和）
        &quot;&quot;&quot;</span>
        OFFENSIVE_WEIGHT = <span class="hljs-number">1.2</span>  <span class="hljs-comment"># 进攻权重：鼓励AI积极进攻</span>
        DEFENSIVE_WEIGHT = <span class="hljs-number">1.0</span>  <span class="hljs-comment"># 防守权重：阻止玩家连成五子</span>

        <span class="hljs-comment"># 综合得分 = AI进攻得分 * 进攻权重 + 防守玩家得分 * 防守权重</span>
        <span class="hljs-keyword">return</span> ai_score * OFFENSIVE_WEIGHT + human_score * DEFENSIVE_WEIGHT

    <span class="hljs-keyword">def</span> <span class="hljs-title function_">ai_run</span>(<span class="hljs-params">self, board</span>):
        <span class="hljs-string">&quot;&quot;&quot;
        AI主逻辑：选择最佳落子位置
        
        参数:
            board: 当前棋盘状态
        
        返回:
            (best_i, best_j): 最佳落子位置的行列坐标
        &quot;&quot;&quot;</span>
        <span class="hljs-comment"># 初始化最佳位置和最佳得分</span>
        best_pos = (<span class="hljs-number">0</span>, <span class="hljs-number">0</span>)  <span class="hljs-comment"># 最佳位置（默认左上角）</span>
        best_score = -<span class="hljs-number">1</span>    <span class="hljs-comment"># 最佳得分（初始为-1）</span>
        
        <span class="hljs-comment"># 遍历棋盘上的所有位置</span>
        <span class="hljs-keyword">for</span> i <span class="hljs-keyword">in</span> <span class="hljs-built_in">range</span>(<span class="hljs-number">1</span>, BOARD_SIZE):
            <span class="hljs-keyword">for</span> j <span class="hljs-keyword">in</span> <span class="hljs-built_in">range</span>(<span class="hljs-number">1</span>, BOARD_SIZE):
                <span class="hljs-comment"># 如果这个位置是空的</span>
                <span class="hljs-keyword">if</span> board[i][j] == <span class="hljs-number">0</span>:
                    <span class="hljs-comment"># 重置当前得分</span>
                    cur_human_score, cur_ai_score = <span class="hljs-number">0</span>, <span class="hljs-number">0</span>
                    
                    <span class="hljs-comment"># 遍历所有获胜模式，计算这个位置的得分</span>
                    <span class="hljs-keyword">for</span> k <span class="hljs-keyword">in</span> <span class="hljs-built_in">range</span>(TOTAL_WIN_PATTERNS):
                        <span class="hljs-comment"># 如果这个位置属于第k个获胜模式</span>
                        <span class="hljs-keyword">if</span> <span class="hljs-variable language_">self</span>.win_patterns[i][j][k]:
                            <span class="hljs-comment"># 累加玩家在这个模式下的得分</span>
                            cur_human_score += <span class="hljs-variable language_">self</span>.human_score_weights.get(
                                <span class="hljs-variable language_">self</span>.human_win_count[k], <span class="hljs-number">0</span>)
                            <span class="hljs-comment"># 累加AI在这个模式下的得分</span>
                            cur_ai_score += <span class="hljs-variable language_">self</span>.ai_score_weights.get(
                                <span class="hljs-variable language_">self</span>.ai_win_count[k], <span class="hljs-number">0</span>)
                    
                    <span class="hljs-comment"># 计算综合得分</span>
                    cur_score = <span class="hljs-variable language_">self</span>.evaluate_position(cur_human_score, cur_ai_score)
                    
                    <span class="hljs-comment"># 如果当前得分更好，更新最佳位置</span>
                    <span class="hljs-keyword">if</span> cur_score &gt;= best_score:
                        best_score = cur_score
                        best_pos = (i, j)
        
        <span class="hljs-comment"># 返回最佳落子位置</span>
        <span class="hljs-keyword">return</span> best_pos

<span class="hljs-keyword">class</span> <span class="hljs-title class_">PopupWindow</span>(<span class="hljs-title class_ inherited__">Tk</span>):
    <span class="hljs-string">&quot;&quot;&quot;弹窗类，用于显示游戏结果提示&quot;&quot;&quot;</span>
    
    <span class="hljs-keyword">def</span> <span class="hljs-title function_">__init__</span>(<span class="hljs-params">self</span>):
        <span class="hljs-string">&quot;&quot;&quot;初始化弹窗窗口&quot;&quot;&quot;</span>
        <span class="hljs-comment"># 调用父类Tk的初始化方法</span>
        <span class="hljs-built_in">super</span>().__init__()
        <span class="hljs-comment"># 隐藏主窗口，我们只需要消息框</span>
        <span class="hljs-variable language_">self</span>.wm_withdraw()
    
    <span class="hljs-keyword">def</span> <span class="hljs-title function_">show_message</span>(<span class="hljs-params">self, msg</span>):
        <span class="hljs-string">&quot;&quot;&quot;
        显示消息框
        
        参数:
            msg: 要显示的消息内容
        &quot;&quot;&quot;</span>
        <span class="hljs-comment"># 使用tkinter的messagebox显示提示信息</span>
        <span class="hljs-comment"># &quot;提示！！！&quot;是窗口标题，msg是具体内容</span>
        messagebox.showinfo(<span class="hljs-string">&quot;提示！！！&quot;</span>, msg)


<span class="hljs-keyword">def</span> <span class="hljs-title function_">show_winner_popup</span>():
    <span class="hljs-string">&quot;&quot;&quot;
    显示获胜弹窗的线程函数
    
    这个函数在一个单独的线程中运行，定期检查是否需要显示获胜提示。
    使用多线程可以避免弹窗阻塞游戏主循环。
    &quot;&quot;&quot;</span>
    <span class="hljs-comment"># 声明使用全局变量</span>
    <span class="hljs-keyword">global</span> show_popup_window, winner
    
    <span class="hljs-comment"># 无限循环，持续检查是否需要显示弹窗</span>
    <span class="hljs-keyword">while</span> <span class="hljs-literal">True</span>:
        <span class="hljs-comment"># 使用线程锁，确保安全访问全局变量</span>
        <span class="hljs-keyword">with</span> lock:
            <span class="hljs-comment"># 检查是否需要显示弹窗</span>
            <span class="hljs-keyword">if</span> show_popup_window:
                <span class="hljs-comment"># 创建弹窗对象</span>
                popup = PopupWindow()
                
                <span class="hljs-comment"># 根据获胜者显示不同的消息</span>
                <span class="hljs-keyword">if</span> winner:  <span class="hljs-comment"># winner不为None，表示有获胜者</span>
                    popup.show_message(<span class="hljs-string">f&quot;<span class="hljs-subst">{winner}</span> 获胜!&quot;</span>)
                <span class="hljs-keyword">else</span>:       <span class="hljs-comment"># winner为None，表示平局</span>
                    popup.show_message(<span class="hljs-string">&quot;平局！&quot;</span>)
                
                <span class="hljs-comment"># 重置弹窗标志，避免重复显示</span>
                show_popup_window = <span class="hljs-literal">False</span>
                
                <span class="hljs-comment"># 退出循环（弹窗已经显示）</span>
                <span class="hljs-keyword">break</span>
        
        <span class="hljs-comment"># 暂停0.5秒再检查，避免过度占用CPU</span>
        time.sleep(<span class="hljs-number">0.5</span>)

<span class="hljs-keyword">def</span> <span class="hljs-title function_">main</span>():
    <span class="hljs-string">&quot;&quot;&quot;程序主入口函数&quot;&quot;&quot;</span>
    <span class="hljs-comment"># 创建游戏对象</span>
    game = GomokuGame()
    
    <span class="hljs-comment"># 创建并启动弹窗线程</span>
    <span class="hljs-comment"># target: 线程要执行的函数</span>
    <span class="hljs-comment"># daemon=True: 设置为守护线程，主程序退出时自动结束</span>
    popup_thread = threading.Thread(target=show_winner_popup, daemon=<span class="hljs-literal">True</span>)
    popup_thread.start()  <span class="hljs-comment"># 启动线程</span>
    
    <span class="hljs-comment"># 开始游戏主循环</span>
    game.main_loop()

<span class="hljs-comment"># Python程序的入口点</span>
<span class="hljs-comment"># 当直接运行这个文件时，__name__等于&quot;__main__&quot;</span>
<span class="hljs-comment"># 当被导入为模块时，__name__等于模块名</span>
<span class="hljs-keyword">if</span> __name__ == <span class="hljs-string">&quot;__main__&quot;</span>:
    main() <span class="hljs-comment"># 调用主函数开始游戏</span>
</code></pre>
]]></content>
        <author>
            <name>Guanngxu</name>
        </author>
    </entry>
    <entry>
        <title type="html"><![CDATA[哈夫曼树实现文件压缩与解压缩]]></title>
        <id>https://www.guanngxu.com/LHd4yqncdM/</id>
        <link href="https://www.guanngxu.com/LHd4yqncdM/"/>
        <updated>2025-11-09T07:57:34.000Z</updated>
        <summary type="html"><![CDATA[
参考内容：
哈夫曼树和哈夫曼编码, 看完秒懂！
哈夫曼实现文件压缩解压缩（c语言）

#include&lt;bits/stdc++.h&gt;
using namespace std;

#define BUFFER_SIZE 1024...]]></summary>
        <content type="html"><![CDATA[<blockquote>
<p>参考内容：</p>
<p><a href="https://www.bilibili.com/video/BV1qu411F7Zs/">哈夫曼树和哈夫曼编码, 看完秒懂！</a></p>
<p><a href="https://cloud.tencent.com/developer/article/2069891">哈夫曼实现文件压缩解压缩（c语言）</a></p>
</blockquote>
<pre class="hljs"><code><span class="hljs-meta">#<span class="hljs-keyword">include</span><span class="hljs-string">&lt;bits/stdc++.h&gt;</span></span>
<span class="hljs-keyword">using</span> <span class="hljs-keyword">namespace</span> std;

<span class="hljs-meta">#<span class="hljs-keyword">define</span> BUFFER_SIZE 1024</span>
<span class="hljs-meta">#<span class="hljs-keyword">define</span> CHAR_COUNT_LEN 256</span>

<span class="hljs-comment">// 哈夫曼树的结点结构 </span>
<span class="hljs-keyword">struct</span> <span class="hljs-title class_">huffman_node</span> {
	<span class="hljs-type">size_t</span> times; <span class="hljs-comment">// 本字符出现的次数 </span>
	<span class="hljs-type">unsigned</span> <span class="hljs-type">char</span> val; <span class="hljs-comment">// 字符的值 </span>
	huffman_node *left; <span class="hljs-comment">// 左孩子 </span>
	huffman_node *right; <span class="hljs-comment">// 右孩子 </span>
};

<span class="hljs-type">size_t</span> char_count_info[CHAR_COUNT_LEN] = {}; <span class="hljs-comment">// 统计出现字符频率</span>
vector&lt;huffman_node *&gt; huffman_tree_nodes; <span class="hljs-comment">// 动态数组用于存储哈夫曼树的结点</span>
huffman_node *huffman_tree_root = <span class="hljs-literal">NULL</span>; <span class="hljs-comment">// 哈夫曼树的根结点 </span>
map&lt;<span class="hljs-type">unsigned</span> <span class="hljs-type">char</span>, string&gt; huffman_codes_table; <span class="hljs-comment">// 用于存储哈夫曼编码</span>

<span class="hljs-comment">// 打印哈夫曼编码表</span>
<span class="hljs-function"><span class="hljs-type">void</span> <span class="hljs-title">print_huffman_codes_table</span><span class="hljs-params">(map&lt;<span class="hljs-type">unsigned</span> <span class="hljs-type">char</span>, string&gt; huffman_codes_table)</span> </span>{
    <span class="hljs-built_in">printf</span>(<span class="hljs-string">&quot;the huffman codes is:\n&quot;</span>);
    <span class="hljs-keyword">for</span>(map&lt;<span class="hljs-type">unsigned</span> <span class="hljs-type">char</span>, string&gt;::iterator it = huffman_codes_table.<span class="hljs-built_in">begin</span>(); 
		it != huffman_codes_table.<span class="hljs-built_in">end</span>(); it++) {
        <span class="hljs-built_in">printf</span>(<span class="hljs-string">&quot;character &#x27;%c&#x27;(%d) : %s\n&quot;</span>, 
               (it-&gt;first &gt;= <span class="hljs-number">32</span> &amp;&amp; it-&gt;first &lt;= <span class="hljs-number">126</span>) ? it-&gt;first : <span class="hljs-string">&#x27;?&#x27;</span>, 
               it-&gt;first, it-&gt;second.<span class="hljs-built_in">c_str</span>());
    }
    <span class="hljs-built_in">printf</span>(<span class="hljs-string">&quot;\n&quot;</span>);
}

<span class="hljs-comment">// 生成哈夫曼编码表（递归函数）</span>
<span class="hljs-function"><span class="hljs-type">void</span> <span class="hljs-title">generate_huffman_codes_table</span><span class="hljs-params">(huffman_node* root, string code)</span> </span>{
    <span class="hljs-keyword">if</span>(root == <span class="hljs-literal">NULL</span>) <span class="hljs-keyword">return</span>;
    
    <span class="hljs-comment">// 如果是叶子节点，保存编码</span>
    <span class="hljs-keyword">if</span>(root-&gt;left == <span class="hljs-literal">NULL</span> &amp;&amp; root-&gt;right == <span class="hljs-literal">NULL</span>) {
        huffman_codes_table[root-&gt;val] = code;
    } <span class="hljs-keyword">else</span> {
    	<span class="hljs-comment">// 递归处理左右子树</span>
	    <span class="hljs-built_in">generate_huffman_codes_table</span>(root-&gt;left, code + <span class="hljs-string">&quot;0&quot;</span>);
	    <span class="hljs-built_in">generate_huffman_codes_table</span>(root-&gt;right, code + <span class="hljs-string">&quot;1&quot;</span>);
	}
}

<span class="hljs-comment">// 排序辅助函数 </span>
<span class="hljs-function"><span class="hljs-type">bool</span> <span class="hljs-title">cmp</span><span class="hljs-params">(huffman_node *a,huffman_node *b)</span></span>{
	<span class="hljs-keyword">return</span> a-&gt;times &gt; b-&gt;times;
}

<span class="hljs-comment">// 创建哈弗曼树 </span>
<span class="hljs-function"><span class="hljs-type">int</span> <span class="hljs-title">generate_huffman_tree</span><span class="hljs-params">()</span> </span>{
	<span class="hljs-built_in">printf</span>(<span class="hljs-string">&quot;begin generate huffman tree......\n&quot;</span>);
	<span class="hljs-keyword">while</span>(huffman_tree_nodes.<span class="hljs-built_in">size</span>() &gt; <span class="hljs-number">1</span>) {
		<span class="hljs-comment">// 按照字符出现频率进行降序排序 </span>
		<span class="hljs-built_in">sort</span>(huffman_tree_nodes.<span class="hljs-built_in">begin</span>(), huffman_tree_nodes.<span class="hljs-built_in">end</span>(), cmp);
		
		<span class="hljs-comment">// 取出字符出现频率最小的两个 </span>
		huffman_node *min1 = huffman_tree_nodes.<span class="hljs-built_in">back</span>();
		huffman_tree_nodes.<span class="hljs-built_in">pop_back</span>();
		huffman_node *min2 = huffman_tree_nodes.<span class="hljs-built_in">back</span>();
		huffman_tree_nodes.<span class="hljs-built_in">pop_back</span>();
		
		<span class="hljs-comment">// 生成新的父亲结点，频率最小的结点作为左孩子 </span>
		huffman_node *cur = (huffman_node *) <span class="hljs-built_in">malloc</span>(<span class="hljs-built_in">sizeof</span>(huffman_node));
		cur-&gt;times = min1-&gt;times + min2-&gt;times;
		cur-&gt;left = min1;
		cur-&gt;right = min2;
		
		huffman_tree_nodes.<span class="hljs-built_in">push_back</span>(cur); 
		huffman_tree_root = cur; 
	}
	<span class="hljs-built_in">printf</span>(<span class="hljs-string">&quot;success generate huffman tree\n\n&quot;</span>);
	<span class="hljs-keyword">return</span> <span class="hljs-number">0</span>;
}



<span class="hljs-comment">// 逐一生成哈夫曼树结点，并存储于全局变量 tree_nodes 中 </span>
<span class="hljs-function"><span class="hljs-type">int</span> <span class="hljs-title">generate_huffman_tree_nodes</span><span class="hljs-params">()</span>
</span>{
	<span class="hljs-built_in">printf</span>(<span class="hljs-string">&quot;begin generate huffman tree nodes......\n&quot;</span>);
	
	<span class="hljs-type">int</span> nodes_count = <span class="hljs-number">0</span>; <span class="hljs-comment">// 统计生成了多少个结点 </span>
	<span class="hljs-keyword">for</span>(<span class="hljs-type">int</span> i = <span class="hljs-number">0</span>; i &lt; CHAR_COUNT_LEN; i++) {
		<span class="hljs-comment">// 出现次数为 0 的字符不需要生成结点 </span>
		<span class="hljs-keyword">if</span>(char_count_info[i] &gt; <span class="hljs-number">0</span>) {
			nodes_count++;
			huffman_node *cur = (huffman_node *) <span class="hljs-built_in">malloc</span>(<span class="hljs-built_in">sizeof</span>(huffman_node));
			cur-&gt;val = i;
			cur-&gt;times = char_count_info[i];
			cur-&gt;left = <span class="hljs-literal">NULL</span>;
			cur-&gt;right = <span class="hljs-literal">NULL</span>;
			
			huffman_tree_nodes.<span class="hljs-built_in">push_back</span>(cur);
		} 
	}
	<span class="hljs-built_in">printf</span>(<span class="hljs-string">&quot;success generate %d huffman tree nodes\n\n&quot;</span>, nodes_count);
	<span class="hljs-keyword">return</span> <span class="hljs-number">0</span>; 
} 

<span class="hljs-comment">// 用于统计每个字符在文件出现的次数</span>
<span class="hljs-comment">// 统计结果存储在全局变量 char_count_info 中 </span>
<span class="hljs-function"><span class="hljs-type">int</span> <span class="hljs-title">count_char</span><span class="hljs-params">(<span class="hljs-type">unsigned</span> <span class="hljs-type">char</span> buffer[], <span class="hljs-type">size_t</span> len)</span>
</span>{
	<span class="hljs-keyword">for</span>(<span class="hljs-type">size_t</span> i = <span class="hljs-number">0</span>; i &lt; len; i++) {
		char_count_info[buffer[i]]++;
	}
	<span class="hljs-keyword">return</span> <span class="hljs-number">0</span>;
}

<span class="hljs-comment">// 读取指定文件内容，并统计字符出现的次数 </span>
<span class="hljs-function"><span class="hljs-type">int</span> <span class="hljs-title">count_file_char</span><span class="hljs-params">(<span class="hljs-type">char</span> *file_name)</span>
</span>{
	FILE *file; <span class="hljs-comment">// 打开文件句柄  </span>
    
    file = <span class="hljs-built_in">fopen</span>(file_name, <span class="hljs-string">&quot;rb&quot;</span>);
    <span class="hljs-keyword">if</span> (file == <span class="hljs-literal">NULL</span>) {
        <span class="hljs-built_in">printf</span>(<span class="hljs-string">&quot;can&#x27;t open file [%s], please check file name!\n&quot;</span>, file_name);
        <span class="hljs-keyword">return</span> <span class="hljs-number">-1</span>;
    }
    
    <span class="hljs-type">unsigned</span> <span class="hljs-type">char</span> buffer[BUFFER_SIZE] = {}; <span class="hljs-comment">// 读取文件时的缓冲区 </span>
    <span class="hljs-type">size_t</span> bytes_read = <span class="hljs-number">0</span>; <span class="hljs-comment">// 每次读了多少个字节 </span>
    <span class="hljs-type">size_t</span> read_times_count = <span class="hljs-number">1</span>; <span class="hljs-comment">// 总共读取了多少次了</span>
    
    <span class="hljs-comment">// 循环读取直到文件结束，1 表示每次读取的数据块大小，单位为字节 </span>
    <span class="hljs-keyword">while</span> ((bytes_read = <span class="hljs-built_in">fread</span>(buffer, <span class="hljs-number">1</span>, BUFFER_SIZE, file)) &gt; <span class="hljs-number">0</span>) {
        <span class="hljs-built_in">printf</span>(<span class="hljs-string">&quot;the %zu th read, read %zu byte! process......\n&quot;</span>, 
				read_times_count, bytes_read);
        <span class="hljs-built_in">count_char</span>(buffer, bytes_read);
        read_times_count++;
    }
    
    <span class="hljs-built_in">fclose</span>(file);
    <span class="hljs-built_in">printf</span>(<span class="hljs-string">&quot;success read file: [%s] and count char\n\n&quot;</span>, file_name);
    <span class="hljs-keyword">return</span> <span class="hljs-number">0</span>;
}

<span class="hljs-function"><span class="hljs-type">int</span> <span class="hljs-title">compress_file</span><span class="hljs-params">(<span class="hljs-type">char</span> *input_file_name, <span class="hljs-type">char</span> *output_file_name)</span> </span>{
	<span class="hljs-built_in">printf</span>(<span class="hljs-string">&quot;begin cmpress [%s] -&gt; [%s]\n&quot;</span>, input_file_name, output_file_name);
    
    FILE *input = <span class="hljs-built_in">fopen</span>(input_file_name, <span class="hljs-string">&quot;rb&quot;</span>);
    FILE *output = <span class="hljs-built_in">fopen</span>(output_file_name, <span class="hljs-string">&quot;wb&quot;</span>);
    
    <span class="hljs-keyword">if</span>(input == <span class="hljs-literal">NULL</span> || output == <span class="hljs-literal">NULL</span>) {
    	<span class="hljs-built_in">printf</span>(<span class="hljs-string">&quot;can&#x27;t open file [%s] or [%s], please check file name!\n&quot;</span>, 
				input_file_name, output_file_name);
        <span class="hljs-keyword">return</span> <span class="hljs-number">-1</span>;
    }
    
    <span class="hljs-type">unsigned</span> <span class="hljs-type">char</span> buffer[BUFFER_SIZE]; <span class="hljs-comment">// 文件读取缓冲区</span>
    <span class="hljs-type">size_t</span> bytes_read; <span class="hljs-comment">// 已经读取了多少字节 </span>
    <span class="hljs-type">unsigned</span> <span class="hljs-type">char</span> current_byte = <span class="hljs-number">0</span>; <span class="hljs-comment">// 当前字节 </span>
    <span class="hljs-type">int</span> bit_count = <span class="hljs-number">0</span>; <span class="hljs-comment">// 已经处理了多少个二进制位 </span>
    
    <span class="hljs-keyword">while</span>((bytes_read = <span class="hljs-built_in">fread</span>(buffer, <span class="hljs-number">1</span>, BUFFER_SIZE, input)) &gt; <span class="hljs-number">0</span>) {
    	<span class="hljs-keyword">for</span>(<span class="hljs-type">size_t</span> i = <span class="hljs-number">0</span>; i &lt; bytes_read; i++) {
            string code = huffman_codes_table[buffer[i]];
            <span class="hljs-type">int</span> code_length = code.<span class="hljs-built_in">length</span>();
            
            <span class="hljs-keyword">for</span>(<span class="hljs-type">int</span> j = <span class="hljs-number">0</span>; j &lt; code_length; j++) {
            	<span class="hljs-type">char</span> bit = code[j];
            	current_byte &lt;&lt;= <span class="hljs-number">1</span>; <span class="hljs-comment">// 左移 1 位</span>
				<span class="hljs-keyword">if</span>(bit == <span class="hljs-string">&#x27;1&#x27;</span>) {
					current_byte |= <span class="hljs-number">1</span>;
				} 
            	
            	bit_count++;
				
				<span class="hljs-keyword">if</span>(bit_count == <span class="hljs-number">8</span>) { <span class="hljs-comment">// 凑齐了 8 位（1 个字节） </span>
					<span class="hljs-built_in">fputc</span>(current_byte, output);
					current_byte = <span class="hljs-number">0</span>; <span class="hljs-comment">// 清零，继续下一个字节 </span>
					bit_count  = <span class="hljs-number">0</span>;
				}
			}
		}
	}
	
	<span class="hljs-comment">// 处理最后一个不完整的字节</span>
	<span class="hljs-keyword">if</span>(bit_count &gt; <span class="hljs-number">0</span>) {
		current_byte &lt;&lt;= (<span class="hljs-number">8</span> - bit_count); <span class="hljs-comment">// 左移补齐 0</span>
		<span class="hljs-built_in">fputc</span>(current_byte, output);
        <span class="hljs-comment">// 记录最后一个字节的有效位数</span>
        <span class="hljs-built_in">fputc</span>(bit_count, output);
	} <span class="hljs-keyword">else</span> {
		<span class="hljs-built_in">fputc</span>(<span class="hljs-number">8</span>, output); <span class="hljs-comment">// 表示最后一个字节是完整的</span>
	}
	
	<span class="hljs-built_in">fclose</span>(input);
    <span class="hljs-built_in">fclose</span>(output);
    <span class="hljs-built_in">printf</span>(<span class="hljs-string">&quot;success compress file: [%s] to [%s]\n\n&quot;</span>, input_file_name, output_file_name);
    <span class="hljs-keyword">return</span> <span class="hljs-number">0</span>;
}

<span class="hljs-comment">// 获取指定文件大小 </span>
<span class="hljs-function"><span class="hljs-type">size_t</span> <span class="hljs-title">get_file_size</span><span class="hljs-params">(<span class="hljs-type">char</span> *file_name)</span> </span>{
	FILE *fp = <span class="hljs-built_in">fopen</span>(file_name, <span class="hljs-string">&quot;rb&quot;</span>);
	<span class="hljs-keyword">if</span> (fp == <span class="hljs-literal">NULL</span>) {
		<span class="hljs-built_in">printf</span>(<span class="hljs-string">&quot;can&#x27;t open file [%s], please check file name!\n&quot;</span>, file_name);
		<span class="hljs-keyword">return</span> <span class="hljs-number">-1</span>;
	}
	<span class="hljs-built_in">fseek</span>(fp, <span class="hljs-number">0</span>, SEEK_END);
	<span class="hljs-type">size_t</span> file_size = <span class="hljs-built_in">ftell</span>(fp);
	<span class="hljs-built_in">fclose</span>(fp);
	<span class="hljs-keyword">return</span> file_size;
}

<span class="hljs-function"><span class="hljs-type">int</span> <span class="hljs-title">uncompress_file</span><span class="hljs-params">(<span class="hljs-type">char</span> *input_file_name, <span class="hljs-type">char</span> *output_file_name)</span> </span>{
	<span class="hljs-built_in">printf</span>(<span class="hljs-string">&quot;begin uncmpress [%s] -&gt; [%s]\n&quot;</span>, input_file_name, output_file_name);
    <span class="hljs-type">size_t</span> input_file_size = <span class="hljs-built_in">get_file_size</span>(input_file_name);
    
    FILE *input = <span class="hljs-built_in">fopen</span>(input_file_name, <span class="hljs-string">&quot;rb&quot;</span>);
    FILE *output = <span class="hljs-built_in">fopen</span>(output_file_name, <span class="hljs-string">&quot;wb&quot;</span>);
    
    <span class="hljs-keyword">if</span>(input == <span class="hljs-literal">NULL</span> || output == <span class="hljs-literal">NULL</span>) {
    	<span class="hljs-built_in">printf</span>(<span class="hljs-string">&quot;can&#x27;t open file [%s] or [%s], please check file name!\n&quot;</span>, 
				input_file_name, output_file_name);
        <span class="hljs-keyword">return</span> <span class="hljs-number">-1</span>;
    }
    
    <span class="hljs-comment">// 3. 解压数据</span>
    huffman_node *current_node = huffman_tree_root; <span class="hljs-comment">// 用于记录当前在哪个结点 </span>
    <span class="hljs-type">unsigned</span> <span class="hljs-type">char</span> cur_byte; <span class="hljs-comment">// 当前读取的字节 </span>
    <span class="hljs-type">int</span> byte_bits = <span class="hljs-number">8</span>; <span class="hljs-comment">// 默认最后一个字节完整</span>
    <span class="hljs-type">long</span> bytes_decoded = <span class="hljs-number">0</span>; <span class="hljs-comment">// 已经解码的字节</span>
    
	<span class="hljs-keyword">while</span>(bytes_decoded &lt; input_file_size) {
		cur_byte = <span class="hljs-built_in">fgetc</span>(input);
		bytes_decoded++;
		
		<span class="hljs-comment">// 需要对最后一个字节处理，因为最后一个字节可能不满</span>
		<span class="hljs-keyword">if</span>(bytes_decoded == input_file_size<span class="hljs-number">-1</span>) {
			<span class="hljs-type">int</span> bit_count = <span class="hljs-built_in">fgetc</span>(input); <span class="hljs-comment">// 最后一个字节有效位数 </span>
			byte_bits = <span class="hljs-number">8</span> - bit_count;
		} 
		
		<span class="hljs-keyword">for</span>(<span class="hljs-type">int</span> i = <span class="hljs-number">7</span>; i &gt;= <span class="hljs-number">8</span> - byte_bits; i--) {
			<span class="hljs-type">int</span> bit = (cur_byte &gt;&gt; i) &amp; <span class="hljs-number">1</span>;
			<span class="hljs-keyword">if</span>(bit) { <span class="hljs-comment">// 1 往右走 </span>
				current_node = current_node-&gt;right;
			} <span class="hljs-keyword">else</span> { <span class="hljs-comment">// 0 往左走 </span>
				current_node = current_node-&gt;left;
			}
			
			<span class="hljs-comment">// 如果到达叶子节点</span>
            <span class="hljs-keyword">if</span>(!current_node-&gt;left &amp;&amp; !current_node-&gt;right) {
            	<span class="hljs-comment">// 将当前结点的 val 写入文件 </span>
                <span class="hljs-built_in">fputc</span>(current_node-&gt;val, output);  
                current_node = huffman_tree_root; <span class="hljs-comment">// 回到根节点 </span>
            }
		}
	}
	<span class="hljs-built_in">printf</span>(<span class="hljs-string">&quot;success uncompress file: [%s] to [%s]\n\n&quot;</span>, input_file_name, output_file_name);
    <span class="hljs-keyword">return</span> <span class="hljs-number">0</span>;
}

<span class="hljs-comment">// 释放哈夫曼树 </span>
<span class="hljs-function"><span class="hljs-type">void</span> <span class="hljs-title">destroy_huffman_tree</span><span class="hljs-params">(huffman_node* root)</span> </span>{
    <span class="hljs-keyword">if</span> (root == <span class="hljs-literal">NULL</span>) <span class="hljs-keyword">return</span>;
    <span class="hljs-built_in">destroy_huffman_tree</span>(root-&gt;left);
    <span class="hljs-built_in">destroy_huffman_tree</span>(root-&gt;right);
    <span class="hljs-built_in">free</span>(root);
}

<span class="hljs-comment">// 哈夫曼函数 </span>
<span class="hljs-function"><span class="hljs-type">int</span> <span class="hljs-title">huffman</span><span class="hljs-params">(<span class="hljs-type">char</span> *file_name)</span>
</span>{
	<span class="hljs-comment">// 压缩后的文件名 </span>
	<span class="hljs-type">char</span> compress_file_name[<span class="hljs-built_in">strlen</span>(file_name) + <span class="hljs-number">8</span>];
	<span class="hljs-comment">// 解压缩后的文件名 </span>
	<span class="hljs-type">char</span> uncompress_file_name[<span class="hljs-built_in">strlen</span>(file_name) + <span class="hljs-number">11</span>];
	<span class="hljs-built_in">strcpy</span>(compress_file_name, file_name);
	<span class="hljs-built_in">strcat</span>(compress_file_name, <span class="hljs-string">&quot;.huffman&quot;</span>);
	
	<span class="hljs-built_in">strcpy</span>(uncompress_file_name, <span class="hljs-string">&quot;uncompress_&quot;</span>);
	<span class="hljs-built_in">strcat</span>(uncompress_file_name, file_name);

	<span class="hljs-built_in">count_file_char</span>(file_name);
	<span class="hljs-built_in">generate_huffman_tree_nodes</span>();
	<span class="hljs-built_in">generate_huffman_tree</span>();
	
    <span class="hljs-comment">// 根据哈夫曼树生成哈夫曼编码表</span>
	huffman_node *root = huffman_tree_root; 
	<span class="hljs-built_in">generate_huffman_codes_table</span>(root, <span class="hljs-string">&quot;&quot;</span>);
	<span class="hljs-built_in">print_huffman_codes_table</span>(huffman_codes_table);

	<span class="hljs-built_in">compress_file</span>(file_name, compress_file_name);
	<span class="hljs-built_in">uncompress_file</span>(compress_file_name, uncompress_file_name);
	<span class="hljs-built_in">destroy_huffman_tree</span>(huffman_tree_root);
}

<span class="hljs-function"><span class="hljs-type">int</span> <span class="hljs-title">main</span><span class="hljs-params">(<span class="hljs-type">int</span> argc, <span class="hljs-type">char</span>** argv)</span>
</span>{
	<span class="hljs-keyword">if</span>(argc &lt; <span class="hljs-number">2</span>) {
		<span class="hljs-built_in">printf</span>(<span class="hljs-string">&quot;usage: %s &lt;filename&gt;\n&quot;</span>, argv[<span class="hljs-number">0</span>]);
		<span class="hljs-keyword">return</span> <span class="hljs-number">-1</span>;
	} <span class="hljs-keyword">else</span> {
		<span class="hljs-built_in">printf</span>(<span class="hljs-string">&quot;the file is [%s]\n&quot;</span>, argv[<span class="hljs-number">1</span>]);
		<span class="hljs-keyword">return</span> <span class="hljs-built_in">huffman</span>(argv[<span class="hljs-number">1</span>]);
	}
	<span class="hljs-keyword">return</span> <span class="hljs-number">0</span>;
}
</code></pre>
]]></content>
        <author>
            <name>Guanngxu</name>
        </author>
    </entry>
    <entry>
        <title type="html"><![CDATA[动态电压调节]]></title>
        <id>https://www.guanngxu.com/yWl6bPr5jd/</id>
        <link href="https://www.guanngxu.com/yWl6bPr5jd/"/>
        <updated>2025-06-18T01:34:57.000Z</updated>
        <summary type="html"><![CDATA[
参考内容：
Dynamically Program Voltage Regulators
How to Dynamically Adjust Power Module Output Voltage
一种通过DAC调节DCDC输出电压的电路...]]></summary>
        <content type="html"><![CDATA[<blockquote>
<p>参考内容：</p>
<p><a href="http://www.mosaic-industries.com/embedded-systems/microcontroller-projects/electronic-circuits/program-regulator-voltage">Dynamically Program Voltage Regulators</a></p>
<p><a href="https://www.ti.com/lit/an/slva861/slva861.pdf">How to Dynamically Adjust Power Module Output Voltage</a></p>
<p><a href="https://www.docin.com/p-2877038993.html">一种通过DAC调节DCDC输出电压的电路方案</a></p>
</blockquote>
<p>诸如音频放大器供电等应用中需要动态电压。虽然有许多线性稳压器或开关电源 IC 可以允许通过固定的分压电阻设置其输出电压，但却很少有实现动态电压调整的，并且设置的输出电压也必须大于内部参考电压。</p>
<p>那么如何实现能够将输出变化到小于参考电压值的电源，或者如何实现一个输出电压可动态调整的稳压器。</p>
<h3 id="固定输出电压反馈网络">固定输出电压反馈网络</h3><p>下面这张图展示了线性稳压器的反馈网络，其包含一个误差放大器，放大器将外部电压（通过分压电阻网络从输出电压采样得到）与内部固定的电压基准源进行比较。误差放大器的输出驱动导通器件的导通程度，导通器件将电流运送到输出端。通过设置外部电阻 R<sub>f1</sub> 和 R<sub>f2</sub>，可以将线性稳压器的输出电压设置为大于或等于内部基准的任何电压。</p>
<figure data-type="image" tabindex="1"><img src="https://www.guanngxu.com/post-images/1750211172926.png" alt=""></figure>
<p>接下来的这张图展示的是降压型开关稳压器的典型电路。与线性放大器类似，同样具有误差放大器和内部基准电压。不同的是导通器件通过 PWM 或者脉冲频率调制方式实现快速打开或关闭，功率波形经过滤波器以产生恒定的直流电压输出。</p>
<figure data-type="image" tabindex="2"><img src="https://www.guanngxu.com/post-images/1750215413967.png" alt=""></figure>
<p>可以发现不论是 LDO，还是降压型开关电源，它们都可以用下面的模型简化。即通过分压电阻网络对输出进行采样，将采样结果反馈至内部误差放大器，与内部基准源进行比较，从而调节输出电压使之稳定在设定值。</p>
<figure data-type="image" tabindex="3"><img src="https://www.guanngxu.com/post-images/1750216440834.png" alt=""></figure>
<h3 id="动态调节输出电压">动态调节输出电压</h3><p>很容易能想到只需要将反馈网络的上分压电阻或下分压电阻替换为滑动电阻，通过改变不同的阻值即可达到动态调节电压的目的。</p>
<p>当然在实际应用中不太可能使用滑动电阻那么笨拙的东西，我们可以通过向反馈网络中注入少量电流，以此来改变反馈网络的有效增益$\frac{V_{FB}}{V_{OUT}}$。当在反馈节点注入正电流时，上分压电阻表现为变小，因此输出电压会降低；当电流被拉出时，下分压电阻表现为变小，因此输出电压表现为增加。</p>
<figure data-type="image" tabindex="4"><img src="https://www.guanngxu.com/post-images/1750434328708.png" alt=""></figure>
<p>上文提到的电流源我们可以通过外部对一个电阻施加电压来实现，用下面图中简化模型来进行理论计算。芯片正常工作时，FB 引脚电压始终为芯片内部基准电压，利用这一特点我们可以推导出 V<sub>out</sub> 与外部施加电压 V<sub>s</sub> 之间的关系。</p>
<figure data-type="image" tabindex="5"><img src="https://www.guanngxu.com/post-images/1750691904614.png" alt=""></figure>
<p>$$I_{2} = I_{1} + I_{3}$$</p>
<p>$$\frac{V_{ref}}{R_{2}} =  \frac{V_{out} - V_{ref}}{R_{1}} + \frac{V_{s} - V_{ref}}{R_{3}}$$</p>
<p>$$V_{out} = R_{1} \bullet (\frac{V_{ref}}{R_{2}} + \frac{V_{ref} - V_{s}}{R_{3}} ) + V_{ref}$$</p>
<p>$$V_{out} = -V_{s}\frac{R_{1}}{R_{3}} + V_{ref}\bullet (1 + \frac{R_{1}}{R_{3}} + \frac{R_{1}}{R_{2}})$$</p>
<p>通过理论计算可以得出$V_{out}$与$V_{s}$是负相关关系。R<sub>3</sub> 的值越大，曲线就越缓，意味着可调的精度越高。当 V<sub>s</sub> 为 0 时，即 R<sub>3</sub> 与 R<sub>2</sub> 并联后的等效阻值为下分压电阻。</p>
<figure data-type="image" tabindex="6"><img src="https://www.guanngxu.com/post-images/1750473443551.png" alt=""></figure>
<h3 id="能否实现负压输出">能否实现负压输出？</h3><p>上文中曲线看出可以通过调节 V<sub>s</sub> 的值，实现输出负压的效果，那是否真的能输出负压呢？</p>
<p>以前在<a href="https://www.guanngxu.com/br0hmOiiG/">BUCK 电路基础知识</a>中提到过电感电流是个三角波，电感电流上升阶段的斜率为$\frac{V_{i}-V_{o}}{L}$，电感电流下降阶段斜率为$-\frac{V_{o}}{L}$。若通过上述方式输出负压，则电感电流将一直处于上升阶段，系统是不稳定的，因此无法输出负压。</p>
<figure data-type="image" tabindex="7"><img src="https://www.guanngxu.com/post-images/1750955117133.png" alt=""></figure>
<p>BUCK 电路不能输出负压，那如果换成 LDO 芯片能否输出负压呢？答案也是不能输出负压，原因在于从 GND 至 VOUT 都放了 ESD 二极管，如果 VOUT 为负压，那么二极管就会被导通钳位。</p>
<h4 id="buck-boost-实现正转负">Buck-Boost 实现正转负</h4><p>虽然 Buck 不能实现正转负的功能，但是我们可以通过改变 Buck 芯片外围电路，使其工作在 Buck-Boost 模式下，以此来达到正转负的目的。而对外围电路的修改也很简单，只需要将原来的的 V<sub>out</sub> 改为 GND，那么原来的 GND 即输出负压。</p>
<p>与上文同理，我们可以通过外部施加一个电流源来实现对负压进行动态调节的目的。用下面图中简化模型来进行理论计算。芯片正常工作时，FB 引脚电压始终为芯片内部基准电压，利用这一特点我们可以推导出 V<sub>out</sub> 与外部施加电压 V<sub>s</sub> 之间的关系。</p>
<figure data-type="image" tabindex="8"><img src="https://www.guanngxu.com/post-images/1762158267822.png" alt=""></figure>
<p>$$I_{2} = I_{1} + I_{3}$$</p>
<p>$$\frac{ GND-V_{ref} }{ R_{1} } + \frac{ V_{s}-V_{ref} }{ R_{3} } = \frac{ V_{ref}-V_{out} }{ R_{2} }$$</p>
<p>$$V_{out} = V_{ref} + \frac{R_{2}}{R_{1}}V_{ref} - \frac{R_{2}}{R_{3}}(V_{s} - V_{ref})$$</p>
<p>$$V_{out} = V_{ref} \bullet (1 + \frac{R_{2}}{R_{1}} + \frac{R_{2}}{R_{3}}) -  \frac{R_{2}}{R_{3}} V_{s}$$</p>
<p>考虑到此处的 V<sub>out</sub> 是负压，V<sub>out</sub> 与 V<sub>s</sub> 为正相关关系，所以画出 V<sub>out</sub> 与 V<sub>s</sub> 的曲线关系如下图所示。</p>
<figure data-type="image" tabindex="9"><img src="https://www.guanngxu.com/post-images/1762159074890.png" alt=""></figure>
<h3 id="输出最大值最小值由什么决定">输出最大值最小值由什么决定？</h3><p>由于 BUCK 上下管打开关断始终需要一定时间，因此最小值由最小占空比决定，即由 min-on time 决定。</p>
<p>同理最大值则由 min-off time 决定。</p>
]]></content>
        <author>
            <name>Guanngxu</name>
        </author>
    </entry>
    <entry>
        <title type="html"><![CDATA[Boost 电路基础知识]]></title>
        <id>https://www.guanngxu.com/VvjVruOGrV/</id>
        <link href="https://www.guanngxu.com/VvjVruOGrV/"/>
        <updated>2024-12-10T01:48:20.000Z</updated>
        <summary type="html"><![CDATA[
参考内容：
boost升压，电感电压和输入电压叠加、实现升压
电力电子技术 05 DC-DC变换器
手撕Boost！Boost公式推导及实验验证

Boost 电路构建我们先复习一下 BUCK 电路的基础知识。BUCK 电路是一个降压结构...]]></summary>
        <content type="html"><![CDATA[<blockquote>
<p>参考内容：</p>
<p><a href="https://blog.csdn.net/weixin_39841002/article/details/109956543">boost升压，电感电压和输入电压叠加、实现升压</a></p>
<p><a href="https://www.bilibili.com/video/BV17t4y1U7Y">电力电子技术 05 DC-DC变换器</a></p>
<p><a href="https://mp.weixin.qq.com/s/V6vDuAu7uUQvotp7YFaIjQ">手撕Boost！Boost公式推导及实验验证</a></p>
</blockquote>
<h3 id="boost-电路构建">Boost 电路构建</h3><p>我们先复习一下 <a href="https://www.guanngxu.com/br0hmOiiG/">BUCK 电路的基础知识</a>。BUCK 电路是一个降压结构，在不考虑电路的损耗时，输入功率 P<sub>in</sub> 与输出功率 P<sub>out</sub> 是一致的。电压降低，那么电流就必然升高，因此我们可以说 BUCK 是一个降压、升流的电路。</p>
<p>在不考虑使用变压器的情况下，如何构造一个升压结构呢？其实换个角度想构造升压电路，其实就是构造一个降流电路出来。那么如何构造降流电路呢？</p>
<p>参考 BUCK 降压的原理，我们使用同样的方式对电流进行处理，即将 BUCK 电路进行改造来完成这个降流工作。下面是一个电流源、开关和负载构成的电路，当开关 S 闭合时，电流不流向负载；当开关 S 打开时，电流即流向负载。</p>
<figure data-type="image" tabindex="1"><img src="https://www.guanngxu.com/post-images/1733795873225.jpg" alt=""></figure>
<p>此时负载所获得的电流是一个方波，大致是下面这个样子的。可以发现与 BUCK 电路很像，我们只需要控制开关 S 的闭合时间，就可以控制流向负载的总电流。根据波形图可以确定，流向负载的电流一定小于输入电流，当开关打开时电流才流向负载。设置占空比为 D，则 $I_{o}=(1-D)I_{i}$，根据 $V_{o}I_{o}=V_{i}I_{i}$，那么即有 $V_{o}=\frac{1}{1-D}V _{i}$</p>
<figure data-type="image" tabindex="2"><img src="https://www.guanngxu.com/post-images/1733796048859.jpg" alt=""></figure>
<p>我们目前的电路还是过于理想化了，我们平时见到的都是电压源，基本没有见过电流源的场景。那么如何利用电压源做出来电流源呢？考虑到电感可以阻碍电路的变化，一个电压源串联一个电感我们可以将之近似认为是一个电流源。</p>
<figure data-type="image" tabindex="3"><img src="https://www.guanngxu.com/post-images/1733796581371.jpg" alt=""></figure>
<p>回想一下前面的波形，负载所获得的电流波动太剧烈了，势必会影响电压的剧烈抖动。与 BUCK 电路的思维一致，我们还是可以用电容来进行稳压。</p>
<figure data-type="image" tabindex="4"><img src="https://www.guanngxu.com/post-images/1733796628327.jpg" alt=""></figure>
<p>在负载端添加输出电容后，电压得以被稳定，但是考虑一下此时开关 S 闭合时是什么情况？直接将电容短路了，如何解决电容被短路的问题呢？发现当只有当 S 闭合时才会有电容短路的问题，S 打开时电流从电源端流向负载并给电容充电。我们可以通过添加一个二极管来防止 S 闭合时电容短路。</p>
<blockquote>
<p>当然，也可以把二极管换成另一个开关，只要保证 S 打开时二极管位置闭合，S 闭合时二极管位置打开即可。</p>
</blockquote>
<figure data-type="image" tabindex="5"><img src="https://www.guanngxu.com/post-images/1733796668790.jpg" alt=""></figure>
<p>考虑到机械开关容易磨损、使用寿命短、有机械惯性（转换频率低）的问题，我们需要将机械开关换成转换频率高的半导体器件，此处我们选择 NMOS 管来替代开关。同时二极管起到的是一个开关的作用。而且考虑到电流从二极管流过期间，二极管两端的压降恒定为导通电压 0.7V，二极管所消耗的能量较大。因此我们也可以把二极管换为导通电阻更小的 NMOS 管。</p>
<figure data-type="image" tabindex="6"><img src="https://www.guanngxu.com/post-images/1733840131728.jpg" alt=""></figure>
<p>为了提高 Boost 电路的稳定性，防止由于输入纹波带来异常，我们在 Boost 电路的输入端并联一个电容，用于滤除输入电压的纹波。</p>
<figure data-type="image" tabindex="7"><img src="https://www.guanngxu.com/post-images/1733840169779.jpg" alt=""></figure>
<p>至此我们即构建了一个标准的同步 Boost 电路。回过头再来看看，我们利用储能元件从输入电源获得能量得到一个电压，并将这个电压与输入电压进行串联，以此来实现了升压的效果。电容和电感是两种常见的储能元件，使用电感或电容均可以达到升压目的。</p>
<p>比如 <a href="https://www.guanngxu.com/br0hmOiiG/">BUCK 电路</a>中若上管为 NMOS，则需要添加 boot 电容实现升压才能打开 NMOS 管。原因在于 NMOS 导通条件为 V<sub>GS</sub> 大于 V<sub>th</sub>，而 BUCK 电路中的 NMOS 上管 S 电压最低为 V<sub>in</sub>，因此需要 boot 电容进行升压。</p>
<p>我们在 Boost 电路中使用电感来实现升压升压的功能。当电感与负载断开并接地时，电感激磁并将能量存储在电感中。当电感和输入电压断开后，由于电感电流不能突变：$V_{in} =L\frac{di}{dt}$，电感中变化的电流产生感应电压，感应电压与输入电压串联达到了升压目的。</p>
<figure data-type="image" tabindex="8"><img src="https://www.guanngxu.com/post-images/1733796818783.jpg" alt=""></figure>
<h3 id="稳态分析">稳态分析</h3><p>我们所说的稳态分析，是指输入输出电压都是稳定不变的前提下进行的分析。我们将一些已知条件列出来：</p>
<ul>
<li>输入电压：$V_{i}$</li>
<li>输出电压：$V_{o}$</li>
<li>负载电阻：$R$</li>
<li>开关频率：$f$</li>
</ul>
<h4 id="伏秒平衡">伏秒平衡</h4><p>计算的基本原理就是电容和电感的充放电，根据前文所说的 $V_{o}=\frac{1}{1-D}V _{i}$，可以知道输出电压肯定大于输入电压。</p>
<p>在下管导通上管截止时，电感两端电压是 $V_{i}$；在下管截止上管导通时，电感右侧的电压为 $V_{o}$，左侧为电源输入 $V_{i}$，所以电感两端电压为 $V_{o}-V_{i}$。</p>
<p>整个电路处于稳定状态，负载电流恒定，那么在一个周期内，电感电流增加的量肯定等于电感电流减小的量，即充了多少电就要放多少电，不然负载的电流或电压将会发生变化。</p>
<p>前文已有$\frac{\mathrm{d} i}{\mathrm{d} t} =\frac{{U}}{L}$，而 $L$ 恒定，那么电感电流的变化速度即与电压成正比关系，即电感电流上升（下降）的斜率与电压成正比关系。而电感电流上升和下降的高度相同，那么上升时间和下降时间就自然构成反比关系。</p>
<p>$\frac{T_{on} }{T_{off} } = \frac{V_{o}-V_{i}}{V_{i}}$，将其进行简单变换即可得到闻名江湖的<strong>伏秒平衡法则</strong>。</p>
<p><strong>$T_{on}V_{i} = T_{off}(V_{o}-V_{i})$</strong></p>
<h4 id="占空比">占空比</h4><p>已知 $T=T_{on}+T_{off}=\frac{1}{f}$，结合伏秒平衡法则可以计算出：</p>
<p>开通时间：$T_{on} = \frac{V_{o}-V_{i}}{V_{o}} \bullet \frac{1}{f}$</p>
<p>关断时间：$T_{off} = \frac{V_{i}}{V_{o}} \bullet \frac{1}{f}$</p>
<p>占空比：$D = \frac{T_{on}}{T}=\frac{V_{o}-V_{i}}{V_{o}}$</p>
<p>通过计算可以发现，占空比与电感没有关系，与负载电流大小也没有关系，<strong>只和输入输出电压有联系</strong>。</p>
<h4 id="纹波电流">纹波电流</h4><p>上文计算电感电流斜率时已经能确定电流波形是个三角波，纹波电流等于在下管导通时电感电流的增大值，也等于在下管截止时电感电流减小的值，计算任意一个即可得到纹波电流。我们以下管导通时增大的电感电流计算。</p>
<figure data-type="image" tabindex="9"><img src="https://www.guanngxu.com/post-images/1733923463080.jpg" alt=""></figure>
<p>下管导通时电感两端电压为 $V_{i}$，导通时间为 $T_{on} = \frac{V_{o}-V_{i}}{V_{o}} \bullet \frac{1}{f}$，根据 $U=L\frac{di}{dt}$ 有：</p>
<p>$\triangle I_{L} =di$</p>
<p>$=T_{on}\bullet \frac{U}{L}$</p>
<p>$=\frac{V_{o}-V_{i}}{V_{o}} \bullet \frac{V_{i}}{L} \bullet \frac{1}{f}$</p>
<p>$=\frac{(V_{o}-V_{i})V_{i}}{V_{o}Lf}$</p>
<p>根据理论计算可以发现，<strong>电感电流的纹波和负载电流的大小没有关系</strong>。同时电感的感值会影响纹波电流的大小，因此电感的选择是系统一个很重要的参数。</p>
<h4 id="功率电感选择">功率电感选择</h4><p>在系统稳态时，输出端电容是不耗电的，电压也不会有变化，所以输出电容上面流过的平均电流为 0。负载的所有电流都从上管而来，因此流过上管的平均电流也是 $I_{o}$。输入功率等于输出功率，输入电流等于流过电感的电流，即有：</p>
<p>$V_{i}I_{L}=V_{o}I_{o}$</p>
<p>$I_{L}=\frac{V_{o}I_{o}}{V_{i}}$</p>
<p>计算电感峰值电流可以得到：</p>
<p>$I_{LP} =I_{o}+\frac{\triangle I_{L}}{2}$</p>
<p>$=I_{L}+\frac{V_{o}-V_{i}}{2V_{o}} \bullet \frac{V_{i}}{L} \bullet \frac{1}{f}$</p>
<p>$=\frac{V_{o}I_{o}}{V_{i}}+\frac{V_{o}-V_{i}}{2V_{o}} \bullet \frac{V_{i}}{L} \bullet \frac{1}{f}$</p>
<p>在选择功率电感时，电感的饱和电流就必须要大于这个$I_{LP}$，并且需要留有一定的裕量。实际应用时电感的纹波电流应是平均电流的 $30%$ 至 $50%$ 为宜，我们将这个参数称之为<strong>电流纹波率 r</strong>，关于电流纹波率为什么选择 0.3 至 0.5，可以查看<a href="https://www.guanngxu.com/br0hmOiiG/#%E4%B8%BA%E4%BD%95-r-%E4%B8%BA-03~05">为何 r 为 0.3～0.5</a>。根据电流纹波率范围就可以计算出电感值的范围：</p>
<p>$\triangle I_{L} =\frac{V_{o}-V_{i}}{V_{o}} \bullet \frac{V_{i}}{L} \bullet \frac{1}{f}$</p>
<p>$L =\frac{V_{o}-V_{i}}{V_{o}} \bullet \frac{V_{i}}{\triangle I_{L}} \bullet \frac{1}{f}$</p>
<p>$=\frac{V_{o}-V_{i}}{V_{o}} \bullet \frac{V_{i}}{(0.3至0.5) I_{i}} \bullet \frac{1}{f}$</p>
<p>$=\frac{(V_{o}-V_{i})V_{i}}{(0.3至0.5) I_{i}V_{o}f}$</p>
<h4 id="输入纹波">输入纹波</h4><p>输入电压纹波就是输入电容上面电压的变化，这个变化可以分为两部分。一部分为电容充放电所导致的电压变化 $U_{q}$，另一部分为电流流过电容 $ESR$ 导致的压降 $U_{esr}$。即 <strong>$\triangle V_{i} = U_{q} + U_{ser}$</strong>。</p>
<p>我们来看输入节点，这个节点的电流有三个，一个是来自电源输入的，在一个周期内我们看作是恒定的。另外两个节点是电容和电感。根据基尔霍夫电流定律，节点电流和为 0，并且电源输入的电流恒定，那么电感电流的变化量必然等于电容电流的变化量，因为最终三者的和为 0。</p>
<figure data-type="image" tabindex="10"><img src="https://www.guanngxu.com/post-images/1734142428282.png" alt=""></figure>
<p>节点电流和为 0，那么输入电容的电流变化就是功率电感的电流变化。我们画出三者的电流波形。电容电流大于 0 时，电容在充电，电容电流小于 0 时，电容在放电。并且电容充电和放电时间长度是一样的，都是周期的一半。</p>
<figure data-type="image" tabindex="11"><img src="https://www.guanngxu.com/post-images/1734143077654.png" alt=""></figure>
<p>需要注意的是充电与放电的切换的时刻并不是开关导通与断开的时候，而是在中间时刻。电容充放电的总电荷量即等于电流乘以时间，其实就是图中阴影三角形的面积。</p>
<p>三角形底部是时间，充电/放电时间为$\frac{T}{2}$；三角形的高为电感纹波电流的一半 $\frac{\triangle I_{L}}{2}$；根据三角形面积计算公式即有：</p>
<p>$Q=C_{i}U_{q}$</p>
<p>$=\frac{1}{2}\bullet \frac{T}{2}\bullet \frac{\triangle I_{L}}{2}$</p>
<p>$=\frac{1}{2}\bullet \frac{1}{2f}\bullet \frac{\frac{(V_{o}-V_{i})V_{i}}{V_{o}Lf}}{2}$</p>
<p>$=\frac{(V_{o}-V_{i})V_{i}}{8f^{2} V_{o}L}$</p>
<p>$U_{q}=\frac{(V_{o}-V_{i})V_{i}}{8f^{2} V_{o}LC_{i}}$</p>
<p>前面波形图已经知道，电容的充电电流最大是 $\frac{\triangle I_{L}}{2}$，放电电流最大是 $-\frac{\triangle I_{L}}{2}$。那么 $ESR$ 引起的总压降即为：</p>
<p>$U_{esr} = \frac{\triangle I_{L}}{2} \bullet ESR - (-\frac{\triangle I_{L}}{2} \bullet ESR)$</p>
<p>$= \triangle I_{L}\bullet ESR$</p>
<p>$= \frac{(V_{o}-V_{i})V_{i}}{V_{o}Lf} \bullet ESR$</p>
<p>最终可得：</p>
<p>$\triangle V_{i} = U_{q} + U_{esr}$</p>
<p>$=\frac{(V_{o}-V_{i})V_{i}}{8f^{2} V_{o}LC_{i}} + \frac{(V_{o}-V_{i})V_{i}}{V_{o}Lf} \bullet ESR$</p>
<h4 id="输入电容选择">输入电容选择</h4><p>考虑到电容的实际使用情况，陶瓷电容的 $ESR$ 小，容量小，所以 $U_{q}$ 对纹波起决定性作用，输入纹波可近似为 $U_{q}$。若选择铝电解电容，则 $ESR$ 大，容量大，$U_{esr}$ 对纹波起到决定性作用，输入纹波可以近似为 $U_{esr}$，假设电路设计要求输入纹波不能大于 $\triangle V_{i}$，则有：</p>
<p>陶瓷电容：$C_{i} \ge \frac{(V_{o}-V_{i})V_{i}}{8f^{2} V_{o}L \triangle V_{i}}$</p>
<p>铝电解电容：$ESR \le \frac{\triangle V_{i}V_{o}Lf}{(V_{o}-V_{i})V_{i}}$</p>
<h4 id="输出纹波">输出纹波</h4><p>输出纹波与输入纹波同理，亦是 $\triangle V_{o} = U_{q} + U_{esr}$。一个周期内电容的充电电荷必然与放电电荷量相等，我们只需要计算出其中任意一个即可。很显然计算放电电荷量更为容易，因为放电电流就是负载电流，负载电流为恒定的 $I_{o} = \frac{V_{o}}{R_{L}}$。</p>
<p>$Q = C_{o}U_{q}=I_{o}T_{on}$</p>
<p>$T_{on} = \frac{V_{o}-V_{i}}{V_{o}} \bullet \frac{1}{f}$</p>
<p>$U_{q} = \frac{I_{o}T_{on}}{C_{o}}$</p>
<p>$= \frac{I_{o}(V_{o}-V_{i})}{C_{o}V_{o}f}$</p>
<p>在下管导通的时候，负载的电流为 $I_{o}$，完全由输出滤波电容提供，即滤波电容的放电电流也为 $I_{o}$，而且还是在导通时间里面恒定不变的。</p>
<p>在下管从导通切换到截止时，电感的电流已经是充到最大的，因为先前下管导通时电感一直在充电，所以切换时电感电流最大，且等于电感平均电流加上纹波电流的一半，即 $I_{L}+\frac{\triangle I_{L}}{2}$。已经充好的电感电流会给负载供电，负载电流为 $I_{o}$。同时电感还要给输出电容进行充电，根据基尔霍夫电流定律，电容的充电电流就是电感充到最大的电流减去负载的电流，即 $I_{L}+\frac{\triangle I_{L}}{2} - I_{o}$。</p>
<p>在下管关断之后，电感电压反向，电感电流持续减小，负载电流恒定不变，所以输出滤波电容的电流持续减小。画出输出电容的电流波形就非常明显了。</p>
<figure data-type="image" tabindex="12"><img src="https://www.guanngxu.com/post-images/1734165065165.png" alt=""></figure>
<p>在下管导通时，$ESR$ 两端的电流为 $-I_{o}$。在下管截止时，$ESR$ 两端的电流为 $I_{L}+\frac{\triangle I_{L}}{2} - I_{o}$。由此即可计算出 $ESR$ 上的电压变化量。</p>
<p>$U_{esr} = (I_{L}+\frac{\triangle I_{L}}{2} - I_{o})\bullet ESR - (-I_{o} \bullet ESR)$</p>
<p>$= (I_{L}+\frac{\triangle I_{L}}{2})\bullet ESR$</p>
<p>$= (\frac{V_{o}I_{o}}{V_{i}} + \frac{(V_{o}-V_{i})V_{i}}{2V_{o}Lf}) \bullet ESR$</p>
<p>最终可得：</p>
<p>$\bigtriangleup U_{o} = U_{q} + U_{esr}$</p>
<p>$=\frac{I_{o}(V_{o}-V_{i})}{C_{o}V_{o}f} +  (\frac{V_{o}I_{o}}{V_{i}} + \frac{(V_{o}-V_{i})V_{i}}{2V_{o}Lf}) \bullet ESR$</p>
<h4 id="输出电容选择">输出电容选择</h4><p>与输入电容选择的方式一致，考虑是容值还是 $ESR$ 占主导地位，假设要求输出纹波要小于 $\triangle V_{o}$，则有：</p>
<p>陶瓷电容：$C_{o} \ge \frac{I_{o}(V_{o}-V_{i})}{\triangle V_{o}V_{o}f}$</p>
<p>铝电解电容：$ESR \le \frac{\triangle V_{o}}{\frac{V_{o}I_{o}}{V_{i}} + \frac{(V_{o}-V_{i})V_{i}}{2V_{o}Lf}}$</p>
]]></content>
        <author>
            <name>Guanngxu</name>
        </author>
    </entry>
    <entry>
        <title type="html"><![CDATA[电源 PCB 布局及其常见错误]]></title>
        <id>https://www.guanngxu.com/-loFmea4T/</id>
        <link href="https://www.guanngxu.com/-loFmea4T/"/>
        <updated>2024-07-09T13:48:04.000Z</updated>
        <summary type="html"><![CDATA[
参考内容：
电源 PCB 布局中的常见错误及避免方式
嘉立创EDA-PCB设计零基础入门课程（54集全）
硬件设计基础之PCB设计中的10条重要布线原则

PCB 基础我们先来看看在没有 PCB 之前的电路是如何连接的，阻容感和芯片等均通...]]></summary>
        <content type="html"><![CDATA[<blockquote>
<p>参考内容：</p>
<p><a href="https://edu.21ic.com/video/4090">电源 PCB 布局中的常见错误及避免方式</a></p>
<p><a href="https://www.bilibili.com/video/BV1fM411Z7cW">嘉立创EDA-PCB设计零基础入门课程（54集全）</a></p>
<p><a href="https://www.bilibili.com/video/BV1J14y1X7rm">硬件设计基础之PCB设计中的10条重要布线原则</a></p>
</blockquote>
<h3 id="pcb-基础">PCB 基础</h3><p>我们先来看看在没有 PCB 之前的电路是如何连接的，阻容感和芯片等均通过导线连接，由此不可避免的就带来了混乱的问题。为了解决束线、混乱等问题就出现了 PCB。</p>
<figure data-type="image" tabindex="1"><img src="https://www.guanngxu.com/post-images/1721100159378.png" alt=""></figure>
<p>PCB（Printed Circuit Board）即印制电路板，由保罗.爱斯勒发明。PCB 是元器件的支撑体，更是实现诸多电子元器件电气连接的载体。随着历史车轮的滚滚前进，为了在 PCB 上放置更多的元器件，由一层板逐渐发展为多层板。</p>
<p>多层板可以看作是由多个两层板，加上一个芯板组成，不同层之间的切换通过过孔来实现，即钻孔并在孔的内壁覆上铜。由于制造 3 层板和制造 4 层板的工艺、成本等均基本一致，所以需要注意一般是没有单数层板的，只有偶数层的 PCB。</p>
<figure data-type="image" tabindex="2"><img src="https://www.guanngxu.com/post-images/1721107206542.png" alt=""></figure>
<p>图中 1 属于盲孔，用于第一层和最后一层之间的切换；2 属于埋孔，用于内层之间的切换；3 属于通孔，用于第一层和除最后一层之间的切换。这些孔我们统一将其称之为过孔。</p>
<h3 id="走线为什么要避免锐角直角">走线为什么要避免锐角、直角</h3><p>PCB 板上的铜线需要做到阻抗连续，那么什么情况下会导致其阻抗不连续呢？当线宽改变的时候阻抗就会变得不连续，同时寄生电容、电感的值也会发生变化，那么什么情况会导致线宽改变呢？自然是涉及到拐弯的时候。</p>
<figure data-type="image" tabindex="3"><img src="https://www.guanngxu.com/post-images/1720962422341.png" alt=""></figure>
<p>除了线宽改变导致阻抗不连续外还有哪些影响呢？比如线宽改变会导致寄生容值变大，即相当于在传输导线上加了容性负载，当信号通过时即会涉及到充放电的过程，信号速度即会被减缓。</p>
<p>另外还存在尖端放电的问题，导体上面很尖的点即表示面积非常小，相应即电荷密度很高，将产生巨大的电场强度，击穿空气释放电子，就会产生比较强的电磁干扰，因此布线拐角应该使用钝角或是圆角。</p>
<h3 id="典型案例">典型案例</h3><p>对于整体性能来说，电源的布局与挑选合适的半导体（芯片、阻容感）是一样重要的。一个糟糕的布局会在电路中产生额外的寄生阻容感，这会让电路在不同的地方产生更多的噪音。除此之外，如果没有注意布局还可能让功率元件无法有效散热。</p>
<h4 id="输入电容太远导致寄生电感过大">输入电容太远导致寄生电感过大</h4><p>如图所示是一个同步降压转换器上下两面的布局。这是一个将 12V 输入降低到 1.2V 输出，且能够承载 8A 电流的降压转换器。可以从图中左上角看到开关节点（SW）对 GND 的电压波形。可以发现振铃峰值已经到了 17V，这种高频的振荡几乎可以肯定会对 EMI 造成不利的影响。而且该开关电源元件建议的最高操作电压即为 17V，绝对额定值最高为 19V，这个振荡峰值已经很接近让电源芯片因超过电压应力而损坏的点。同时该振荡电压也导致了输出纹波超过 100mV。</p>
<figure data-type="image" tabindex="4"><img src="https://www.guanngxu.com/post-images/1720533107134.png" alt=""></figure>
<p>在图中右边部分用绿色框起来的电容即为输入电容，它不止离集成开关电源芯片的 VIN 和 PGND 有一段距离，甚至还通过过孔连接到另外一面，造成额外的寄生电感。而改进方法也很简单，在原理图不变的前提下，直接在芯片的 VIN 和 PGND 引脚旁边放上输入电容器，如下图中绿色框中所示。需要注意的是较小的高频旁路电容要越靠近电源芯片才好。</p>
<figure data-type="image" tabindex="5"><img src="https://www.guanngxu.com/post-images/1720534659279.png" alt=""></figure>
<p>从测试波形可以发现，当输入电容位置摆放正确后，振荡电压几乎就被消除了，这肯定会对 EMI 特性产生正面影响。开关电压峰值也远低于芯片所建议的操作范围之内。输出电压纹波同样有了极大的改善，峰峰值已经降到 10mV 以下。</p>
<figure data-type="image" tabindex="6"><img src="https://www.guanngxu.com/post-images/1720602398229.png" alt=""></figure>
<p>方波的波形是由基本的开关频率加上各个奇次谐波所组成，越快的电压瞬间爬升或下降即表示会包含更高的频率，这就是可能导致振荡和 EMI 的原因。上图给出了不同容值（尺寸）的陶瓷电容的典型阻抗曲线，电容引起的阻抗会随着频率的增加减小，到某个频率点以上则由寄生电感所带来的阻抗影响占主导。图中红色虚线表示将不同尺寸的电容器并联时等效的阻抗，随着开关频率的不断增加，其 EMI 的要求也会变得更加严格，高频电容器和其摆放的位置也变得更加重要。</p>
<figure data-type="image" tabindex="7"><img src="https://www.guanngxu.com/post-images/1720602338714.png" alt=""></figure>
<p>回过头来看为什么将输入电容仅靠输入引脚就可以减少解决上述问题呢？如上图所示表示了一个同步降压拓扑中，寄生电感影响最大的地方，以及可以容忍且影响相对小的地方。可以发现输入电容的环路对寄生电感最为敏感，上述案例中最开始的布局由于输入电容离芯片引脚过长，PCB 走线太长导致寄生电感过大，进一步导致高频振荡严重。</p>
<h4 id="电感磁场扩散至外围">电感磁场扩散至外围</h4><p>如下图是一个从 12V 输入转为 2.5V 输出，承载 2.5A 的一个拓扑结构。从右上角可以看到测量的输出波形中虽然纹波没有很大，但是可以看到里面包含可疑的方波分量。有意思的是当我们把电感解焊旋转 180 度后再装上，方波分量会随之反转，那么这个设计发生了什么呢？</p>
<figure data-type="image" tabindex="8"><img src="https://www.guanngxu.com/post-images/1720603467518.png" alt=""></figure>
<p>此处的线索是电感器，仔细观察会发现这个是一个半屏蔽式的电感器。实际上半屏蔽式的电感器和非屏蔽式的电感器没有太大区别，这表示磁场没有得到适当的控制，而且还有可能扩散到电感器外面。如图所示可以发现输出电容 1 在靠近电感器的位置，这会导致电感器的磁场耦合到输出电容的等效串联电感中，这就像一个变压器一样。因此显示了除转换器本身的输出纹波外，还加上了被磁场耦合到的夹噪。</p>
<figure data-type="image" tabindex="9"><img src="https://www.guanngxu.com/post-images/1720768938617.png" alt=""></figure>
<p>当把电感器旋转 180 度时，磁场也会跟着翻转，这就是波形完全反转的原因。要摆脱这种方波的方法是确保来自电感的磁场不会耦合到输出电容器的等效串联电感中，那么我们把输出电容移到电感磁场范围之外，比如图中 2 或另一侧 3 的地方。上图左侧就是改善后的布局，即将输出电容移到了另一侧，从波形可以看出方波分量即被消除了。</p>
<figure data-type="image" tabindex="10"><img src="https://www.guanngxu.com/post-images/1720769861368.png" alt=""></figure>
<p>另一种减少电感器和输出电容之间耦合的方法是使用全屏蔽的电感器，这与上文移动输出电容位置的方法一样有效。上图右下角显示了非屏蔽、半屏蔽、全屏蔽电感器的磁幅射影，可以发现黑色线和红色线仅仅只是小了 5dB 左右，而蓝色线则少了 30dB 的以上。</p>
<p>另一个小地方要注意的是绕组（电感）的起点用一个点或一条线来标记，这个端点应该要连接到电路里面的开关节点，即接到电压会有跳动方波的那一点。因为绕组的起点埋在电感器的中心，并且连接到直流输出的外绕组，会有一定的自我屏蔽的效应。在电感器下方有一个接地层也能屏蔽电场带来的干扰。</p>
<p>回过头来看上文虽然将输出纹波中的方波分量消除了，但是开关瞬间产生的纹波尖峰并没有消失，这些尖峰的主要来源是通过电感器本身并联的寄生电容耦合到电压开关瞬间变化所造成的。电容本身很少出现在电感器的规格书中，但是我们还是可以通过自谐振频率推断出来，为了确保最小寄生电容和最小输出尖峰，我们应该选择具有高 SRF 的电感器。</p>
<h4 id="高阻抗反馈电阻走线太长引入噪声">高阻抗反馈电阻走线太长引入噪声</h4><p>如下是一个将 12V 输入转换为 1.05V 且输出为 5A 的拓扑，从右上角波形可以看出输出波形中大约有 70mV 的噪音纹波，另外开关波形上面也能看出有一些抖动，这会让输出瞬态响应的稳定性受到影响。</p>
<figure data-type="image" tabindex="11"><img src="https://www.guanngxu.com/post-images/1720771316473.png" alt=""></figure>
<p>解决上述问题的线索是反馈电阻，可以发现绿色框出来的地方摆在板子的右上方，而红色框起来的地方为电源芯片摆在板子的正中央。反馈引脚是一个高阻抗的节点，因此对噪音会很敏感。在初版布局中反馈电阻摆放在输出连接器的附近，这会使敏感的反馈走线跨越了快整个电路板，而且还穿过了电感的下方，干扰源很容易就耦合到这种长的反馈走线上，还有可能会导致转换器运行不稳定。</p>
<figure data-type="image" tabindex="12"><img src="https://www.guanngxu.com/post-images/1720880310266.png" alt=""></figure>
<p>右上角的第二版是改善后的布局，可以看出反馈电阻比上一版更靠近反馈引脚，同时输出电压反馈点放在远离电感和开关节点的另一侧，这样输出的波形会更加干净。右下角可以发现纹波也从大于 70mV 降低到 20mV，开关波形的抖动现象也大大减小。</p>
<figure data-type="image" tabindex="13"><img src="https://www.guanngxu.com/post-images/1720880961880.png" alt=""></figure>
<p>尽量让敏感的信号拉线远离电感器或开关节点，如果负载端与反馈调节点有一段的距离，那就要考虑上图所示的差分接线，以此来提高噪音的抑制能力。另外在选择反馈电阻时，虽然阻值越高是可以减少一些损耗，但是也更容易受到噪音的影响，并且可能因为一点点的反馈偏置电流而改变输出电压，10kohm~100kohm 范围的总的反馈电阻通常是一个不错的折中方案。</p>
<h4 id="大电流引起压降">大电流引起压降</h4><p>下面是一个大电流同步降压转换器的负载调节案例，这是一个将 12V 转换为 0.85V 并承受 20A 负载的设计。其 PCB 布局如图左下角所示，控制器位于电路板右侧而输出是位于左上角。左上图显示了输出电压调节与负载的关系，可以看到随着电流的增加输出电压下降到低于规定值的两个百分点。</p>
<figure data-type="image" tabindex="14"><img src="https://www.guanngxu.com/post-images/1720882983451.png" alt=""></figure>
<p>造成这个问题的原因是因为控制器直接检测输出电压，第一版设计中反馈与输出之间的连接点就在电感旁边，而且远离输出连接器。大的铜线会有一些寄生电阻，根据欧姆定律我们知道当电流流过电阻时会有一定的电压降，虽然铜的电阻相对较低，但是在高电流下可能还是会有影响。</p>
<figure data-type="image" tabindex="15"><img src="https://www.guanngxu.com/post-images/1720883384103.png" alt=""></figure>
<p>通过将输出的反馈点移动到输出连接器旁边，控制器能够补偿寄生电阻带来的损失，并且更好的调节输出电压。左边的蓝线即改进后的输出电压调节与负载的关系。那么又如何计算铜的寄生电阻呢？</p>
<figure data-type="image" tabindex="16"><img src="https://www.guanngxu.com/post-images/1720884635953.png" alt=""></figure>
<p>估计铜铂寄生电阻的快速方法是使用计数平方，通过将长宽设置为彼此相等，平面总电阻的方程简化为铜的电阻率除以厚度。表中给出了几个标准铜铂厚度的每平方电阻，在查看铜铂时串联的电阻相加，平行的相当于并联等效电阻更小。</p>
<figure data-type="image" tabindex="17"><img src="https://www.guanngxu.com/post-images/1720885480737.png" alt=""></figure>
<p>再回过头来看这个案例，使用的 PCB 是 2 盎司的铜，从输出到回收点的距离有 4 个正方形这么长，每平方电阻为 0.25mohm，4 个即为 1mohm，再乘以 20A 的负载电流就可以解释最大负载下输出大约下降了 20mV 的现象。</p>
]]></content>
        <author>
            <name>Guanngxu</name>
        </author>
    </entry>
    <entry>
        <title type="html"><![CDATA[电平转换器基础知识]]></title>
        <id>https://www.guanngxu.com/asBsvJSki/</id>
        <link href="https://www.guanngxu.com/asBsvJSki/"/>
        <updated>2024-05-27T09:08:53.000Z</updated>
        <summary type="html"><![CDATA[
参考内容：
迷失在电平转换中
为什么芯片都是 CMOS 的呢？CMOS 和 TTL 有什么区别？
高阻态，三态门
TI 162245 用户手册

为什么需要电平转换在电路设计中，常常会遇到一个系统中存在不同的电平信号，比如 0.9V、1....]]></summary>
        <content type="html"><![CDATA[<blockquote>
<p>参考内容：</p>
<p><a href="https://www.youtube.com/watch?v=PELQij4Rci4">迷失在电平转换中</a></p>
<p><a href="https://www.youtube.com/watch?v=4wgccSVBGYk">为什么芯片都是 CMOS 的呢？CMOS 和 TTL 有什么区别？</a></p>
<p><a href="https://www.eet-china.com/mp/a82794.html">高阻态，三态门</a></p>
<p><a href="https://www.semiee.com/file/TI/TI-SN74ABT162245.pdf">TI 162245 用户手册</a></p>
</blockquote>
<h3 id="为什么需要电平转换">为什么需要电平转换</h3><p>在电路设计中，常常会遇到一个系统中存在不同的电平信号，比如 0.9V、1.8V、3.3V、5V 等，要使系统不同模块协同工作，就需要对电平信号进行转换。</p>
<p>其实需要进行电平转换的本质原因是因为半导体加工工艺的不同。比如 TTL 电平 2.0V 以上表示<code>高</code>，0.8V 以下表示<code>低</code>。但是 CMOS 电平由于其物理原因，常常是将<code>高</code>定义为 3.5V 以上，<code>低</code>定义为 1.5V 以下。所以伴随着每一个新的半导体发展阶段，<code>高</code>和<code>低</code>都被改了一下，直到形成了下图这个样子。</p>
<figure data-type="image" tabindex="1"><img src="https://www.guanngxu.com/post-images/1716801232626.jpg" alt=""></figure>
<p>一个 5V TTL 输出跟一个 5V TTL 输入可以没有障碍的交流，但是当一个 3V CMOS 和一个 5V CMOS 进行交流时就存在问题了。当 3V CMOS 输出<code>高</code>时，意味着它应用一个 2.5V 的信号在其输出引脚上，如果此时将这个输出与一个 5V CMOS 输入端进行连接，5V CMOS 只能识别超过 3.5V 的<code>高</code>信号和低于 1.5V 的<code>低</code>信号，那么 2.5V 的输入对于 5V CMOS 来说是<code>高</code>还是<code>低</code>呢？</p>
<h3 id="常见电平转换电路">常见电平转换电路</h3><h4 id="使用电阻分压">使用电阻分压</h4><p>最直接也是最简单的办法就是使用电阻来分压来转换电平，后续芯片可以等效为一个负载电阻，通过串联一个电阻与芯片内部电阻构成分压关系。这种电路的优势是电阻器件采购方便、价格低廉，电路也很简单。但是缺点也很明显，因为两芯片引脚之间存在电压差且中间只有串联电阻，所以会有电流的流动造成两芯片相互影响。</p>
<figure data-type="image" tabindex="2"><img src="https://www.guanngxu.com/post-images/1716801272174.jpg" alt=""></figure>
<h4 id="二极管电平转换">二极管电平转换</h4><p>二极管电平转换电路如下图所示。该电路由二极管和电阻组成，电路使用元件比较少，其中二极管最好用肖特基二极管，因为肖特基二极管具有开关频率高和正向压降低的优点。</p>
<ol>
<li>当 5V TXD1 发送高电平时，二极管正极电压比负极电压低，二极管截止，RXD2 被上拉电阻拉为高电平；</li>
<li>当 5V TXD1 发送低电平时，二极管导通，RXD2 被钳位至二极管管压降（0.7V），所以收到低电平；</li>
<li>当 3.3V TXD2 发送低电平时，二极管导通，RXD1 被钳位至二极管管压降（0.7V），所以收到低电平；</li>
<li>当 3.3V TXD2 发送高电平时，二极管也是导通的，RXD1 电压为 3.3V 加上一个管压降，即 4.0V，所以收到高电平。</li>
</ol>
<p>当 3.3V 端发送高电平给 5V 端时，5V 端收到的并不是 5V，同时该电路只适合单向通讯的场景，发送端和接收端不可以互换。</p>
<figure data-type="image" tabindex="3"><img src="https://www.guanngxu.com/post-images/1716801316133.jpg" alt=""></figure>
<h4 id="三极管电平转换">三极管电平转换</h4><p>三极管电平转换电路其实就是模电中的共射极放大电路，也就是 TTL 的前身，原来的 RTL（Resistor Transistor Logic），以下图左边 5V 转 3.3V 为例。</p>
<ol>
<li>当 TXD1 发送高电平时，第一个三极管导通，导致其集电极电位为低电平，所以第二个三极管基极为低电平，第二个三极管处于截止状态，RXD2 被上拉电阻拉高至 3.3V；</li>
<li>当 TXD1 发送低电平时，第一个三极管截止，第二个三极管导通，RXD2 相当于接地，输出低电平信号。</li>
<li>3V 转 5V 类似则不再赘述。</li>
</ol>
<figure data-type="image" tabindex="4"><img src="https://www.guanngxu.com/post-images/1716801357060.jpg" alt=""></figure>
<p>如果接收端可以接受反相信号，则可以去掉电路中一个三极管，电路会简单一些，如下图所示。</p>
<figure data-type="image" tabindex="5"><img src="https://www.guanngxu.com/post-images/1716801386301.jpg" alt=""></figure>
<h4 id="mos-管电平转换">MOS 管电平转换</h4><p>使用三极管搭建的逻辑电路的优点是速度快，但是其缺点也很明显，就是静态电流损耗很大，所以无法进行大规模的集成。MOS 管的导通电阻很小，其静态功耗可以忽略不计。MOS 管搭建的逻辑电路相比三极管的速度要慢，原因在于三极管是电流控制型器件，而 MOS 管是电压控制型器件，打开 MOS 管会涉及到给 MOS 管寄生电容充电，这个充电过程即是导致速度慢的原因。以 MOS 管搭建的如下电平转换电路可双向传输。</p>
<ol>
<li>5V 电路发送高电平时，MOS 管 D 极电压被拉至 5V，但是 MOS 管的状态和 D 极电压无关，由 VGS 决定导通程度，所以 MOS 管截止，S 极被上拉电阻拉高为 3.3V；</li>
<li>5V 电路发送低电平时，MOS 管 D 极电压为 0V，S 极电压为 3.3V，由于 MOS 管存在体二极管，所以 3.3V 电路被钳位在 0.7V，为低电平；</li>
<li>3.3V 发送高电平时，MOS 管 G 极和 S 极电压都为 3.3V，MOS 管截止，D 极被上拉电阻拉高至 5V；</li>
<li>3.3V 发送低电平时，MOS 管 G 极为 3.3V，S 极为 0V，MOS 管导通，D 极为低电平。</li>
</ol>
<figure data-type="image" tabindex="6"><img src="https://www.guanngxu.com/post-images/1716801423782.jpg" alt=""></figure>
<h3 id="电平转换芯片">电平转换芯片</h3><p>在介绍集成好的电平转换芯片之前，我们需要先了解高阻态、三态门等基本的概念，最后再将三态门等进行组合形成电平转换芯片。</p>
<h4 id="高阻态">高阻态</h4><p><code>高阻态</code>是数字电路中常见的术语，表示电路中的某个节点具有相对电路中其它点更高的阻抗，它是电路的一种特殊的输出状态，但是这个状态既不是高电平也不是低电平。如果用万用表进行测试，可能测到高电平也可能测到低电平，它的状态取决于其后面的电路。</p>
<p>在电路分析时可以将高阻态作开路理解，理论上高阻态是不悬空的，它对地和对电源的电阻都非常大，但实际应用时与引脚悬空几乎一致，因此高阻态的极限可以认为就是悬空。</p>
<p>那么高阻态又是如何造成的呢？通常是由于三态门或三态缓冲器的存在而导致的，当三态门的使能端为低电平时，门电路输出上拉管和下拉管都截止，输出端就处于浮空状态，即没有电流的流动。此时相当于门电路放弃了对输出端电路的控制，其实际电平就由外部电平来决定。</p>
<h4 id="三态门">三态门</h4><p>那么什么又是<code>三态门</code>呢？三态门允许输出端除了高电平和低电平之外呈现高阻态。可以理解为除了输出输出端口外，三态门另外加了一个 EN 引脚，若没有对三态门进行使能，则其输出为高阻态，否则输出由输入决定。</p>
<figure data-type="image" tabindex="7"><img src="https://www.guanngxu.com/post-images/1718096194711.jpg" alt=""></figure>
<p>三态门常常用于总线连接结构。在总线上有多个设备连接到同一条数据线上，但是在特定时间内只有一个设备是有效的，不活动的设备处于高阻态，因此不会对总线上其它设备产生影响。一般来说需要双向数据传输，我们再增加一个三态门即可实现此功能。</p>
<figure data-type="image" tabindex="8"><img src="https://www.guanngxu.com/post-images/1718097966059.jpg" alt=""></figure>
<h4 id="三态门输出结构">三态门输出结构</h4><p>三态门输出端由上 PMOS 管、下 NMOS 管和一个输出供电电压构成，当上管导通时输出低电平，当下管导通时输出为高电平。</p>
<figure data-type="image" tabindex="9"><img src="https://www.guanngxu.com/post-images/1718115240043.png" alt=""></figure>
<p>但是仔细观察会发现输出和输入是反相的，因此为了保证输入与输出同相，我们还需要在输入端加一级反相器。神奇的事情发生了，这不是<code>施密特触发器</code>吗？而且实际应用时为加强电路的抗干扰能力，使用的就是施密特触发器。</p>
<figure data-type="image" tabindex="10"><img src="https://www.guanngxu.com/post-images/1718115813032.png" alt=""></figure>
<p>我们在上文的基础上再加入 EN 使能逻辑，即可实现完整的三态门输出结构，在排故障时可通过该等效电路对现象进行分析。</p>
<figure data-type="image" tabindex="11"><img src="https://www.guanngxu.com/post-images/1718117267080.png" alt=""></figure>
<h4 id="集成芯片">集成芯片</h4><p>在实际应用中肯定不能一位一位的传输，我们假设一次传输一个字节（8 bit），为了简化电路我们把同一个方向的 EN 信号接到一起，如此即只需要两个信号即可控制数据的传输方向了。</p>
<figure data-type="image" tabindex="12"><img src="https://www.guanngxu.com/post-images/1718118135417.jpg" alt=""></figure>
<p>通过控制 EN1 和 EN2 的使能与否，即可控制数据的传输方向。需要注意的是应该避免 EN1 和 EN2 同时都使能的状态，因为在此种状态下左右两侧的数据传输会打架，谁也不知道接收到的信号到底是自己发出去的还是别人发过来的。</p>
<table>
<thead>
<tr>
<th>EN1</th>
<th>EN2</th>
<th>左侧</th>
<th>右侧</th>
<th>输出状态</th>
</tr>
</thead>
<tbody>
<tr>
<td>H</td>
<td>H</td>
<td>使能</td>
<td>使能</td>
<td>无效状态</td>
</tr>
<tr>
<td>H</td>
<td>L</td>
<td>使能</td>
<td>高阻态</td>
<td>数据由左至右</td>
</tr>
<tr>
<td>L</td>
<td>H</td>
<td>高阻态</td>
<td>使能</td>
<td>数据由右至左</td>
</tr>
<tr>
<td>L</td>
<td>L</td>
<td>高阻态</td>
<td>高阻态</td>
<td>隔离，不传输数据</td>
</tr>
</tbody>
</table>
<p>为了避免上述同时使能的状态，我们对电路做一点改进，以保证芯片不会出现稀奇古怪的现象。</p>
<figure data-type="image" tabindex="13"><img src="https://www.guanngxu.com/post-images/1718118147442.jpg" alt=""></figure>
<table>
<thead>
<tr>
<th>OE</th>
<th>DIR</th>
<th>左侧</th>
<th>右侧</th>
<th>输出状态</th>
</tr>
</thead>
<tbody>
<tr>
<td>H</td>
<td>H</td>
<td>使能</td>
<td>高阻态</td>
<td>数据由左至右</td>
</tr>
<tr>
<td>H</td>
<td>L</td>
<td>高阻态</td>
<td>使能</td>
<td>数据由右至左</td>
</tr>
<tr>
<td>L</td>
<td>H</td>
<td>高阻态</td>
<td>高阻态</td>
<td>隔离，不传输数据</td>
</tr>
<tr>
<td>L</td>
<td>L</td>
<td>高阻态</td>
<td>高阻态</td>
<td>隔离，不传输数据</td>
</tr>
</tbody>
</table>
]]></content>
        <author>
            <name>Guanngxu</name>
        </author>
    </entry>
    <entry>
        <title type="html"><![CDATA[LDO 基础知识]]></title>
        <id>https://www.guanngxu.com/L3EbniCYD/</id>
        <link href="https://www.guanngxu.com/L3EbniCYD/"/>
        <updated>2024-05-09T00:52:56.000Z</updated>
        <summary type="html"><![CDATA[
参考内容：
ADI 公司 LDO 电容选型指南
线性和低压降 (LDO) 稳压器

BUCK 电路通过控制占空比来达到降压的目的，添加 LC 二阶低通滤波器将高频部分滤除，即可达到稳定输出直流的目的。但是滤波不能完全滤除高频分量，BUCK...]]></summary>
        <content type="html"><![CDATA[<blockquote>
<p>参考内容：</p>
<p><a href="https://www.analog.com/media/cn/technical-documentation/application-notes/AN-1099_cn.pdf">ADI 公司 LDO 电容选型指南</a></p>
<p><a href="https://www.ti.com.cn/zh-cn/power-management/linear-regulators-ldo/overview.html">线性和低压降 (LDO) 稳压器</a></p>
</blockquote>
<p><a href="https://www.guanngxu.com/br0hmOiiG/">BUCK 电路</a>通过控制占空比来达到降压的目的，添加 LC 二阶低通滤波器将高频部分滤除，即可达到稳定输出直流的目的。但是滤波不能完全滤除高频分量，BUCK 从原理上就决定了其纹波不容易做到很小，其固有的开关频率会导致电源噪声很大，用来给噪声敏感的元器件供电就不合适。</p>
<p>相比 BUCK 来说，LDO（Low Dropout Regulaor：低压差线性稳压器）输出的电压会更加平稳，可以弥补 BUCK 输出纹波大的缺点。</p>
<h3 id="总体框图">总体框图</h3><p>线性稳压器主要由四部分组成，基准源用于提供精准的电压基准、导通器件用于控制从 VIN 到 VOUT 的电流大小、误差放大器将强制反馈节点与基准电压匹配、反馈电阻用于调整以改变输出电压。</p>
<figure data-type="image" tabindex="1"><img src="https://www.guanngxu.com/post-images/1715221368832.jpg" alt=""></figure>
<p>从框图中也可以看到线性稳压器只能用于降压，因此输入电压必须高于输出电压。当然其名字中本身带了<code>低压差</code>的，低压差就意味着少的发热，意味着电源转化效率的提升。<code>线性</code>则是指器件的工作状态，器件的内部模块工作在放大区，放大状态呈线性关系。</p>
<h3 id="工作原理">工作原理</h3><p>线性稳压器的工作可以模拟为两个电阻器和一个用于 VIN 的电源，其中电源用于给负载供电，通过调整可变电阻（导通器件）的阻值来控制负载电阻所获得的电压，整个系统中唯一不变的恒定的参数就是输出电压 VOUT。</p>
<figure data-type="image" tabindex="2"><img src="https://www.guanngxu.com/post-images/1715224425238.jpg" alt=""></figure>
<p>其稳压过程如下图所示，当负载电压升高/降低时，采样电路所采到的电压就跟着升高/降低，传递给误差放大器后通过调节导通器件的导通程度来调节输出电压。</p>
<figure data-type="image" tabindex="3"><img src="https://www.guanngxu.com/post-images/1715224847762.jpg" alt=""></figure>
<h3 id="导通器件">导通器件</h3><p>导通器件常见的有 PMOS、NMOS、BJT 等。BJT 应用于大电流的场景。PMOS 不需要额外的电源轨即可控制其导通程度，但是相比 NMOS 其 RDSon 更大，即 PMOS 架构的 LDO 在芯片本身所消耗的能量会更大。</p>
<figure data-type="image" tabindex="4"><img src="https://www.guanngxu.com/post-images/1715330662896.png" alt=""></figure>
<p>使用 NMOS 作为导通器件时，需要添加辅助电源轨或者使用电荷泵才能将 NMOS 打开。当然电荷泵也有其缺点，虽然电荷泵可以提升 VIN，但是也带来了额外的噪声影响。若采用辅助电源轨时则需要注意，VBIAS 会影响 NMOS 的导通程度，进而影响输出电压的大小。</p>
<figure data-type="image" tabindex="5"><img src="https://www.guanngxu.com/post-images/1715309697553.png" alt=""></figure>
<h3 id="psrr">PSRR</h3><p>PSRR（Power Supply Rejection Ratio）量化了 LDO 抑制任何电源变化传递到其输出信号的能力，也就是 PSRR 决定了输入耦合到输出的噪声有多少。除了 LDO 本身的设计影响 PSRR 外，也可以通过调整 VIN 与 VOUT 之间的差值、输出电容来提高在特定应用（频率）下的 PSRR。</p>
<figure data-type="image" tabindex="6"><img src="https://www.guanngxu.com/post-images/1715242112851.jpg" alt=""></figure>
<figure data-type="image" tabindex="7"><img src="https://www.guanngxu.com/post-images/1715242122909.jpg" alt=""></figure>
<h3 id="输入输出电容">输入输出电容</h3><p>为了确保 LDO 稳定工作，会在 LDO 输入输出端增加旁路电容，并且旁路电容的 ESR 需要很小，即在符合最小电容和最大 ESR 的要求下，使用任何质量良好的电容都可采用。在选择电容时还需要注意由于直流电压偏置、温度变化、制造商容差等需要对电容进行一定的降额。</p>
<p>输出电容除了可以进行滤波外，还会影响负载电流的变化的瞬态响应，采用较大的输出电容可以改善 LDO 对大负载电流变化的瞬态响应。输入电容则可以降低电路对 PCB 布局的敏感性，尤其是在长输入走线或者高源阻抗的情况下。</p>
<p>多层陶瓷电容、固态钽点解电容、铝电解电容通常用作输入和输出旁路电容。多层陶瓷电容具备 ESR 和 ESL 低、工作温度范围宽的优点，但是陶瓷电容中的介质材料具备压电性，振动或机械冲击可能会转化为电容上的交流噪声电压，在极端情况下可能会产生 mV 级的噪声。</p>
<blockquote>
<p>压电性是在某些固体材料（晶体、陶瓷、骨头、DNA、蛋白质等）受到机械应力作用后，在材料中聚集电荷的现象。「压电」即由压力产生的电。</p>
</blockquote>
<p>钽电容的优点是单位体积电容最高（CV 乘积），并且不太容易受到温度、偏执电压、震动效应的影响，在无法容忍压电效应的低噪声应用中，钽电容基本是唯一可行的选择。与陶瓷电容相比，钽电容的泄漏电流要比等值的陶瓷电容大很多倍，不适合超低电流应用。</p>
<p>铝电解电容往往体积较大、ESR 和 ESL 较高，漏电流相对较高，与钽电容一样不受压电效应影响，适合要求低噪声的应用场合，但是铝电解电容在航天应用中禁止使用。</p>
]]></content>
        <author>
            <name>Guanngxu</name>
        </author>
    </entry>
    <entry>
        <title type="html"><![CDATA[半导体功率器件]]></title>
        <id>https://www.guanngxu.com/xdxGDHzvU/</id>
        <link href="https://www.guanngxu.com/xdxGDHzvU/"/>
        <updated>2024-05-04T23:48:51.000Z</updated>
        <summary type="html"><![CDATA[高中时候我们在化学课程中学过元素周期表，「氢氦锂铍硼、碳氮氧氟氖......」倒背如流，在元素周期表的中间三、四、五族元素定义为半导体元素，所谓半导体是根据其导电能力来定义的，我们可以通过一定的半导体工艺来改变其导电能力。
以硅（Si）为例...]]></summary>
        <content type="html"><![CDATA[<p>高中时候我们在化学课程中学过元素周期表，「氢氦锂铍硼、碳氮氧氟氖......」倒背如流，在元素周期表的中间三、四、五族元素定义为半导体元素，所谓半导体是根据其导电能力来定义的，我们可以通过一定的半导体工艺来改变其导电能力。</p>
<p>以硅（Si）为例，硅是处在第四族的元素，它的外部有 4 个电子，所以硅的稳定结构是形成下图所示的及其稳定的共价键结构。</p>
<figure data-type="image" tabindex="1"><img src="https://www.guanngxu.com/post-images/1714867782240.png" alt=""></figure>
<p>硅的两侧对应的是三族和五族的元素，三族的元素意味着外层有 3 个电子，五族的元素意味着外层有 5 个电子。如果把三族元素插入到四族的硅当中，由于硅想形成稳定的四个共价键结构，所以会存在一个空置的位置，我们称之为<strong>空穴</strong>，这个空穴是具备一定的正电荷的能力的，如此就形成了 <strong>P 型半导体</strong>。</p>
<figure data-type="image" tabindex="2"><img src="https://www.guanngxu.com/post-images/1714869273668.png" alt=""></figure>
<p>同理，若使用五族元素与硅进行掺杂，就会多出一个可移动的电子，即存在自由电荷，形成了 <strong>N 型半导体</strong>。</p>
<figure data-type="image" tabindex="3"><img src="https://www.guanngxu.com/post-images/1714869331525.png" alt=""></figure>
<h3 id="pn-结普通二极管">PN 结（普通二极管）</h3><p>当我们把 P 型半导体和 N 型半导体进行组合后，即可得到最基本的二极管（PN 结）。在 P 型半导体中存在高浓度的空穴（正电荷），在 N 型半导体中存在高浓度的电子，浓度高的载流子会自然而然向浓度低的区域进行扩散。</p>
<p>由于载流子扩散，最终会形成一个<strong>势垒</strong>，这是一个空间电荷区，也称之为<strong>耗尽层</strong>。可以发现 PN 结存在 P 区、耗尽层、N 区三个区域，这几个区域都是呈现电中性的，不管是空穴还是电子想要到达另一个区域，都必须要穿过耗尽层，即耗尽层会阻碍空穴和电子的运动，因此整个 PN 结在没有外界干扰的情况下，是不具备导电能力的。</p>
<figure data-type="image" tabindex="4"><img src="https://www.guanngxu.com/post-images/1714869605240.png" alt=""></figure>
<p>当我们从外界施加 N 到 P 的电场时，即 PN 结反偏。此时外界电场与耗尽层电场是同向的，所以在外部电场的作用下，耗尽层的宽度会被加强，于是 PN 结的导电能力就变得更弱，因此就呈现了一个无导电能力的特性。</p>
<p>当然导电只是一种相对情况，即便空间电荷区变宽了，也不能百分百保证说就完全没有导电能力，因为还是有一定的空间电荷浓度，在这样的情况下会有微弱的电流流经 PN 结，意味着系统存在一个反向电流，这就是二极管一个比较重要的<strong>漏电流</strong>参数。</p>
<figure data-type="image" tabindex="5"><img src="https://www.guanngxu.com/post-images/1714870536761.png" alt=""></figure>
<p>当外部施加的电场是从 P 到 N 时，即 PN 结正偏。外界电场的效果是使耗尽层变窄，加强了 P 区内空穴往 N 区内移动的能力，扩散电流远大于漂移电流，形成了一个正向导通电流。</p>
<figure data-type="image" tabindex="6"><img src="https://www.guanngxu.com/post-images/1714870915602.png" alt=""></figure>
<p>最终二极管将呈现如下的导通特性，当正向电压大于势垒电压时，二极管开始导通。当施加反向电压时，二极管将截止，当反向电压大到一定程度后，二极管就会被反向击穿，即二极管损坏的过程。</p>
<figure data-type="image" tabindex="7"><img src="https://www.guanngxu.com/post-images/1714871441450.png" alt=""></figure>
<h3 id="功率二极管">功率二极管</h3><p>既然谈到了「功率」二字，那么更加关注的就是二极管承载电流、电压的能力了。如何把二极管承载电流、电压的能力加强呢？根据上文关于二极管的介绍可以知道，将耗尽层加宽可以承载更大的电压。</p>
<p>图中中间 n- 为轻度参杂区域，下面 n+ 为重度参杂区域，这个参杂就导致了耗尽层的加宽，当然也导致导通损耗更大，不过也正因为如此，功率二极管才更加能耐压。</p>
<figure data-type="image" tabindex="8"><img src="https://www.guanngxu.com/post-images/1714874810469.jpg" alt=""></figure>
<p>我们以非同步 BUCK 电路为载体，来说明一下功率二极管的变化过程。</p>
<figure data-type="image" tabindex="9"><img src="https://www.guanngxu.com/post-images/1715335896388.jpg" alt=""></figure>
<ol>
<li>图中（1）部分指二极管导通，有一个小小的二极管导通压降，因此曲线没有贴着 x 轴；</li>
<li>图中（2）的位置由于二极管承受的是反向电压，此时它关断了，所以电压为负；</li>
<li>图中（3）二极管需要经历一个从没有电压到有外加电压的变化，当电压加到二极管上时，二极管中的载流子流动的趋势逐渐增大，宏观表现出来是电阻慢慢变小的过程，但是电流保持不变，所有会有一个小尖峰，这一小段时间也会导致整体功率的损耗，开关频率越高，这个导通过程导致的损耗越多；</li>
<li>图中（4）处伴随系统从通到断的状态变化，大规模载流子需要进行重新分配，这个重新分配表现出来就是电流，而且这个电流与主电流相反，所以会看到一个反向的电流，而且这个反向电流会施加在主电路里面。这一段反向电流又分为两部分，下降阶段是之前外加电压时，PN 结中从 P 区域移动到 N 区域的载流子移除（恢复）过程，即从正偏到反偏的过程，正偏时空间电荷区非常非常窄，此时要进入反偏状态，空间电荷区需要加强，载流子需要重新分配，外部激励会移除不必要的空间电荷。电流上升的过程，即二极管又变成一个耐压器件了，也就是空间电荷区加宽，更多的载流子会不均匀的分布在两端。整个过程不可避免的需要移动电荷，而电荷的聚集效应可以认为就是一个电容的效应，当我们需要施加电压时，电压的增加就会需要额外的电荷，电荷不断聚集提供相反电荷，使其电压不断增加，以致增加到刚好截止输出电压为止。</li>
</ol>
<h3 id="mos-管">MOS 管</h3><p>以 NMOS 为例，它以 P 型半导体衬底，以 N 型半导体作为导电沟道，金属部分作为栅极（Gate），氧化部分（SiO2）作为绝缘层，两端分别为源极（Source）和漏极（Drain），从物理结构可以看出 MOS 管的源极和漏极是可以互换的，不像三极管有严格的顺序。</p>
<p>在栅极和源极施加电压，随着电压的不断增大，导电沟道将逐渐形成，当导电沟道刚好形成时的电压，称之为开启电压。外加电压继续增大，导电沟道将变得越来越宽，即导电能力越来越强。</p>
<figure data-type="image" tabindex="10"><img src="https://www.guanngxu.com/post-images/1715354684295.png" alt=""></figure>
<p>PMOS 相比 NMOS 更加容易驱动，只需要 VGS 小于一定值即可导通。但是 PMOS 的导通电阻比 NMOS 要大，并且成本也比 NMOS 要高，所以比 NMOS 的实际应用场景要少许多。</p>
<figure data-type="image" tabindex="11"><img src="https://www.guanngxu.com/post-images/1715354184895.jpg" alt=""></figure>
<h3 id="功率-mos-管">功率 MOS 管</h3><figure data-type="image" tabindex="12"><img src="https://www.guanngxu.com/post-images/1715387535669.png" alt=""></figure>
<p>对比前文普通 MOS 管，可以看到源极、栅极、漏极是分开的，顶上那个灰色的板子是金属板。而功率 MOS 管在这个基础上做了一点创新，下图中的阴影部分就是金属板，可以发现总共只有两个金属板，上面的金属板把 N 区和 P 区都给连起来了，所以即使在栅极没有加电压的时候，也会存在一个天然的二极管通道，但是普通 MOS 管是没有体二极管通道存在的。同时由于是功率 MOS 管，所以也会想办法将耗尽层加宽，以增加其耐压能力。</p>
<figure data-type="image" tabindex="13"><img src="https://www.guanngxu.com/post-images/1715388986257.jpg" alt=""></figure>
<p>体二极管和耐压能力的加强是功率 MOS 和普通 MOS 的区别。</p>
<figure data-type="image" tabindex="14"><img src="https://www.guanngxu.com/post-images/1715389375099.png" alt=""></figure>
<p>功率 MOS 管的正向导通能力就是涉及「场效应」了，所谓的场效应即意味着外部可以通过电场来控制其内部载流子的浓度，在栅极施加正电压时就会产生一个电子的导电沟道，由于整体是 N 型半导体衬底，所以整体也就形成了一个电子的导电沟道，并且该沟道支持电子的双向移动。</p>
<figure data-type="image" tabindex="15"><img src="https://www.guanngxu.com/post-images/1715389968409.png" alt=""></figure>
<p>如下图所示是功率 MOS 管的等效电路模型。其主要损耗由三部分组成，分别为导通损耗、开关损耗（开通损耗和关断损耗）、驱动损耗。其中导通损耗与开关损耗容易理解，驱动损耗作何理解呢？MOS 并不像二极管是一个被动型器件，MOS 管开或关的行为都需要能量作为代价，就好比要打开机械开关需要用手去按压，这个过程所消耗的能量就是驱动损耗。</p>
<figure data-type="image" tabindex="16"><img src="https://www.guanngxu.com/post-images/1715391233428.jpg" alt=""></figure>
<h3 id="晶体管">晶体管</h3><p>二极管只有一个 P 型半导体和一个 N 型半导体结合，如果再加一个 N 型半导体（或 P 型半导体）即构成了晶体管（三极管），晶体管有集电极、发射极、基极三个极。</p>
<figure data-type="image" tabindex="17"><img src="https://www.guanngxu.com/post-images/1715394337004.png" alt=""></figure>
<p>需要注意的是三极管的集电区和发射区掺杂浓度是不一样的，其中基区多子少且做的很薄，而发射区的多子浓度很高，集电区多子浓度相对较低但面积大。不管三极管是正接还是反接，三极管都处于截止状态，这是因为三极管可以看作两个二极管反向相连，不论如何接都会有一个二极管处于截止状态。</p>
<figure data-type="image" tabindex="18"><img src="https://www.guanngxu.com/post-images/1715394348459.png" alt=""></figure>
<p>为了能让三极管导通，我们在基极和发射极再施加一个电压，此时二极管开始导通，发射区的自由电子就可以源源不断的流向基区，但是基区的掺杂浓度很低且很薄，基区短时间内吸收不了太多的电子，只有一少部分电子能与空穴复合形成基极电流，而大部分被吸引到了集电区，形成集电极电流，也就是三极管的输出电流。</p>
<figure data-type="image" tabindex="19"><img src="https://www.guanngxu.com/post-images/1715397133346.png" alt=""></figure>
<p>流过基极的电流越大，流到基区的自由电子也就越多，相应的被吸引到集电区的电子也就更多，这就是三极管小电流控制大电流的原理。基区做的很薄是为了让发射区的电子更容易进入集电区，浓度很低视为了形成更小的基极电流，这样才会有更多的自由电子流向集电区。</p>
<h3 id="igbt">IGBT</h3><p>三极管工作时涉及载流子的注入和抽离所以会很慢，由于其性能的关系正在逐步退出历史舞台，因此需要对其进行改进，改进后的器件就是 IGBT，如下图所示。</p>
<figure data-type="image" tabindex="20"><img src="https://www.guanngxu.com/post-images/1715401686567.png" alt=""></figure>
<p>可以发现 IGBT 是一个受 MOS 管控制的 BJT，即同时继承了 MOS 管快速和 BJT 大电流的优点。当然，它也有缺点，并且缺点主要来自于 BJT 关断较慢的问题，因为当 MOS 管门级信号撤出时，并不能立马把电流都抽走，所以电流会经历一段下降时间。</p>
<figure data-type="image" tabindex="21"><img src="https://www.guanngxu.com/post-images/1715402444954.png" alt=""></figure>
]]></content>
        <author>
            <name>Guanngxu</name>
        </author>
    </entry>
</feed>